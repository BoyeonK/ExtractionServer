프롬프트 138개를 읽었습니다. 평균 124자, 절반 이상(56%)이 100자 미만인데도 정보 밀도가 상당히 높은 편입니다. 결론부터 말하면 설계 대화는 매우 잘하고, 실행 지시가 약합니다.

잘하고 있는 습관

1. 구현 전 반박 요청 (15회)
"놓친 부분이나 더 나은 방향이 있으면 제안해줘", "반론이 있을까?"를 습관적으로 붙이십니다. 이건 상위 5% 수준의 습관입니다. 대부분은 바로 구현을 시켜서 잘못된 전제 위에 코드를 쌓습니다.

2. 번호 매긴 요구사항 명세 (22회)
Fire() 구현이나 귀환 요청 프로토콜처럼 1~6번으로 나눠 주신 케이스들은 사실상 스펙 문서입니다. 특히 응답도 "1. 맞아 오타야. 2. SpreadRecoveryRate는..." 식으로 번호를 맞춰 주셔서 왕복이 짧아집니다.

3. 이해 확인 후 진행
"즉, 2번 방법은 반동으로 인한 시점 변경을 즉시 적용하지 않고 해당 수치를 가지고 있다가... 라는 거지?" → "좋아 이 방법으로 구현해줘". 이 재진술 단계가 있어서 엉뚱한 구현이 나오는 걸 막고 있습니다.

4. 세션 간 교차 검증
ignoreParentClaudeMdFiles 건에서 서버 세션과 클라이언트 세션의 답변이 충돌하자 "어느 쪽이 사실인지 검증이 필요해"라고 하신 것 — LLM 답변을 기본적으로 의심하는 태도가 자리잡혀 있습니다.

5. 범위 통제
"클라이언트측 구현은 현재 작업 영역 밖이야", "TODO 주석으로만 적어두자", "데미지 구현 부분은 아직 진행하면 안돼" — 스코프 크립을 능동적으로 막고 계십니다.

개선할 부분

1. 검증 지시가 사실상 없습니다 (138개 중 1개)
"빌드해줘", "테스트해봐", "실제로 확인해줘"에 해당하는 지시가 한 번뿐입니다. 지금은 Claude가 코드를 쓰고 → 사용자가 유니티에서 돌려보고 → 버그를 말로 설명하는 루프인데, 이 왕복이 가장 비쌉니다. 특히 서버 쪽 C++은 컴파일 검증만 시켜도 되돌아오는 횟수가 줄어듭니다.

개선: 구현 후 서버 빌드까지 돌려서 컴파일 에러가 없는지 확인해줘.

2. 버그 리포트가 증상만 있고 재현 조건이 없습니다

현재: "현재 쭉 발사를 진행한 경우, 어느 순간엔가 마우스 아래 조작이 먹히지 않는 현상이 있어."

"어느 순간엔가"가 핵심 정보인데 빠져 있습니다. 몇 발째인지, 반동 누적값이 얼마일 때인지, 상한/하한에 걸렸을 때만인지 — 이걸 Claude가 추측하느라 엉뚱한 곳을 고칠 여지가 큽니다.

개선: 증상 / 재현 절차 / 기대 동작 / 실제 동작 / 의심 파일 5줄 템플릿을 고정해서 쓰기. 값을 모르면 "먼저 로그를 심어서 원인 지점을 특정해줘"로 시작.

3. 파일 경로 명시가 6회뿐
SetArmor, _currentShield, PlayerController 같은 심볼만 던지시는데, Claude Code는 매번 탐색부터 합니다. @Extraction/Scripts/Player/PlayerController.cs 형태로 앵커를 주면 탐색 토큰도 줄고 동명 심볼 오수정도 막힙니다.

4. 임시 코드에 회수 표시가 없음 (3회)
"테스트를 위해서 탄약 1발 차감을 진행하지 않으려고 해. 잠시 주석처리해줘" 같은 요청이 반복되는데, 되돌리라는 지시는 로그에 없습니다. 프로젝트에 죽은 채로 남기 쉽습니다.

개선: // TEMP(test, 2026-08-12): 탄약 차감 비활성화 형식으로 마커를 강제하고, progress.md에 "임시 변경 회수 목록" 섹션을 두기.

5. 네이밍 협의에 턴을 많이 씁니다
FIRE → "추상적이야" → "후보 추천해줄래?" → D2C_BROADCAST_WEAPON_FIRE까지 4턴. 이미 규칙(C2DRequest*, D2CResponse*, D2CBroadcast*, D2CNotify*)이 머릿속에 잡혀 있으니 CLAUDE.md에 한 번 박아두면 이 왕복 전체가 사라집니다.

6. "반론이 있을까?"의 한계
좋은 습관이지만 열린 질문이라 모델이 형식적인 우려를 나열하기 쉽습니다. 이미 결정을 굳힌 상태에서 물으실 때가 많은데, 그럴 땐 오히려 노이즈가 됩니다.

개선: 강도를 나눠 쓰기.

설계 초기 → 이 설계가 깨지는 구체적 시나리오를 3개만 제시해줘. 없으면 없다고 해.
결정 후 → 결정했어. 반론 말고, 이 결정 때문에 나중에 손대야 할 파일만 알려줘.
지금 바로 적용할 3가지
CLAUDE.md에 패킷 네이밍 규칙 + 빌드 검증 명령어 + TEMP 마커 규칙 3개 섹션 추가
버그 리포트 5줄 템플릿 고정
구현 요청 끝에 구현 후 빌드/컴파일까지 확인해줘 한 줄 붙이기 습관화