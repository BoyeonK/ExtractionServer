# 진행 상황 정리 (2026-08-19 업데이트)


## 완료된 것들

### 네트워크 / 패킷
- [x] (2026-08-15 #0) 시신 컨테이너 구현 — 사망 시 인벤토리·장착·탄창 전부가 시신으로 넘어간다. 스폰 지점은 `GameRoom::DetachPlayer()`의 TODO 자리(오브젝트 제거 **직전**, `position`과 인벤토리를 아직 함께 쥐고 있는 유일한 지점)이고 기존 `Clear()`는 그대로 뒀다 — DEAD 경로는 DB에서도 해당 범위를 삭제하므로 '이동'의 의미가 성립한다. **새 패킷·새 핸들러·`.proto` 변경이 0**인 것이 이 작업의 성격을 규정한다 — `FindNonplayerObject()`가 `_dynamicObjects`를 뒤지고 `dynamic_cast<Container*>`가 통과하므로 `C2DRequestOpenContainer`·`C2DRequestInteractContainerObject`가 무수정으로 시신에 동작하고, `object_type`이 `int32`라 `ObjectType::Corpse = 3` 값 추가만으로 클라이언트까지 전달된다. 배치는 **압축(compaction)** — 주무기 → 주탄창 → 보조무기 → 보조탄창 → 방어구 → 인벤토리 0~24 순으로 비어있지 않은 슬롯만 0번부터 채운다. 인덱스 1:1 매핑을 쓰지 않은 이유는 인벤토리 25칸 + 장착·탄창 5칸 = 30칸이 `DEFAULT_CONTAINER_VOLUME`(30)과 **정확히 같아 여유가 0**이기 때문이고, 시신은 약탈 대상이라 원래 배치를 보존할 이유도 없다. 탄창은 `blueprintId`가 이미 탄약이라 그대로 놓으면 약탈자에게 탄약으로 보여 별도 변환이 필요 없다. **빈손이어도 시신을 생성한다** — 사망 위치의 표지가 예외 없이 하나로 통일되고(클라이언트는 `ObjectType 3` 하나만 보면 된다), 슬롯 30칸을 가진 빈 시신은 짐을 버릴 곳으로도 쓰인다. 사망이 곧 이탈이라 한 사람이 두 번 죽지 않고 룸 인원이 4명이므로 **TTL이 없어도 룸당 시신은 최대 4개**이며, 반대로 TTL을 두지 않은 덕에 약탈 중 시신이 사라져 `_interactingContainerId`가 유령을 가리키는 문제가 아예 성립하지 않는다. 당사자 제외는 코드 없이 성립한다 — `DetachPlayer()`가 첫머리에서 `SetSessionState(LEFT)`를 하므로 `Broadcast()`의 INPLAY 필터가 죽은 본인을 자연히 거른다. 수명은 `~GameRoom()`의 `_dynamicObjects` 해제가 맡고, 늦게 들어온 세션은 `FillDynamicObjects()`가 시신을 함께 실어 보낸다. 스폰 헬퍼는 `GameRoom::SpawnCorpseContainer()` protected 하나로 모아 파생 룸 2종에 같은 코드가 생기지 않게 했다 (`UnityGameObject.h`, `UnityGameObjects/CorpseContainer.h`, `GameRoom.h/cpp`)
- [x] (2026-08-15 #1) 장착 변경의 서버 상태 반영과 룸 통보 — **주운 무기로 사격이 되지 않던 버그를 함께 잡았다.** `Handle_C2D_RequestEquipItem`이 방어구만 `PlayerObject`에 반영하고(`SetArmor`) 무기는 `PlayerInventory`만 고쳐서, `PlayerObject::_primaryWeaponId`가 **스폰 시점 값에 고정**돼 있었다(원인은 `SetWeapons()` 호출부가 스폰 한 곳뿐이었던 것). 그 결과 `Handle_C2D_RequestWeaponFire`의 `weapon_dbid` 검사가 실패해 바꾼 무기로 쏘면 **deny 패킷도 없이 `return false`** 였다 — 시신 컨테이너로 무기 획득이 핵심 동선이 되면서 드러난 문제라 이번에 처리했다. 블록을 재구성해 `PlayerObject`를 한 번만 조회하고 방어구/무기를 대칭으로 갱신한다(해제 시 `blueprintId == 0`이 들어가 맨손도 반영된다). 통보는 `D2CNotifyEquipmentChanged { object_id, weapon_id, armor_id }` 신설(pktId 37)로 `BroadcastExcept` — 당사자는 `D2CResponseEquipItem`을 이미 받는다. **reliable로 잡았다**: 한 장을 놓치면 남의 화면에 옛 장비가 영구히 남는다. 무기 필드를 주/보조 둘이 아니라 **현재 든 무기 하나**로 한 이유는 클라이언트가 둘 중 어느 쪽이 손에 있는지 알 수단이 없기 때문이다 — 스폰 때 받는 `D2CSpawnPlayerObject.weapon_id`와 같은 의미로 맞췄다 (`External_Protocol.proto`, `enum.h`, `ClientPacketHandler.h/cpp`)
- [x] (2026-08-15 #2) 무기 교체 패킷 구현 — `C2DRequestSwitchWeapon { target_slot, my_inventory_version }` 신설(pktId 38). **토글이 아니라 절대 지정**인 것이 설계의 핵심 — `PlayerSession::UpdateRRecvState()`는 32비트 슬라이딩 윈도우로 **중복만 거르고 순서는 보장하지 않으며**, reliable(rSeqNum)과 unreliable(uSeqNum)은 시퀀스 공간이 분리돼 두 채널 사이에는 순서 개념 자체가 없다. 절대 지정이면 재전송·순서 역전에도 결과가 같고 같은 슬롯 재지정이 멱등이다. 장착 조작과의 순서 역전은 `my_inventory_version`으로 막는다 — `PlayerInventory`의 변경 함수 11곳이 예외 없이 `_inventoryVersion`을 올리므로 장착이 끼어들었으면 그 전에 보낸 교체 요청이 자동으로 거부된다(`C2DRequestEquipItem`이 쓰는 관용구와 동일). **D2C 패킷은 만들지 않았다** — 성공은 `Broadcast`(본인 포함), 거부는 요청자에게만 현재 실제 상태를 담아 `D2CNotifyEquipmentChanged`를 보낸다. 이 패킷이 이미 "네가 든 것은 이것"이라는 서버 권위 선언이고 클라이언트가 거부에 할 수 있는 일은 서버 상태로 맞추기뿐이라 사유 마스크를 둘 실익이 없다(사유는 서버 로그에 남긴다). 함께 `PlayerObject::SetWeapons()`가 `_isUsingPrimary`를 무조건 재계산하던 것을 **들고 있던 무기가 실제로 사라졌을 때만 옮기도록** 고쳤다 — 그대로 두면 장착 조작 한 번마다 손에 든 무기가 주무기로 되돌아간다. 초기값이 `true`라 스폰 시점 첫 호출의 결과는 옛 식과 세 경우(주무기 있음/보조만 있음/맨손) 모두 일치하고, 달라지는 것은 두 번째 호출부터다. 토글 `SwitchWeapon()`은 호출부가 0곳이었고 절대 지정 설계를 우회하는 함정이라 `SetUsingPrimary(bool)`로 교체했다 (`External_Protocol.proto`, `enum.h`, `UnityGameObjects/PlayerObject.h/cpp`, `ClientPacketHandler.h/cpp`)
- [x] (2026-08-18 #0) 비플레이어 전투 오브젝트의 사망 경로 구현 — 착수 전 확인에서 **이 항목이 "죽음 처리"가 아니라 비플레이어 전투 오브젝트를 처음 만드는 일**임이 드러났다. `CombatObject`의 파생은 `PlayerObject` 하나뿐이었고(`TestItemBox`는 `Container` 파생이라 전투와 무관), `Handle_C2D_RequestWeaponFire`가 `FindPlayerObject()`만 불러 **피해가 애초에 도달하지 않았으며**, `ProcessLeaves()`는 `_playerObjects`만 훑으므로 TODO가 달려 있던 `pSession == nullptr` 분기는 AI가 도달할 수 있는 자리가 아니었다(그 자리는 "PlayerObject인데 세션이 사라진" 이상 상태를 잡는 곳이라 TODO를 그대로 제거했다). **훅을 둘로 나눈 것**이 이 작업의 골격이다 — `OnDeath()`는 `_deathPending` 표식만 남기고, 흔적은 룸을 인자로 받는 `OnDeathResolved(GameRoom&)`가 맡는다. 흔적을 남기려면 `SpawnDynamicObject()`·`Broadcast()`가 필요한데 `CombatObject`에 `GameRoom*` 역포인터를 심으면 오브젝트가 룸의 컨테이너를 직접 건드릴 수 있게 되어 "순회 중 변형 금지" 규약이 무너진다. 훅의 **빈 기본 구현이 곧 "흔적 없음"** 이라 남길 오브젝트만 override하면 된다. `PlayerObject`는 이 훅을 쓰지 않는다 — 시신 컨테이너는 인벤토리가 필요한데 그건 오브젝트가 아니라 세션이 쥐고 있어 `DetachPlayer()`가 계속 담당한다. **회수는 즉시**(매 틱 sweep 없음)이되 `TakeDamage()` 안에서 하지 않는다 — 그 안에서 `delete`하면 호출부인 `Handle_C2D_RequestWeaponFire`가 직후에 같은 포인터로 `GetCurrentHp()`·`GetCurrentShield()`를 읽어 use-after-free가 된다. 그래서 `TakeDamage()`는 표식만 남기고 핸들러가 오브젝트를 다 쓴 뒤 `DestroyDeadObject()`를 지시한다(같은 틱, 지연 없음). `DestroyDeadObject()` 안에서는 **반복자를 훅에 넘기지 않는다** — 훅이 흔적을 스폰하면 `_dynamicObjects`가 rehash되어 반복자가 무효화되므로 포인터를 빼고 erase를 먼저 한 뒤 훅을 부르고 마지막에 delete한다(통보 순서는 시신 경로와 같은 흔적 스폰 → 본체 제거). 통보는 `D2CNotifyDespawnObject { object_id }` 신설(pktId 39, reliable — 한 장을 놓치면 부서진 오브젝트가 남의 화면에 영구히 남는다). `D2CDespawnPlayerObject`를 재사용하지 않은 이유는 `DespawnReason`의 RECALLED/DISCONNECTED가 비플레이어에 무의미하고 클라이언트가 플레이어와 그 외를 다른 컨테이너로 들고 있을 것이기 때문이며, `reason` 필드는 사유별 연출이 범위 밖이라 넣지 않았다(필요해지면 필드 2번 추가가 하위 호환). `CanBeStaticObject()`를 `GameRoom`에 두어 파생 룸 2종이 한 줄씩 호출하게 했다(시신 스폰 헬퍼를 하나로 모은 것과 같은 형태). **AI는 서버에서 다루지 않기로 했고 사망 연출도 제외**했다 — 연출은 오브젝트마다 달라 결국 `OnDeathResolved()` 안에서 각자 처리하는 형태가 된다. 검증용 구체 클래스도 만들지 않기로 해서 **이 경로는 첫 파괴 가능 오브젝트가 생기기 전까지 실행되지 않는다** (`UnityGameObjects/CombatObject.h`, `UnityGameObjects/PlayerObject.h`, `GameRoom.h/cpp`, `ClientPacketHandler.h/cpp`, `External_Protocol.proto`, `enum.h`)
- [x] (2026-08-18 #1) 플레이어 상태 브로드캐스트를 `GameRoom`으로 올림 — **윈체스터 룸에서 `D2CUpdatePlayerStates`가 한 장도 나가지 않던 것**을 함께 잡았다. `WinchesterGameRoom::Update()`가 빈 함수였고 상태 브로드캐스트는 `TestGameRoom::Update()`에만 있어서, 실제 게임 맵에서는 **다른 플레이어가 화면에서 전혀 움직이지 않았다**(튜토리얼에서만 정상 동작). 블록을 `GameRoom::BroadcastPlayerStates()` protected 로 추출하고 `Update()`를 순수 가상에서 구현을 가진 가상 함수로 바꿔 베이스가 이를 부르게 했다. 파생 2종의 `Update()`는 **선언·정의째 삭제** — 베이스와 하는 일이 같아져서 남기면 "아무것도 안 하고 베이스만 부르는 함수"가 둘 생긴다. 대안으로 **NVI(비가상 `Update()` + 순수 가상 `OnUpdate()` 훅)를 검토했으나 채택하지 않았다** — 파생이 베이스 호출을 빠뜨릴 여지를 구조적으로 없앨 수 있지만, 나중에 코드를 다시 볼 때 "`Update`가 왜 둘이지?"라는 의문을 부르는 쪽의 유지보수 비용이 더 크다는 판단. 그 대신 순서 규약을 선언 옆 주석으로 남겼다(아래 「진행 고려사항」에도 기록). 호출부인 `DediServerService::UpdateGameRooms()`는 무수정 (`GameRoom.h/cpp`)
- [x] (2026-08-18 #2) 가해자 전달 경로와 킬 피드 구현 — `CombatObject::TakeDamage()`가 가해자를 받지 않아 `OnDeath()` 시점에 **누가 죽였는지 서버도 모르던 것**을 뚫었다. 인자 하나(`attackerObjectId`)를 추가하고 함수 첫머리에서 `_lastAttackerId`에 대입한다(실드에 전부 흡수돼도 갱신되지만 킬 크레딧에는 사망을 유발한 호출의 값이 남아 결과가 같다). **`TakeDamageByPlayer`/`TakeDamageByObject` 2분할을 검토했으나 기각** — ① 오브젝트 id 공간이 `GameRoom::_nxtObjectId` 하나로 플레이어·정적·동적을 통틀어 발급하므로 `uint32_t` 하나로 가해자가 유일하게 특정되고, 종류 판별은 `FindPlayerObject()`→`FindNonplayerObject()` 순 조회로 이미 성립한다 ② 경우가 2개가 아니라 3개다(플레이어 / 오브젝트 / **가해자 없음** — 낙하·지역 피해). 인자 하나면 `hit_object_id`가 쓰는 `0xFFFFFFFF` 센티널을 `NO_ATTACKER`로 그대로 재사용해 규약이 하나로 유지되지만, 2분할은 세 번째를 표현할 자리가 없다 ③ 실드→관통→감소율→사망 판정 본문이 통째로 복제되어 밸런스 수식을 두 곳에서 고쳐야 하고, 어긋나면 "플레이어에게 맞을 때와 함정에 맞을 때 데미지가 다른" 형태로 조용히 나타난다 ④ 두 시그니처가 `(int32_t, uint32_t)`로 같아 이름으로만 갈리므로 호출부가 반대쪽을 불러도 컴파일이 통과한다(타입 안전성 이득 0). 가해자 종류로 갈리는 처리(팀킬, 히트마커 회신, PvP 집계)는 전부 데미지 계산이 아니라 **통보 시점의 분기**라 `_lastAttackerId`를 읽어 그 자리에서 나누면 된다. **통보 지점은 사격 핸들러가 아니라 `GameRoom::DetachPlayer()`의 DEAD 분기** — 플레이어 사망은 사격 틱에 확정되지 않고 다음 틱 `ProcessLeaves()`가 `IsDeathPending()`을 보고 `MarkLeaving(DEAD)`를 걸기 때문이며, 죽음이 관측되는 유일한 자리에 두어야 낙하 피해 등 다른 사망 원인이 생겨도 킬 피드 코드가 늘지 않는다. **대상이 둘로 갈린다** — `DetachPlayer()`가 첫머리에서 `SetSessionState(LEFT)`를 해 `Broadcast()`의 INPLAY 필터가 정작 가장 알아야 할 피해자를 거른다. 그래서 피해자에게는 기존 `D2CNotifyHealthChange`에 `attacker_object_id`(4번)를 추가해 **사격 시점에** 전달하고(그 경로는 이미 있고 사망 시 `SetLeaveNotifyRSeq()`가 도달까지 보장하며, **피격 방향 표시**가 덤으로 열린다), 나머지 룸원에게는 `D2CNotifyPlayerKilled { victim_object_id, killer_object_id }` 신설(pktId 40, reliable — 한 장을 놓치면 킬 피드에 영구히 구멍이 남는다)을 `Broadcast`한다. 킬러는 나머지에 포함되어 `killer_object_id == 자기 objectId`로 본인 킬을 인식한다 — `D2CDespawnPlayerObject`의 "당사자는 사유별 통보를 이미 받으므로 제외" 관용구와 같은 형태다. **무기 id는 싣지 않았다** — 통보가 사격보다 한 틱 뒤라 그 사이 교체하면 틀린 값이 나가고, 클라이언트가 `D2CSpawnPlayerObject.weapon_id` + `D2CNotifyEquipmentChanged`로 이미 추적할 수 있다(오차 구간은 킬 직전 교체 한 틱, 대가는 아이콘 하나). 서버 권위로 옮기고 싶어지면 `_lastAttackerWeaponId` 필드와 proto 필드를 더하는 순수 추가 변경이라 지금 뺀 것이 재작업이 되지 않는다. 비플레이어 오브젝트의 킬 크레딧은 범위 밖이다 — `DestroyDeadObject()`는 `D2CNotifyDespawnObject`를 그대로 쓰고, `_lastAttackerId`가 `CombatObject`에 있으므로 파괴 가능 오브젝트가 생길 때 필드 추가만으로 열린다. 함께 `TakeDamage()`의 감소율 적용 범위를 **실드 잔량이 있을 때만 유효**로 확정했다(기존 동작이 의도였으므로 코드 변경 0) (`UnityGameObjects/CombatObject.h`, `External_Protocol.proto`, `enum.h`, `ClientPacketHandler.h/cpp`, `GameRoom.cpp`)

- [x] (2026-08-18 #3) 끊김 감지를 헤더 `timestampEcho` 기반으로 재설계 — **조용한 순간에 끊기면 감지가 아예 열리지 않던 구멍**을 막았다. `MarkLeaving(DISCONNECTED)`의 트리거가 재전송 MAX_RETRY 실패 하나뿐이었는데, 주기적으로 나가는 패킷은 전부 unreliable이고(`D2CUpdatePlayerStates`·`D2CHeartBeat`) reliable은 스폰·장착·피격처럼 사건이 있을 때만 나간다. 그래서 아무 일도 없는 순간에 회선이 끊기면 `_pendingReliable`이 비어 후보가 없고 MAX_RETRY에 도달할 일도 없어, 세션이 INPLAY로 **영구히 남는다** → `CheckAllLeft()`가 성립하지 않아 룸이 회수되지 않고 `_allocatedPlayers`도 반납되지 않는다(2026-08-14 #1이 막았던 프로세스 누적 증가가 이 경로로 재현). **수신 시각(`_lastRecvTime`) 기반을 먼저 검토했으나 기각** — `_clientAddr`은 IPC로 받은 값으로 한 번만 설정되고 수신 패킷으로 갱신되지 않는다(갱신 setter 자체가 없다). 그래서 NAT 리바인딩이 일어나면 클라이언트 패킷은 새 포트에서 계속 도착하는데 서버는 옛 주소로만 쏘는, **상향은 살아있고 하향만 죽은** 상태가 되고 수신 시각 기반은 그 세션을 영원히 건강하다고 본다. `timestampEcho`는 왕복을 요구하므로 양방향을 증명한다. **새 필드는 필요 없었다** — `_lastEchoTs`가 이미 있고 `UpdateRtt()`가 매 수신마다 갱신하고 있어 getter만 열었다(`ClientPacketHandler`는 한 줄도 고치지 않았다). 값이 서버 시계 도메인이라 `NowMs()`와 그대로 비교되고 uint32 wrap도 뺄셈으로 자연 처리된다(`GetRetransmitCandidates()`가 쓰는 관용구와 동일). **임계값 6초**는 클라이언트가 0.1초 주기로 상태를 보내므로 연속 60회 유실에 해당한다 — 이 결합이 상수의 유일한 근거라 선언 옆에 남겼다. **유예(`LEAVE_GRACE_MS_DISCONNECTED`)는 개념째 제거** — 임계값 자체가 "이 시간 동안 응답이 없었다"는 관찰이라 그 위에 유예를 얹으면 같은 대기를 두 번 센다. 감지 구간에도 아바타가 INPLAY로 남아 전투 로그아웃 완화는 이미 제공되고, 유예에 남는 역할은 오탐 흡수뿐인데 60회 연속 유실 뒤라면 오탐이 아니다. **MAX_RETRY도 제거** — 그것이 사실상 생존 판정 대용이었으므로 전용 감지자가 생긴 이상 불필요하고, 살아있는 세션에서 reliable을 도중에 포기하면 그 클라이언트는 스폰·디스폰·장착 변경 중 하나를 **영구히 놓친 채** 플레이하게 되어 상태 발산이 조용히 남는다. 이제 살아있는 세션에는 무한 재전송하고 죽은 세션은 6초 안에 회수되므로 큐가 무한히 자라지 않는다. 유예 제거가 **연쇄 삭제**를 불렀다 — `IsDetachReady()`의 취소 분기가 도달 불가가 되고, 그 함수가 `CancelLeaving()`·`HasRecvSince()`의 유일한 호출부였으며, `HasRecvSince()`가 `_lastRecvTime`의 유일한 독자여서 그 필드와 대입 5곳까지 함께 걷어냈다(`retryCount`도 MAX_RETRY 제거로 write-only가 되어 삭제). 흔적 둘을 남겼다 — ① `RegisterReliable()`에서 in-flight가 34장이 되는 순간 1회 로그(아래 33장 항목의 선행 지표) ② `echo == 0` 건너뛰기 자리에 `OPTION:`. 덤으로 서명이 깨진 클라이언트도 6초 뒤 정리된다 — `UpdateRtt()`가 서명·중복 검사를 통과한 패킷에서만 불리므로 `_lastEchoTs`가 멎는다 (`GameRoom.h/cpp`, `PlayerSession.h/cpp`, `DediServerService.cpp`)
- [x] (2026-08-19 #0) 남의 방어구 정보를 은닉하도록 장비 통보를 좁힘 — `D2CNotifyEquipmentChanged`를 **`D2CNotifyWeaponChanged`(pktId 37 유지)** 로 바꾸고 `armor_id`(3번)를 제거했다. HP·실드 **수치**는 원래부터 남에게 가지 않았다(`PlayerState`에 없고 `D2CNotifyHealthChange`는 `pHitSession->Send()`로 피격 당사자에게만 간다). 그런데 `armor_id`만 `BroadcastExcept`로 나가고 있었고, 이것이 은닉을 사실상 무력화했다 — **`armor_id` 하나가 `ArmorSpec`(`maxShieldPoint`·`DamageReductionRate`·`regenerationPerSecond`) 전체와 동치**이고, 클라이언트는 자기 인벤토리 UI를 그리려 같은 아이템 테이블을 이미 갖고 있어 id만 받으면 상대의 방어 수치를 정확히 역산한다. **결정의 전제는 "방어구가 캐릭터 외형에 드러나지 않는다"** 이다(무기는 보이므로 `weapon_id`는 계속 브로드캐스트한다). 남은 필드가 `object_id`+`weapon_id`뿐이라 이름을 함께 좁혔고, **pktId는 37 그대로** 뒀다 — 클라이언트가 아직 이 패킷 수신을 구현하지 않아 재번호의 이득이 없다. **방어구 변경 시에는 브로드캐스트 자체를 없앴다** — `armor_id`만 빼면 `equipSlotType == 2` 경로가 *바뀌지 않은* `weapon_id`를 담아 reliable 브로드캐스트를 쏘는 순수 낭비가 되므로, 분기를 갈라 `SetArmor()`(서버 측 실드 스펙 반영에 계속 필요)만 하고 통보하지 않는다. 부수적으로 방어구 교체 1건당 reliable (N-1)장이 사라져 아래 33장 in-flight 압력이 줄었다. **연쇄 삭제** — `PlayerObject::GetArmorId()`의 유일한 독자가 `FillEquipmentChanged`였고 그것이 사라지자 `_armorId`가 write-only가 되어 필드째 걷어냈다(`retryCount`·`_lastRecvTime`을 걷어낸 것과 같은 형태). 직전까지 우선사항이던 "`D2CSpawnPlayerObject`에 `armor_id` 추가"는 이 결정으로 **폐기**했다 — 그 변경은 구멍을 하나 더 뚫는 방향이었다 (`External_Protocol.proto`, `enum.h`, `ClientPacketHandler.h/cpp`, `UnityGameObjects/PlayerObject.h/cpp`)
- [x] (2026-08-19 #1) 실드 재생 구현 — 방어구 착용 시 실드가 0으로 시작하는 것은 **의도된 설계**(인벤토리에 방어구를 쟁여두고 스왑해 실드 파괴 리스크를 회피하는 플레이 차단)인데, 채울 수단인 재생이 구현돼 있지 않아 `ArmorSpec.regenerationPerSecond`가 `_shieldRegenPerSec`에 저장만 되고 독자가 0곳이었다. 즉 주워 입은 방어구의 실드가 0에서 *시작*하는 게 아니라 0에 *머물렀다*. 구동은 `GameRoom::Update()`의 `RegenPlayerShields()` 한 곳이며 `BroadcastPlayerStates()` **앞**에 둔다(상태 변경이 출력보다 앞서야 그 틱의 변화가 그 틱에 나간다 — `Update()` override 규약과 같은 이유). 순회 대상이 `BroadcastPlayerStates()`가 이미 도는 `_playerObjects`라 **자료구조도 타이머도 추가되지 않았다**. **틱 수가 아니라 실제 경과(ms)로 계산한다** — 룸 틱은 위상 분할로 100ms지만 메인 루프가 밀리면 흔들리므로 틱당 고정량을 주면 회복 속도가 서버 부하에 따라 달라진다. `_lastRegenMs`는 uint32 차 비교라 wrap이 자연 처리되고 `0`이면 기준 시각만 잡고 반환한다(`_lastEchoTs`와 같은 관용구). 누적은 **(실드 × ms) 단위로 1000이 모일 때마다 1 지급** — 값이 100배 스케일이라 대부분의 회복률은 100ms에 나누어떨어지지만, 그렇지 않은 값(예: 3.33/초 = 333)에서 매 틱 버림이 생기면 초당 1%씩 손실이 쌓인다. 누산기는 **만충·사망·방어구 없음일 때와 `SetShield()`에서 0으로 리셋**한다 — 만충으로 대기하는 동안 쌓인 누적이 피격 직후 한꺼번에 터지는 것과, 방어구를 갈아입을 때 이전 방어구의 누적이 이월되는 것을 막는다. `MAX_REGEN_STEP_MS`(1000)는 프로세스가 멈췄다 재개했을 때의 폭증 방지 겸 곱셈 오버플로 방지다. **통보 패킷은 0장** — `D2CNotifyHealthChange`는 reliable이라 100ms마다 쏘면 33장 한계에 즉시 걸린다. 대신 클라이언트가 마지막으로 받은 `D2CNotifyHealthChange` 시점을 기준으로 자기 실드를 예측하고, 서버 권위값은 다음 피격 통보가 정정한다(수치가 여전히 당사자에게만 가므로 위 은닉 원칙과도 맞는다). 착수 전 성능을 먼저 따져 **per-player 순회는 같은 틱 브로드캐스트의 1/200~1/400 수준**이고 실제 비용은 `DedicateMain`의 1ms 슬립 루프(초당 1000회 기상, 이미 보류 결정)임을 확인했다 (`UnityGameObjects/CombatObject.h`, `GameRoom.h/cpp`)
- [x] (2026-08-19 #2) `.proto`의 PktId 누락 보정과 클라이언트 인수인계 주석 — 8월 12일 이후 변경분을 추적하다 **`.proto`의 `enum PktId`가 35에서 멈춰 있는 것**을 발견했다. 서버는 `enum.h`에 36~40(`D2C_NOTIFY_SPAWN_OBJECT`·`D2C_NOTIFY_WEAPON_CHANGED`·`C2D_REQUEST_SWITCH_WEAPON`·`D2C_NOTIFY_DESPAWN_OBJECT`·`D2C_NOTIFY_PLAYER_KILLED`)을 정의하고 **이미 그 값으로 송수신하고 있었으나** `.proto`에는 message만 추가되고 enum 항목이 빠져, 생성 코드를 쓰는 클라이언트는 다섯 패킷의 id 상수를 얻을 수 없었다. 원인은 두 파일이 같은 사실을 각자 적는 구조 — `enum.h`는 C++ 컴파일에 필요해 반드시 갱신되지만 `.proto`의 PktId는 서버 빌드에 쓰이지 않아 빠뜨려도 아무것도 깨지지 않는다(같은 성질의 침묵 실패인 2026-08-14 #2의 사본 갱신 누락과 형태가 같다). **한쪽을 생성원으로 삼는 통합은 하지 않았다** — 실질 위험이 프로토콜에 패킷을 더할 때뿐이라 빈도가 낮고, 서버가 `.proto` PktId를 참조하도록 바꾸면 `PKT_ID_MAX` 배열 크기 등 C++ 쪽 관용구가 protobuf 헤더에 묶인다. 대신 이 문서의 「진행 고려사항」에 두 파일이 짝이라는 것을 남겼다. 값은 `enum.h`를 그대로 옮겨 **기존 0~35는 한 줄도 건드리지 않았다**(클라이언트의 기존 분기가 무효화되지 않는다). 함께 클라이언트 세션이 참고할 `// [작업사항]` 주석 레이어를 `.proto`에 넣었다 — 접두어 하나로 통일해 반영이 끝나면 블록만 골라 지운다. 파일 상단에 패킷에 매이지 않는 공통 4건(PktId 추가 / **`timestampEcho` 에코 의무** / `object_type = 3` 매핑 / 실드 재생의 로컬 예측 규칙)을, 각 message 위에 신규 7건과 **스펙은 그대로인데 의미·수신 시점이 바뀐 6건**을 적었다. 후자가 이 작업의 실질 — `D2CSpawnPlayerObject`(질의 응답 → 서버 push), `D2CResponseRecall`(완료 → 승인), `C2DRequestWeaponFire`(대상이 비플레이어까지), `D2CUpdatePlayerStates`(윈체스터 누락 수정)는 **diff만 봐서는 드러나지 않아** 클라이언트가 변경을 놓치기 가장 쉬운 부류다. `D2CNotifyWeaponChanged`에는 구 `D2CNotifyEquipmentChanged`에서의 리네임·`armor_id` 삭제 이력을 함께 남겼다 — 8월 15~18일 사이 스펙을 받아갔다면 재작업이 필요하기 때문이다 (`External_Protocol.proto`)

---

## 진행 중 / 다음 할 것들

### 진행 우선사항
1. **사망 연출** — 사망 시 이탈 처리(오브젝트 제거·퇴장 통보·인벤토리 소실), 시신 컨테이너, 비플레이어 전투 오브젝트의 사망 경로, 가해자 전달·킬 피드까지 구현 완료. 현재 클라이언트가 받는 것은 `D2CDespawnPlayerObject(DESPAWN_DEAD)`, 시신의 `D2CNotifySpawnObject`, `D2CNotifyPlayerKilled`, 그리고 피해자 본인의 `D2CNotifyHealthChange(attacker_object_id)` 넷이다. 남은 것은 **사망 애니메이션·연출**뿐이며, 오브젝트마다 다르므로 `OnDeathResolved()` 안에서 각자 처리하는 형태가 된다. 킬 피드에 표시할 이름은 아직 없다 (아래 「진행 고려사항」의 표시명 항목)
2. **HeartBeat / RequestBlueprint / RequestSpawnMe 클라이언트 연동 테스트 필요** — 프로토콜·직렬화 수정 완료, 빌드 통과. 클라이언트 측 구현 필요
    - `[DROP] 서명 불일치` 출력 → 클라이언트 서명 계산 로직 점검
    - `[DROP] unreliable 시퀀스 중복` 출력 → 클라이언트 uSeqNum 증가 로직 점검
    - **헤더 `timestampEcho` 에코 미구현 시 6초 뒤 강제 이탈** — 수신 패킷의 헤더 `timestamp`를 보관했다가 자신이 보내는 모든 패킷의 `timestampEcho`에 **가공 없이** 되돌려줘야 한다(서버 시계 도메인 값). 채우지 않으면 RTT 측정과 끊김 감지가 함께 무력화되고, 채우다 멈추면 6초 뒤 `DISCONNECTED`로 이탈 처리되어 인벤토리가 소실된다. 현재 `echo == 0` 세션은 판정에서 제외되지만 과도기 조치라 의존하지 말 것 (아래 「진행 고려사항」의 끊김 감지 항목)
    - StaticObjects 역직렬화 시 `TransformInfo` 구조 변경 반영 여부 클라이언트 측 확인 필요
    - `TestGameRoom::InitTestGameRoom()`에서 TestItemBox 등록 → Blueprint 응답(StaticObjects 청크)에 포함되는지 확인
    - **오브젝트 생성 브로드캐스트 수신 처리 미구현** — ① `D2CNotifySpawnObject`(id 36) 핸들러 신설. 페이로드가 `D2CResponseSpawnByObjectId`와 동일하므로 기존 역직렬화를 재사용할 수 있다 ② `D2CSpawnPlayerObject`(id 13)를 **요청 없이 서버가 먼저 보내는 경우**의 처리 — 지금까지는 `C2DRequestSpawnByObjectId`의 응답으로만 도착했다. ①은 시신 컨테이너가 붙으면서 선택이 아니게 됐다 — 없으면 **시신이 살아있는 플레이어 화면에 보이지 않고** 재접속해 SpawnMe를 새로 받은 경우에만 나타난다
    - **`ObjectType 3`(시신) 렌더링 미구현** — `object_type`이 `int32`라 프로토콜 변경 없이 값만 늘었다(0=None, 1=Player, 2=TestItemBox, 3=Corpse). 클라이언트가 3을 시신으로 매핑해야 하며, 시신은 열 수 있는 컨테이너이기도 하므로 상호작용 대상으로도 잡아야 한다
    - **`D2CNotifyWeaponChanged`(id 37) 수신 처리 미구현** — 같은 룸 플레이어의 손에 든 무기 갱신. 교체 성공 시에는 **본인에게도** 오고, 교체가 거부되면 **본인에게만** 현재 실제 상태가 오므로 예측 롤백에 그대로 쓰면 된다. **방어구는 오지 않는다** — 외형에 드러나지 않으므로 의도적으로 뺐다(아래 「진행 고려사항」의 은닉 항목)
    - **자기 실드 재생 예측 미구현** — 서버는 재생을 100ms 틱으로 돌리지만 **통보 패킷을 보내지 않는다**. 클라이언트가 마지막으로 받은 `D2CNotifyHealthChange`의 `shield_point`를 기준점으로 삼고 자기 방어구의 `ArmorSpec.regenerationPerSecond`로 경과분을 더해 그리며, 다음 `D2CNotifyHealthChange`가 오면 그 값으로 덮는다. 최대치 clamp를 잊지 말 것 — 서버는 `_maxShield`에서 멈춘다. 피격 후 재생 지연은 **없다**(연속 재생)
    - **`D2CNotifyDespawnObject`(id 39) 수신 처리 미구현** — 비플레이어 오브젝트의 제거 통보. 플레이어는 기존 `D2CDespawnPlayerObject`(id 35)를 계속 쓰므로 지우는 대상이 패킷으로 갈린다. 파괴 가능한 오브젝트가 추가되기 전까지는 서버가 이 패킷을 보내지 않는다
    - **`D2CNotifyPlayerKilled`(id 40) 수신 미구현** — 킬 피드. 피해자 본인에게는 오지 않는다(아래 항목이 대신한다). 무기 아이콘이 필요하면 `D2CSpawnPlayerObject.weapon_id` + `D2CNotifyWeaponChanged`로 직접 추적할 것 — 다만 **살아있는 오브젝트에서 즉석 조회하면 안 된다**. 상호 사살이나 킬 직후 귀환·끊김이면 킬 피드 도착 시점에 킬러 오브젝트가 이미 디스폰돼 있고, D2C reliable도 순서 보장이 없어 디스폰이 먼저 처리될 수 있다. `objectId → (마지막 무기, 캐릭터 타입)`를 디스폰 뒤에도 남겨둘 것(objectId는 룸 안에서 재사용되지 않아 캐시가 커지지 않는다)
    - **`D2CNotifyHealthChange.attacker_object_id`(4번 필드) 활용 미구현** — 피격 방향 표시(damage indicator)와 "누구에게 죽었나". 사망 시에도 이 패킷이 피해자에게 도달하도록 서버가 보장한다. 위와 같은 이유로 이 id도 이미 사라진 오브젝트를 가리킬 수 있다
    - **`C2DRequestSwitchWeapon`(id 38) 송신 미구현** — `target_slot`(0=주무기, 1=보조무기)과 자신이 아는 `my_inventory_version`을 실어 보낸다. **교체 확정(`D2CNotifyWeaponChanged` 수신) 전까지 발사 트리거를 막을 것** — reliable/unreliable 채널 사이에는 순서 보장이 없어 사격이 먼저 처리되면 `weapon_dbid` 불일치로 조용히 버려진다 (클라이언트 측에서 구현하기로 확정)
    - 위 항목들의 상세는 `External_Protocol.proto`의 `// [작업사항]` 주석에 패킷별로 적혀 있다. **클라이언트 반영이 끝나면 그 접두어가 붙은 블록만 일괄 삭제할 것** — 임시 주석이며, 남겨두면 코드 주석 규약(「진행 고려사항」)과 충돌한다
3. **윈체스터 룸의 고유 게임 루프 로직** — 상태 브로드캐스트는 `GameRoom::Update()`가 모든 룸에 대해 처리하므로 `WinchesterGameRoom`은 현재 `Update()`를 override하지 않는다. 맵 고유의 주기 처리(이벤트 등)가 정해지면 `Update() override`를 새로 쓰면서 **파생 로직을 먼저 하고 마지막에 `GameRoom::Update()`를 부를 것**. AI는 서버에서 다루지 않기로 했다 (`GameRoom.h/cpp`)

### 진행 고려사항
- **`TODO:` / `TEMP:` / `OPTION:` 주석 구분** — `TODO:`는 아직 구현되지 않은 것, `TEMP:`는 테스트를 위해 의도적으로 값을 제한하거나 코드를 막아둔 것, `OPTION:`은 없어도 동작에 문제가 없고 여유가 생기면 구현할 개선 항목이다. 릴리스를 막는 것은 `TEMP:`뿐이므로 릴리스 전 되돌려야 할 항목은 `grep -rn "TEMP" src HTTPServer`로 전수 확인할 것 (현재 8곳). `OPTION:` 항목은 할 일 목록에 올리지 않고 주석으로만 남긴다
- **코드 주석은 이 세 종류와 상수 유래뿐** — 흐름 서술("~를 전송한다"), 단계 나열, 구획 배너, 설계 배경은 코드에 쓰지 않는다. 코드만 봐서는 알 수 없는 제약·불변식·결정 배경은 **이 문서의 「진행 고려사항」에 적는다** (2026-08-13 #7·#8에서 코드베이스 전체 466줄을 이 기준으로 정리). 코드에 남기는 예외는 숫자만 남는 상수의 유래(슬롯 범위, 페이로드 한계, 프로토콜 반환 코드 등)와 바이트 레이아웃. `src/sample.h`만 예외로 남겨뒀다 — 학습 노트 성격이라 정리 대상으로 본다면 파일째 삭제가 맞다
- **`active_match` 락은 네 곳이 한 묶음** — `match.js`의 `/start` EX 값, `matchCancel` Lua의 `return 2`/`return 3` 분기, `DBProxyRequest.h`의 `ACTIVE_MATCH_TTL_SEC`, `redis_keys.md` 2번 절이 서로를 전제한다. TTL(3600초)은 **백스톱일 뿐**이고 정상 해제는 이탈 확정 한 곳이다. 세 가지 불변식을 깨지 말 것 — ① TTL은 최대 게임 길이보다 길어야 한다(짧으면 게임 도중 락이 풀린다) ② `match.js`의 EX와 `ACTIVE_MATCH_TTL_SEC`는 같은 값이어야 한다 ③ `INGAME:` 접두어는 `UpdateEntryTokenRequest`가 쓰고 `matchCancel`이 읽는다. 티켓 TTL(300초)이 락 TTL보다 짧아 생기는 "티켓 없고 락만 남은" 구간을 이 접두어로만 구분할 수 있다
- **MySQL과 Redis 사이에는 공유 트랜잭션이 없다** — 이탈 확정 반영은 **MySQL 먼저, 락 해제 나중**(fail-closed) 순서에 의존한다. 락을 먼저 풀면 DB 반영 실패 시 유저가 반영 안 된 인벤토리로 새 매치를 시작한다. 반대로 실패 시 락을 계속 잡으면 영구 잠금이 되므로, 재시도 2회 후에는 락을 풀고 페이로드를 error 로그로 남긴다. 이 순서를 바꾸는 변경은 `NotifyPlayerLeftRequest::Execute()` 안에 갇혀 있다
- **`sql::Connection`은 자동 재연결이 없다** — `sw::redis::Redis`와 달리 커넥션 풀도 재연결도 없어, `wait_timeout`(기본 8시간)이나 HeatWave 페일오버로 끊기면 이후 모든 사용이 예외를 던진다. **MySQL을 쓰는 코드는 반드시 `pMysql->Get()`으로 핸들을 받을 것** — 그 안에서 `isValid()`→`reconnect()`가 처리된다. `sql::Connection*`를 어딘가에 캐시하면 이 보호가 무력화된다
- **`damageReductionRate`는 실드 잔량이 있을 때만 유효하다** — 실드가 0으로 소진되면 `_currentShield -= damage`가 즉시 음수가 되어 penetrated 경로를 타고 감소율이 적용되지 않는다. 이것이 **의도된 설계로 확정**됐다(2026-08-18 #2). 즉 armor는 "상시 방어력"이 아니라 "실드가 버티는 동안의 감쇄"이므로, 밸런싱에서 `damageReductionRate`를 상시 적용으로 읽지 말 것 (`CombatObject.h`)
- **남의 HP·실드 수치와 방어구 정보는 클라이언트에 보내지 않는다** — 매 틱 브로드캐스트되는 `PlayerState`에는 이동·조준만 있고, `D2CNotifyHealthChange`(hp·shield 절대값)는 `pHitSession->Send()`로 **피격 당사자에게만** 간다. 적 체력바는 원천적으로 불가능하며 이는 의도된 결과다. 방어구도 같은 이유로 뺐다(2026-08-19 #0) — **외형에 드러나지 않고**, `blueprint_id` 하나가 `ArmorSpec`(최대 실드·피해 감소율·재생량) 전체와 동치라 id만 흘려도 곧 수치 노출이 된다. 서버가 안 보내면 클라이언트를 뜯어도 못 얻으므로 치트 표면적도 함께 줄어든다. **새 D2C 패킷에 남의 hp·실드·방어구를 싣지 말 것.** 무기(`weapon_id`)는 외형에 보이므로 예외이고, 히트마커처럼 "맞았다/못 맞았다" bool만 돌려주는 것은 이 원칙과 충돌하지 않는다
- **방어구 착용 시 실드가 0인 것은 의도된 설계다** — `SetShield()`가 `_currentShield = 0`으로 초기화하는 것을 버그로 보지 말 것. 착용 시 만충이나 이전 실드 비율 유지로 바꾸면 인벤토리에 방어구를 쟁여두고 실드가 0이 될 때마다 스왑해 **실드 파괴 리스크를 지지 않는 플레이**가 가능해진다. 만충으로 시작하는 유일한 예외는 스폰 시점의 `ChargeShield(GetMaxShield())`이고, 그 뒤로 채우는 수단은 `RegenShield()` 하나뿐이다. **피격 후 재생 지연은 없다** — `ArmorSpec`에 지연 필드가 없어 현재 데이터 모델이 표현할 수 있는 것이 연속 재생뿐이라서다. 필요해지면 `ArmorSpec` 필드 하나와 `_lastDamagedMs` 추가로 열린다(`TakeDamage()`가 매 피격에 불리므로 기록할 자리는 이미 있다)
- **가해자 id 필드는 예외 없이 명시적으로 채울 것** — `D2CNotifyHealthChange.attacker_object_id`와 `D2CNotifyPlayerKilled.killer_object_id`의 "없음"은 `0xFFFFFFFF`(`CombatObject::NO_ATTACKER`)다. **proto3 기본값 0은 실재하는 object_id**이므로(`GameRoom::_nxtObjectId`가 0부터 발급) 설정을 빠뜨리면 0번 오브젝트에 오귀속된다. 지금은 가해자 없는 피해 경로가 없어 드러나지 않지만, 회복(`REASON_ITEM_HEAL`)·낙하·지역 피해가 붙는 시점에 걸린다
- **킬 피드에 실을 표시명이 없다** — `Protocol/` 전체에 이름 필드가 하나도 없어 클라이언트가 표시할 수 있는 것은 objectId뿐이다. 서버에는 `Player::GetUserId()`가 있지만 그건 로그인 계정 id라 다른 플레이어에게 노출할 것이 못 된다. 표시명을 붙이려면 Main→Dedicate IPC로 닉네임을 실어오고 `D2CSpawnPlayerObject`에 필드를 여는 별도 작업이 필요하다. 그때까지 킬 피드의 실질 가치는 "내가 누구에게 죽었나"(피해자)와 "내가 잡았다"(킬러) 둘이며, 이 둘은 이름 없이도 성립한다
- **패킷을 추가하면 `enum.h`와 `.proto`의 `enum PktId`가 짝이다** — 서버는 `enum.h`의 `PKT_ID_*`만 쓰고 `.proto`의 `PktId`를 참조하지 않으므로, `.proto` 쪽을 빠뜨려도 **서버는 정상 빌드되고 정상 동작한다**. 대신 생성 코드를 쓰는 클라이언트가 그 패킷의 id 상수를 얻지 못해 분기 자체를 만들 수 없다(2026-08-19 #2에서 다섯 개가 이 상태였다). `PKT_ID_MAX`도 함께 올릴 것 — 핸들러 배열 크기이자 수신 시 범위 검사 기준이라 빠뜨리면 새 C2D 패킷이 조용히 버려진다. 통합하지 않은 이유는 서버가 `.proto` PktId를 참조하면 배열 크기 관용구가 protobuf 헤더에 묶이기 때문이며, 위험 구간이 "패킷 추가" 한 시점뿐이라 규율로 지킨다
- **IPC 프로토콜 파일을 추가하면 `CMakeLists.txt` 두 곳을 같이 고칠 것** — `PROJECT_SOURCES`의 `.pb.cc`/`.pb.h` 목록과 `IPC_PB_NAMES`. 한쪽만 고치면 컴파일 대상에서 빠지거나 `Protocol/Compiled/IPC/` → `src/IPCProtocol/` 복사가 누락된다. External 프로토콜은 protoc가 `src/` 아래로 직접 생성하므로 `PROJECT_SOURCES`만 보면 된다
- **`_allocatedPlayers`의 증감은 두 곳뿐이고 짝이 맞는다** — 증가는 `M2DSession::AllocatePlayers()`의 `+= ticketVec.size()`, 감소는 `ReleasePlayers()`가 받는 `player_count`(= 룸의 `_playerSessions.size()`)뿐이다. 둘이 같다는 보장은 코드에 드러나지 않으므로 근거를 남긴다 — ① `MatchMaker::VerifyAndSetMatchStatus()`의 Lua가 그룹 전원의 티켓 해시 존재와 `WAITING` 상태를 원자적으로 확인한 뒤 `INPROGRESS`로 바꾸고, 1을 반환했을 때만 `DistributePlayerGroup()`이 호출된다 ② Main은 단일 스레드이고 그 검증부터 `MakeM2DMakeRoomForThisGroup()`의 `hgetall`까지 연속 호출이라 중간에 io_uring CQ도 다른 핸들러도 끼어들지 못한다(가용 세션이 없을 때의 `fork`+`execl`만 들어오며 Redis를 건드리지 않는다) ③ 유일한 외부 삭제자인 Node `matchCancel` Lua는 `WAITING`일 때만 `DEL`하므로 `INPROGRESS` 티켓을 지우지 못한다. 따라서 `MakeM2DMakeRoomForThisGroup()`의 `if (val.empty()) continue`는 정상 동작에서 도달하지 않고 `player_infos` 크기는 `ticketVec.size()`와 같다. **이 세 조건 중 하나라도 바꾸면 카운트가 어긋나 프로세스가 다시 누적 증가한다.** 남는 이론적 구멍은 티켓 TTL(300초) 만료가 ①과 ② 사이 마이크로초에 걸리는 경우와 Redis eviction뿐이라 대응하지 않기로 했다
- **전용 프로세스는 룸이 비어도 종료하지 않는다** — 서버 가동 중 관측된 최대 수용 인원만큼의 프로세스를 유지하고, 이후 인원이 줄어도 회수하지 않는다는 결정(fork·exec과 초기화 비용 회피). 이에 따라 `GameRoom::CheckAllLeft()`에 있던 "남은 룸이 없을 때의 프로세스 정리" TODO를 제거했다. 딸려오는 것 둘 — ① 이 결정의 전제(프로세스 수가 최대 동시 인원에 수렴한다)는 `_allocatedPlayers` 감소 경로가 붙어서야 성립한다 (2026-08-14 #1에서 구현 완료) ② 유휴 프로세스도 `DedicateMain`의 1ms 슬립 루프 때문에 초당 1000회 깨어난다. 수십 개가 되면 폴링만으로 상시 CPU를 먹으므로 룸 0개일 때의 백오프(1ms→50ms)가 필요해지지만, **예상 프로세스 수가 2개 규모라 지금은 보류**한다
- **지연 콜백은 예외 없이 sessionId 재조회 규약을 따를 것** — `TimerExecuter`에 취소 API가 없어 룸/세션이 정리된 뒤에도 잔여 콜백이 실행될 수 있다. 콜백이 `PlayerSession*`를 캡처하지 않고 sessionId로 매번 재조회하며 uid·generation을 검증하면(현재 `RecallTick()`이 이 방식), 정리 시 `_players[id] = nullptr`만으로 잔여 콜백이 스스로 포기하므로 별도의 정지 확인(quiescence) 로직이 필요 없다. **한 곳이라도 raw 포인터를 캡처하면 이 설계가 무너진다.** 룸 소멸로 sessionId 반납이 실제로 시작됐으므로 이 규약은 이제 선택이 아니다 — `_freePlayerIds`가 FIFO라 반납된 id는 앞선 것들이 모두 소진된 뒤에야 재사용되어 간격 자체는 넉넉하지만, 재사용이 아예 없던 이전과 달리 규약을 어긴 콜백은 살아 있는 남의 세션을 잡는다
- **reliable in-flight 33개 초과 시 영구 ACK 불가** — ACK 커버리지는 `ackRSeqNum` 1개 + 비트필드 32개 = 총 33개다(`PlayerSession.cpp:85`, `120-130`). `_pendingReliable`에 34개 이상이 동시에 쌓이면 가장 오래된 것은 클라이언트가 이미 받았어도 알릴 수단이 없어 **세션이 끝날 때까지 재전송된다**(수신 측은 `UpdateRRecvState()`에서 `diff > 32`로 버리므로 정확성은 유지, 대역폭만 낭비). 2026-08-18 #3에서 MAX_RETRY를 없애면서 유계였던 낭비가 무계가 됐으므로, `RegisterReliable()`이 in-flight 34장 진입 시 남기는 로그가 이 상태의 유일한 신호다. **reliable 브로드캐스트가 셋으로 늘었다** — `D2CDespawnPlayerObject`(이탈 1건당 1장), `D2CNotifySpawnObject`·`D2CSpawnPlayerObject`(런타임 스폰 1건당 1장). 킬 피드가 붙으면서 **사망 1건은 3장**(시신 스폰 + 킬 피드 + 플레이어 디스폰)이 됐고, 룸 전원이 죽어도 12장이라 아직 여유가 있지만 이 경로가 늘어나면 도달 가능해진다. 대량 통보는 개별 전송 대신 청킹으로 묶을 것. 근본 해결은 비트필드 64비트 확장 또는 송신 창 제한이며 별도 작업
- **끊김 감지는 `PlayerSession::_lastEchoTs` 하나에 달려 있다** — `MarkLeaving(DISCONNECTED)`의 호출부는 `GameRoom::DetectDisconnectedSessions()` 한 곳뿐이고, 그 판정은 클라이언트가 UDP 헤더에 되돌려주는 `timestampEcho`가 6초 이상 갱신되지 않는 것을 본다. 딸린 전제 셋 — ① **클라이언트가 0.1초 주기로 상태를 보낸다**(6초 = 연속 60회 유실이라는 근거가 여기서 나온다. 주기를 늘리면 안전 마진이 그만큼 깎인다) ② `UpdateRtt()`가 `_lastEchoTs`를 갱신하는 유일한 지점이고 서명·시퀀스 검사를 통과한 패킷에서만 불린다 ③ `echo == 0`이면 판정을 건너뛰므로, 클라이언트가 `timestampEcho`를 채우지 않으면 감지가 조용히 무력화된다(`OPTION:`으로 남김). 수신 시각 기반으로 바꾸려는 유혹이 있다면 `_clientAddr`이 갱신되지 않아 NAT 리바인딩 시 하향만 죽는 경우를 놓친다는 점을 볼 것
- **응답 없는 세션의 재전송 큐는 이탈 처리로만 정리된다** — 개별 제거 경로는 `ProcessIncomingAck()` 뿐이고, `GameRoom::DetachPlayer()`의 `ClearPendingReliableExcept()`가 유일한 일괄 정리 지점이다. MAX_RETRY가 없어져 살아있는 세션에는 무한 재전송하므로, 큐의 상한을 정하는 것은 이제 **끊김 감지 6초**다. 감지 임계값을 늘리면 그만큼 죽은 세션의 큐가 오래 남아 `GetRetransmitCandidates()`가 매번 전체를 순회한다
- **브로드캐스트 페이로드 재직렬화는 의도적 보류** — N명에게 보낼 때 페이로드가 N번 직렬화된다. 세션마다 헤더·서명이 달라 `SendBuffer` 재사용이 불가능하지만 **페이로드 바이트 자체는 동일**하므로 1회 직렬화 + memcpy로 줄일 수 있다. 다만 4인/100ms 기준 룸당 절약분이 ~3μs, 250룸이어도 코어의 0.75% 수준이고 같은 경로의 `sendto` 비용이 이를 압도해 실익이 없다. `Make*FromPayload` 짝 함수가 패킷마다 늘어나 `pktId`/`reliable`이 어긋날 여지가 생기는 쪽이 손해. **재검토 조건: 룸 인원 10명 초과 / 틱 25~50ms로 상향 / 동시 룸 수 세 자릿수.** 변경 영향은 `ClientPacketHandler`와 브로드캐스트 두 함수 안에 갇혀 있다
- **reliable C2D 채널은 순서를 보장하지 않는다** — `PlayerSession::UpdateRRecvState()`(`PlayerSession.cpp:57-79`)는 최고 시퀀스 + 32비트 비트필드로 **중복만 거른다**. 늦게 도착한 낮은 시퀀스도 처음 보는 것이면 그대로 처리되고, reliable(rSeqNum)과 unreliable(uSeqNum)은 시퀀스 공간이 분리돼 두 채널 사이에는 순서 개념 자체가 없다. 따라서 **상태를 바꾸는 C2D 요청은 토글·증분이 아니라 절대 지정으로 설계할 것** — 재전송과 순서 역전에도 결과가 같아야 한다. 다른 조작과의 순서 역전은 `my_inventory_version` 같은 버전 가드로 막는다(`C2DRequestEquipItem`, `C2DRequestSwitchWeapon`이 쓰는 관용구). `PlayerInventory`의 변경 함수 11곳이 예외 없이 `_inventoryVersion`을 올리는 것이 이 가드의 전제다
- **사격 거부는 조용하다** — `Handle_C2D_RequestWeaponFire`의 `weapon_dbid` 불일치(`ClientPacketHandler.cpp:585`)와 `fire_sequence` 불일치는 deny 패킷 없이 `return false`다. 클라이언트에는 "총이 안 나감"으로만 보이므로 증상을 만나면 서버 로그의 `[Handle_C2D_RequestWeaponFire]` 줄을 먼저 볼 것
- **시신 컨테이너의 용량에는 여유가 0이다** — `PlayerInventory::INVENTORY_SLOT_COUNT`(25) + 장착·탄창 5칸 = 30이 `Container::DEFAULT_CONTAINER_VOLUME`(30)과 정확히 같다. 인벤토리 칸이나 장착 슬롯을 늘리면서 `DEFAULT_CONTAINER_VOLUME`을 함께 늘리지 않으면 `Container::PlaceItem()`이 `false`만 반환하고 초과분을 **조용히 버린다**(반환값을 보는 호출부가 없다). `CorpseContainer.h`의 `static_assert`가 이 경우를 컴파일 타임에 잡으므로 빌드가 깨지면 두 상수를 같이 볼 것
- **`GameRoom::Update()`를 override하면 마지막에 베이스를 부를 것** — 베이스가 `BroadcastPlayerStates()`를 담당하므로 빠뜨리면 그 룸에서 플레이어가 서로 움직이지 않는다(윈체스터에서 실제로 일어났던 증상). 먼저 부르면 그 틱의 파생 로직이 만든 변화가 다음 틱에야 나가 상태가 한 틱 밀린다. 이 누락을 구조적으로 막는 NVI(비가상 `Update()` + 순수 가상 훅)는 **의도적으로 채택하지 않았다** — `Update`가 둘로 보이는 쪽의 인지 비용이 더 크다는 판단이므로, 이 규약은 규율로 지킨다
- **`CombatObject`는 `_dynamicObjects`에만 둔다** — 회수 경로인 `GameRoom::DestroyDeadObject()`가 `_dynamicObjects`만 보므로, 정적으로 등록된 전투 오브젝트는 죽어도 회수되지 않고 화면에서만 사라진다. 입구는 `CanBeStaticObject()`가 막지만(`SpawnStaticObject()` 세 갈래 모두에서 호출) 우회하면 성립한다. AI를 서버에서 다루지 않기로 했으므로 이 자리에 올 수 있는 것은 파괴 가능한 오브젝트뿐이다
- **파괴 가능하면서 컨테이너인 오브젝트는 만들지 않는다** — 만들면 약탈 중이던 세션의 `_interactingContainerId`가 사라진 오브젝트를 가리킨다. 아래 「시신에는 TTL이 없다」에서 예고한 문제가 이 조합에서 실현되므로, 필요해지면 제거 시 해당 id를 보는 세션을 찾아 `-1`로 되돌리는 처리가 함께 와야 한다
- **비플레이어 사망 경로는 아직 한 번도 실행되지 않는다** — `CombatObject`의 파생이 `PlayerObject`뿐이라 `FindNonplayerObject()`가 전투 오브젝트를 찾는 일이 없다. 검증용 더미 클래스를 만들지 않기로 했으므로 첫 파괴 가능 오브젝트를 만드는 시점에 피격→사망→회수→통보 전체를 그 자리에서 디버그할 것. 그때 지켜야 할 불변식 둘 — ① 회수 지시는 `TakeDamage()` 안이 아니라 **호출부가** 한다(안에서 `delete`하면 호출부가 직후에 읽는 `GetCurrentHp()`가 use-after-free) ② `OnDeathResolved()`에 `_dynamicObjects`의 반복자를 넘기지 않는다(훅이 흔적을 스폰하면 rehash로 무효화)
- **시신에는 TTL이 없다** — 룸 소멸까지 유지되고 `~GameRoom()`이 회수한다. 나중에 만료를 붙이려면 두 가지가 따라온다 — ① 약탈 중이던 세션의 `_interactingContainerId`가 사라진 오브젝트를 가리키게 되므로 소멸 시 해당 세션을 찾아 `-1`로 되돌려야 한다 ② `TimerExecuter`에 취소 API가 없어 잔여 콜백이 objectId 재조회 규약(세션의 sessionId 재조회와 같은 형태)을 따라야 한다. 지금은 둘 다 성립하지 않아 문제 자체가 없다
- **귀환 유효성 검사는 안티치트가 아님** — 검사에 쓰이는 `PlayerObject::position`은 `ApplyState()`에서 클라이언트가 보낸 좌표를 그대로 기록한 값이다(`PlayerObject.cpp:10-13`). 좌표 조작 클라이언트는 항상 통과한다. 실질적 차단에는 서버 측 이동 검증(속도/텔레포트 체크)이 별도로 필요

---
