# 진행 상황 정리 (2026-08-31 업데이트)


## 완료된 것들

### 네트워크 / 패킷
- [x] (2026-08-29 #11) `.proto`의 `[작업사항]` 주석 관례 폐지 — 클라이언트 반영이 필요한 변경은 상위 세션이 두 디렉터리의 컨텍스트를 함께 읽고 처리하기로 해, 마지막으로 남아 있던 `D2CUpdatePlayerStates` 블록과 파일 상단의 관례 안내를 지웠다. 루트 `CLAUDE.md`의 규율은 "`.proto`에는 프로토콜 자체의 설명만 남기고, 통보의 대상·시점·보장 범위를 바꾸면 그 주석을 함께 고칠 것" 한 항목으로 줄였다. 지우기 전에 클라이언트 쪽 계약 하나(UI가 열려 있다고 컨테이너 점유가 유지되지는 않는다)는 `src/DedicateProcess/CLAUDE.md`로 옮겼다 (`External_Protocol.proto`, `CLAUDE.md`, `src/DedicateProcess/CLAUDE.md`)
- [x] (2026-08-30 #0) 무기 슬롯 조작 시 탄창 언로드 결과 통보 — 장착·해제는 스왑에 앞서 탄창을 인벤토리로 쏟는데 `D2CResponseEquipItem`이 조작의 좌표만 실어, 클라이언트가 같은 규칙을 미러하더라도 버전 밖에서 깎이는 탄약 수량의 오차가 **버전이 붙은** 인벤토리 칸으로 세탁돼 이후 버전 비교로는 드러나지 않았다. 응답에 `unloaded_ammo_slot`(결과 칸의 최종 상태)을 실어 클라이언트가 자기 계산을 검산·덮어쓰게 했고, 칸이 어긋나면 규칙 드리프트로 보고 전체 재동기화하도록 `.proto`에 언로드 규칙 세 갈래(같은 탄종 스택 합침 → 최소 인덱스 빈 칸 → 폐기)를 명문화했다. 전체 스냅샷(재장전 방식) 대신 규칙 공유를 택한 배경과, 발사 시 버전을 올리는 변경이 오면 이 필드가 불필요해진다는 조건은 CLAUDE.md에 남겼다 (`External_Protocol.proto`, `PlayerInventory.h/cpp`, `ClientPacketHandler.cpp`, `src/DedicateProcess/CLAUDE.md`)
- [x] (2026-08-30 #1) 무기 해제의 목적지 칸 별칭 버그와 빈 칸 인덱스 미갱신 수정 — `UnequipWeaponToSlot()`이 언로드를 먼저 해서, 목적지가 최소 인덱스 빈 칸이면 규칙 ②가 그 칸을 채우고 뒤따르는 `dstSlot.IsEmpty()` 분기가 뒤집혀 **무기 슬롯에 탄약이 들어앉았다**(타입 검사와 맨손 금지도 함께 우회됐고, 재장전·발사가 조용히 죽는다). 무기를 옮긴 **뒤** 언로드하도록 순서를 바꿔 목적지와 탄약 칸이 겹치는 경우를 구조적으로 없앴다. 별개로 `_firstEmptySlotIndex` 갱신이 살아 있는 `*Slot` 계열 넷에 하나도 없어 낡은 인덱스가 찬 칸을 가리키면 언로드가 그 아이템을 덮어써 소멸시켰는데, 네 곳 모두에 붙였다 (`PlayerInventory.cpp`, `External_Protocol.proto`, `src/DedicateProcess/CLAUDE.md`)
- [x] (2026-08-30 #2) 장비 슬롯 스왑 계약 명문화 — 클라이언트 세션이 「목적지가 차 있으면 스왑인가 거부인가 / 타입 검사는 하는가 / 스왑 뒤 탄창은 비어 있는가」 셋을 물어왔는데, 서버 동작은 이미 확정돼 있었고(스왑이다 / 장비 슬롯으로 **들어가는 쪽**만 검사한다 / 언제나 빈 탄창이다) 문서에만 없었다. `.proto`의 `DENY_ITEM_TYPE_MISMATCH` 설명이 "예: 장갑 슬롯에 무기" 하나만 들어 "해제 방향은 검사하지 않는다"로 읽힌 것이 오독의 원인이었다. 스왑 규약·맨손 금지의 적용 조건·탄창 최종 상태와 그 귀결(무기를 뺐다 끼우면 탄약이 초기화된다)을 CLAUDE.md에 박았다. 서버 로직 변경 없음 (`src/DedicateProcess/CLAUDE.md`)
- [x] (2026-08-30 #3) 클라이언트 반영 완료에 따른 `.proto` 정리 — 탄창 언로드 규칙 세 갈래·계산 순서·검산 지시를 클라이언트가 구현 완료해 `C2DRequestEquipItem`의 설명 블록을 두 줄로 줄였다(`unloaded_ammo_slot` 필드 설명은 유지). 함께 지워졌던 `DENY_ITEM_TYPE_MISMATCH`(0x0040)의 적용 범위 설명은 되살렸다 — 거부 사유 목록은 루트 CLAUDE.md가 `.proto`에 남기라고 지정한 항목이고, 문구가 좁은 탓에 실제로 클라이언트가 "해제 방향은 검사하지 않는다"로 오독한 적이 있다. 규칙이 이제 서버·클라이언트 양쪽에 각각 구현돼 저장소 안에 변경을 알릴 신호가 없다는 것과 "언로드 규칙·계산 순서를 건드리면 상위 세션에 클라이언트 동반 수정을 요청할 것"을 CLAUDE.md에 고쳐 적었다 (`External_Protocol.proto`, `src/DedicateProcess/CLAUDE.md`)
- [x] (2026-08-31 #3) `PlayerInventory`의 호출부 없는 함수 다섯 삭제 — `EquipWeaponFromInventory`·`UnequipWeaponToInventory`·`EquipArmorFromInventory`·`UnequipArmorToInventory`·`MoveInventorySlot`은 저장소 전체에 참조가 0건이었고, 인덱스 대신 `Slot&`를 받아 컨테이너 칸까지 다루는 `*Slot` 계열이 상위 호환이라 기능 손실이 없다. 남겨두면 함정인 쪽이 문제였다 — `UnequipWeaponToInventory`에는 이미 고친 결함 둘(언로드 후 타입 검사 실패로 탄창만 쏟고 실패 / 목적지 칸 별칭)이 그대로 있었고, `MoveInventorySlot`은 살아 있는 get 경로(목적지 빈 칸·수량 분할)와 규칙이 달라 되살리면 조용히 섞인다. 사설 헬퍼 둘은 `*Slot` 계열이 쓰므로 유지했고, 탄종 검사 생략 근거의 "네 경로"를 "두 경로"로 함께 고쳤다 (`PlayerInventory.h/cpp`, `src/DedicateProcess/CLAUDE.md`)

### HTTP 서버 / 인증·세션
- [x] (2026-08-31 #0) 중복 로그인 시 진행 중인 게임 보호 — 기존 `/api/login`은 옛 세션을 조건 없이 지우고 항상 새 세션을 발급해, 게임 중에 로그인하면 옛 클라이언트는 UDP로 계속 플레이하고 새 클라이언트는 `active_match` 락에 막혀 매칭만 못 하는 어중간한 상태가 됐다. `loginTakeover` Lua 하나로 락 판정과 세션 교체를 원자화해 — `INGAME:` 표식이면 409 `ERR_ALREADY_IN_GAME`, 티켓이 이미 성사됐으면 409 `ERR_MATCH_ALREADY_SUCCESS`(`/match/cancel`과 같은 코드)로 거부하고, WAITING 티켓이면 티켓·락을 파기하고 매치메이커에 취소 IPC를 보낸 뒤 로그인시킨다(IPC를 빠뜨리면 로비에 있는 유저에게 방이 만들어진다). 판정은 락 검사라 세션 유무와 무관하게 늘 돌지만 bcrypt 뒤에 두어, 비밀번호를 모르는 제3자에게 "이 계정 게임 중"이 새지 않게 했다 (`routes/auth.js`, `http-api-spec.yaml`, `database/redis_keys.md`, `HTTPServer/CLAUDE.md`)
- [x] (2026-08-31 #1) 세션·`active_match` TTL 3600초 → 900초 — 락이 이제 로그인 가능 여부까지 쥐게 돼 잔류 사고의 회복이 빨라야 했고, 「한 판 10분 미만」 확정으로 백스톱 여유가 섰다(락은 게임 시작 시 `INGAME:`으로 재무장되므로 900초가 덮는 건 게임 구간뿐이다). 세션 TTL은 네 곳에 흩어진 리터럴이던 것을 `SESSION_TTL_SEC` 하나로 모았고 — `sess:`와 `user_sess:`가 갈라지면 `loginTakeover`가 옛 세션을 못 찾아 한 계정에 유효 세션이 둘 남는다 — 락 쪽은 `match.js`의 매직넘버에 C++과 같은 이름을 붙여 짝을 드러냈다. 다만 `GameRoom`에 시간 상한이 없어 이 여유는 코드가 아니라 기획이 지킨다는 것을 불변식으로 남겼다 (`config/redisClient.js`, `routes/auth.js`, `routes/match.js`, `src/DBProxyRequest.h`, `src/M2DSessions.cpp`, `database/redis_keys.md`, `src/CLAUDE.md`, `HTTPServer/CLAUDE.md`)
- [x] (2026-08-31 #2) 401의 이유를 클라이언트에 구분해 주지 않는 방침 확정 — `requireAuth`의 401은 자연 만료·중복 로그인 축출·위조 세션 ID 셋에 같은 응답을 내는데, 셋 다 클라이언트 대응이 재로그인 하나라 구분에 기능적 가치가 없고 세션 ID를 쥔 쪽에 "존재했으나 만료됨"과 "애초에 없음"을 알려주는 손해만 남는다. 그래서 축출된 클라이언트에게 통보가 없는 것은 미결이 아니라 확정으로 올리고, 구분용 `error.code`·통보 채널(로비 폴링·tombstone) 신설 금지를 못 박았다(서버 추적은 `[Auth] 기존 접속 끊기` 로그로 한다). 예외는 `/login`의 401 둘(없는 ID·틀린 비밀번호)로 — 합치면 유저가 로그인 실패 원인을 알 수 없어 플레이 경험을 해치고, 계정 열거는 `/signup`의 409가 구조상 같은 정보를 주므로 합쳐도 어차피 막히지 않는다. 코드 변경 없음 (`HTTPServer/CLAUDE.md`)
- [x] (2026-08-31 #4) 세션 TTL 갱신의 떠 있는 프라미스에 `.catch()` — `requireAuth`가 `refreshSession()`을 await도 catch도 없이 불러, Redis가 그 명령을 거부하면(순단 시 in-flight 명령, `maxmemory`+`noeviction` 상태의 EVAL 거부) 미처리 프라미스 거부로 **HTTP 서버 프로세스가 통째로 종료**될 수 있었다 — 같은 미들웨어의 `hGetAll`은 await + try/catch로 500을 내는데 이쪽만 보호 밖이었다. 갱신 실패는 그 요청 한 번의 TTL 연장만 건너뛰면 되는 성격이라 응답을 지연시키는 await 대신 `.catch(() => {})`로 삼켰다(로그 없음). `HTTPServer` 전체에서 await 없이 떠 있는 프라미스는 이 한 줄뿐임을 확인하고, 전역 `unhandledRejection` 핸들러가 없다는 전제를 CLAUDE.md에 남겼다 (`middleware/auth.js`, `HTTPServer/CLAUDE.md`)

---

## 진행 중 / 다음 할 것들

### 진행 우선사항
1. **`GameRoom` 수명을 10분으로 제한** — `active_match` 락 TTL 900초의 불변식(TTL은 최대 게임 길이보다 길 것)이 지금은 코드가 아니라 「한 판 10분 미만」 기획 확정에만 기대고 있다. `GameRoom`에 라운드 타이머·존 축소가 없어 방은 플레이어가 개별로 나갈 때(귀환·사망·연결 끊김)만 끝나고, 체류 시간에 서버가 정한 상한이 없다. 900초를 넘긴 게임이 생기면 락이 도중에 풀리고, 그 뒤 `NotifyPlayerLeftRequest::Execute()`의 `del("active_match:" + uid)`가 값을 보지 않으므로 그 사이 시작된 새 매치의 락을 지운다 — 더 나쁜 쪽은 락이 아니라 옛 세션의 `ApplyInventoryToDb()`가 새 매치 `/match/start`의 스냅샷을 덮어써 아이템이 유실·복제되는 것이다. 상한이 서면 값-무검사 `del`을 따로 고칠 필요가 없어진다. 착수 전 결정 셋 — ① **기준 시각**을 룸 생성으로 잡을지 첫 플레이어 접속으로 잡을지(생성 기준이면 매칭·로딩이 10분을 잠식해 실플레이가 짧아진다) ② **만료 시 각 플레이어의 처리**를 귀환으로 볼지 사망으로 볼지(인벤토리 보존 여부가 갈리고, `LeaveReason`이 그대로 MySQL 반영에 쓰인다) ③ **종료 예고를 클라이언트에 통보할지**(남은 시간 UI가 필요하면 새 D2C 통보와 `.proto` 변경이 붙는다). 구현 자체는 `GameRoom::Update()`에서 경과를 재고 만료 시 전원 `DetachPlayer()`를 도는 것이라, 개별 이탈 경로를 그대로 타므로 락 해제·DB 반영은 이미 있는 것이 처리한다
2. **첫 파괴 가능 오브젝트** — `CombatObject`의 파생이 `PlayerObject`뿐이라 피격→사망→회수→통보 경로가 한 번도 실행된 적이 없다(아래 「진행 고려사항」 참조). 서버 몫은 `ObjectType` 추가와 `ObjectTypeToName()` case 짝, `CombatObject` 파생 클래스 하나, 룸의 `SpawnDynamicObject()` 배치이고 킬·디스폰 통보(41·39)는 이미 붙어 있다. 착수 전에 기획 결정 셋이 필요하다 — 무엇을 만들지(컨테이너 겸 파괴 가능은 금지라 상자류 제외), 최대 HP(100배 스케일), 파괴 시 흔적을 남길지(남긴다면 `OnDeathResolved()` 안에서 별개 오브젝트로 스폰)

### 진행 고려사항

미결·보류·검증 대기 항목 전용이다. 확정된 불변식·규약·결정 배경은 해당 디렉터리의 CLAUDE.md에 적는다 (루트 `CLAUDE.md` 「진행 상황 추적」 절 참조).

- **비플레이어 사망 경로는 아직 한 번도 실행되지 않는다** — `CombatObject`의 파생이 `PlayerObject`뿐이라 `FindNonplayerObject()`가 전투 오브젝트를 찾는 일이 없다. 검증용 더미 클래스를 만들지 않기로 했으므로 첫 파괴 가능 오브젝트를 만드는 시점에 피격→사망→회수→통보 전체를 그 자리에서 디버그할 것. 그때 지켜야 할 불변식 둘 — ① 회수 지시는 `TakeDamage()` 안이 아니라 **호출부가** 한다(안에서 `delete`하면 호출부가 직후에 읽는 `GetCurrentHp()`가 use-after-free) ② `OnDeathResolved()`에 `_dynamicObjects`의 반복자를 넘기지 않는다(훅이 흔적을 스폰하면 rehash로 무효화)

---
