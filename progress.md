# 진행 상황 정리 (2026-08-25 업데이트)


## 완료된 것들

### 네트워크 / 패킷
- [x] (2026-08-19 #1) 실드 재생 구현 — 방어구 착용 시 실드가 0으로 시작하는 것은 **의도된 설계**(인벤토리에 방어구를 쟁여두고 스왑해 실드 파괴 리스크를 회피하는 플레이 차단)인데, 채울 수단인 재생이 구현돼 있지 않아 `ArmorSpec.regenerationPerSecond`가 `_shieldRegenPerSec`에 저장만 되고 독자가 0곳이었다. 즉 주워 입은 방어구의 실드가 0에서 *시작*하는 게 아니라 0에 *머물렀다*. 구동은 `GameRoom::Update()`의 `RegenPlayerShields()` 한 곳이며 `BroadcastPlayerStates()` **앞**에 둔다(상태 변경이 출력보다 앞서야 그 틱의 변화가 그 틱에 나간다 — `Update()` override 규약과 같은 이유). 순회 대상이 `BroadcastPlayerStates()`가 이미 도는 `_playerObjects`라 **자료구조도 타이머도 추가되지 않았다**. **틱 수가 아니라 실제 경과(ms)로 계산한다** — 룸 틱은 위상 분할로 100ms지만 메인 루프가 밀리면 흔들리므로 틱당 고정량을 주면 회복 속도가 서버 부하에 따라 달라진다. `_lastRegenMs`는 uint32 차 비교라 wrap이 자연 처리되고 `0`이면 기준 시각만 잡고 반환한다(`_lastEchoTs`와 같은 관용구). 누적은 **(실드 × ms) 단위로 1000이 모일 때마다 1 지급** — 값이 100배 스케일이라 대부분의 회복률은 100ms에 나누어떨어지지만, 그렇지 않은 값(예: 3.33/초 = 333)에서 매 틱 버림이 생기면 초당 1%씩 손실이 쌓인다. 누산기는 **만충·사망·방어구 없음일 때와 `SetShield()`에서 0으로 리셋**한다 — 만충으로 대기하는 동안 쌓인 누적이 피격 직후 한꺼번에 터지는 것과, 방어구를 갈아입을 때 이전 방어구의 누적이 이월되는 것을 막는다. `MAX_REGEN_STEP_MS`(1000)는 프로세스가 멈췄다 재개했을 때의 폭증 방지 겸 곱셈 오버플로 방지다. **통보 패킷은 0장** — `D2CNotifyHealthChange`는 reliable이라 100ms마다 쏘면 33장 한계에 즉시 걸린다. 대신 클라이언트가 마지막으로 받은 `D2CNotifyHealthChange` 시점을 기준으로 자기 실드를 예측하고, 서버 권위값은 다음 피격 통보가 정정한다(수치가 여전히 당사자에게만 가므로 위 은닉 원칙과도 맞는다). 착수 전 성능을 먼저 따져 **per-player 순회는 같은 틱 브로드캐스트의 1/200~1/400 수준**이고 실제 비용은 `DedicateMain`의 1ms 슬립 루프(초당 1000회 기상, 이미 보류 결정)임을 확인했다 (`UnityGameObjects/CombatObject.h`, `GameRoom.h/cpp`)
- [x] (2026-08-19 #2) `.proto`의 PktId 누락 보정과 클라이언트 인수인계 주석 — 8월 12일 이후 변경분을 추적하다 **`.proto`의 `enum PktId`가 35에서 멈춰 있는 것**을 발견했다. 서버는 `enum.h`에 36~40(`D2C_NOTIFY_SPAWN_OBJECT`·`D2C_NOTIFY_WEAPON_CHANGED`·`C2D_REQUEST_SWITCH_WEAPON`·`D2C_NOTIFY_DESPAWN_OBJECT`·`D2C_NOTIFY_PLAYER_KILLED`)을 정의하고 **이미 그 값으로 송수신하고 있었으나** `.proto`에는 message만 추가되고 enum 항목이 빠져, 생성 코드를 쓰는 클라이언트는 다섯 패킷의 id 상수를 얻을 수 없었다. 원인은 두 파일이 같은 사실을 각자 적는 구조 — `enum.h`는 C++ 컴파일에 필요해 반드시 갱신되지만 `.proto`의 PktId는 서버 빌드에 쓰이지 않아 빠뜨려도 아무것도 깨지지 않는다(같은 성질의 침묵 실패인 2026-08-14 #2의 사본 갱신 누락과 형태가 같다). **한쪽을 생성원으로 삼는 통합은 하지 않았다** — 실질 위험이 프로토콜에 패킷을 더할 때뿐이라 빈도가 낮고, 서버가 `.proto` PktId를 참조하도록 바꾸면 `PKT_ID_MAX` 배열 크기 등 C++ 쪽 관용구가 protobuf 헤더에 묶인다. 대신 이 문서의 「진행 고려사항」에 두 파일이 짝이라는 것을 남겼다. 값은 `enum.h`를 그대로 옮겨 **기존 0~35는 한 줄도 건드리지 않았다**(클라이언트의 기존 분기가 무효화되지 않는다). 함께 클라이언트 세션이 참고할 `// [작업사항]` 주석 레이어를 `.proto`에 넣었다 — 접두어 하나로 통일해 반영이 끝나면 블록만 골라 지운다. 파일 상단에 패킷에 매이지 않는 공통 4건(PktId 추가 / **`timestampEcho` 에코 의무** / `object_type = 3` 매핑 / 실드 재생의 로컬 예측 규칙)을, 각 message 위에 신규 7건과 **스펙은 그대로인데 의미·수신 시점이 바뀐 6건**을 적었다. 후자가 이 작업의 실질 — `D2CSpawnPlayerObject`(질의 응답 → 서버 push), `D2CResponseRecall`(완료 → 승인), `C2DRequestWeaponFire`(대상이 비플레이어까지), `D2CUpdatePlayerStates`(윈체스터 누락 수정)는 **diff만 봐서는 드러나지 않아** 클라이언트가 변경을 놓치기 가장 쉬운 부류다. `D2CNotifyWeaponChanged`에는 구 `D2CNotifyEquipmentChanged`에서의 리네임·`armor_id` 삭제 이력을 함께 남겼다 — 8월 15~18일 사이 스펙을 받아갔다면 재작업이 필요하기 때문이다 (`External_Protocol.proto`)
- [x] (2026-08-19 #3) 재전송이 실어 오는 낡은 헤더로부터 timestamp 상태를 보호 — 착수는 "서버가 `timestampEcho`를 reliable 채널에만 채우는가"라는 확인이었고, `MakeD2CPacketImpl`이 `reliable` 분기 **밖에서** 무조건 채우므로 송신 자체는 문제가 없었다(그 분기가 가르는 것은 시퀀스 번호·`FLAG_RELIABLE`·`RegisterReliable` 등록뿐이다). 실제 구멍은 재전송 경로였다 — `CheckRetransmits()`가 보관 버퍼를 `memcpy`해 그대로 쏘므로(서명이 전체 패킷 해시라 헤더를 고치려면 재서명이 따라온다) 재전송분의 `timestamp`·`timestampEcho`·ACK 필드가 **최초 송신 시점 값 그대로** 나가고, 클라이언트도 미확인 reliable 페이로드를 풀링해 같은 방식으로 재전송하므로 이 성질은 양방향이다. **중복 검사가 걸러주지 못하는 경우가 정확히 문제의 경우다** — 원본이 유실돼 재전송이 그 시퀀스의 첫 도착이면 `UpdateRRecvState()`를 통과하고 낡은 헤더가 `SetLastRecvTimestamp()`·`UpdateRtt()`에 그대로 들어간다(재전송이 존재하는 이유가 곧 원본 유실이라 이쪽이 예외가 아니라 기본 경로다). 피해 셋 — ① `_lastEchoTs`가 과거로 되감겨 `DetectDisconnectedSessions()`의 `nowMs - echoTs`가 부풀고 **멀쩡한 세션이 끊김으로 킥된다**. 아래 33장 항목대로 in-flight 초과로 영구 pending이 생기면 그 패킷의 timestamp는 상한 없이 늙으므로 6초 초과가 사실상 확정된다 ② 부풀려진 RTT 샘플이 EWMA를 오염시키고 `_rttMs`가 재전송 타임아웃(`rtt*3/2`)을 정하므로 **손실이 있을 때 복구가 더 느려지는 양의 피드백**이 된다 ③ `_lastRecvTimestamp`가 되감기면 서버가 그 낡은 값을 에코로 되돌려 이번엔 클라이언트의 RTT와 끊김 감지를 오염시킨다. 대응은 두 필드의 **단조 갱신** — `int32_t` 차로 wrap을 처리하고 `_last* != 0` 분기를 앞에 둬, 서버 가동이 24.8일을 넘겨 `NowMs()`가 2³¹을 넘겼을 때 첫 샘플이 음수 차로 영구 거부되는 함정을 막는다(`UpdateRtt()`의 기존 `echoTs == _lastEchoTs` 검사는 새 조건에 포함돼 삭제). 세션 재사용 문제는 성립하지 않는다 — `PlayerSession`은 입장마다 새로 생성되고 두 필드가 멤버 초기값 0이라 클라이언트 시계가 0부터 다시 시작해도 영구 거부에 빠지지 않으며, 남는 전제는 "클라이언트 시계가 한 세션 안에서 단조" 하나다. **재전송 시 헤더 재스탬프 + 재서명은 검토 후 보류** — ① 실패 모드가 조용하고 치명적이다(해시 전 `signature = 0`을 빠뜨리면 클라이언트가 재전송분을 전부 드롭해 그 시퀀스가 영원히 ACK되지 않는데, 드롭이 클라이언트 쪽에서 일어나 서버 로그에 흔적이 없다) ② 손실 없는 로컬에서는 이 경로가 거의 돌지 않아 회귀를 확인할 수단이 없다 ③ 이득 중 낡은 ACK 문제는 서버가 `UpdateRRecvState()`에서 중복을 걸러내 상태 오염이 아니라 대역폭 낭비에 그친다 ④ 무엇보다 **"중복 패킷이어도 헤더 필드는 반영한다"는 클라이언트 계약이 확정된 뒤에야** 이득이 남는다. RTT 이상치 클램프도 함께 기각했다 — 상한 초과분을 폐기하면 실제로 고RTT인 회선의 `_rttMs`가 낮게 고정돼 가장 열악한 연결에 재전송 폭풍을 쏜다. **판단이 하나 뒤집혔다** — 중복 reliable을 ACK·timestamp 처리 **전에** 버리는 조기 `return`(`ClientPacketHandler.h:186-190`)을 처음엔 비대칭 결함으로 봤으나, 양쪽 다 verbatim 재전송인 현 구조에서는 낡은 헤더를 걸러주는 보호막이라 그대로 뒀다. 클라이언트가 "받은 timestamp 중 최대값을 에코"하는 방안은 검토 중이라 `.proto` 주석에는 반영하지 않았다 (`PlayerSession.h/cpp`)
- [x] (2026-08-21 #0) 무기 교체 결과를 클라이언트가 판정할 수 있게 함 — 발단은 **`.proto` 인수인계 주석이 코드와 정반대**인 것이었다. 2026-08-15 #2에서 성공을 `Broadcast`(본인 포함)로 보내기로 했는데 2026-08-19 #2의 주석은 "이 패킷의 `object_id`가 자기 objectId면 교체 실패"라고 적었다 — 그대로 구현하면 **모든 성공이 실패로 오판**된다(클라이언트가 아직 id 37 수신을 구현하지 않아 터지지 않았을 뿐이다). 주석을 고치려다 **그 규칙 말고는 판정 수단이 없다**는 것이 드러났다. 사유 마스크를 두지 않고 통보 한 장으로 끝내는 설계(2026-08-15 #2)는 그대로 두되, 그 한 장에 빠져 있던 둘을 채웠다 — `slot`(3번)과 `inventory_version`(4번). ① **`slot`**: 주무기와 보조무기에 같은 `blueprintId`를 들면 `weapon_id`만으로는 서버가 어느 쪽을 든 상태인지 구분되지 않는다. 지금은 탄약 차감이 `TEMP`로 막혀 있어 드러나지 않지만 `IsUsingPrimary()`가 차감할 탄창을 고르므로 탄창을 살리는 시점에 **탄약 UI가 어긋난다** ② **`inventory_version`**: 거부 사유 셋(`target_slot` 범위 / 버전 불일치 / 빈 슬롯) 중 버전 불일치만 클라이언트 조치가 다르다(재동기화 후 재요청). 구분이 안 되면 클라이언트가 "슬롯이 빈 건가" 하고 재시도를 포기한다. **영구 거부 루프는 아니다** — reliable이 전달을 보장해 클라이언트 버전은 결국 갱신되므로 실제로는 장착 조작이 in-flight인 구간의 일시적 거부이고, 이 필드의 실익은 그 짧은 구간에서 **원인을 알고 즉시 올바른 버전으로 재시도**하는 것이다. **전송 경로를 갈랐다** — 성공 시 `Broadcast` 한 번이던 것을 `BroadcastExcept`(남) + 요청자 개별 `Send`로 나눴다. `BroadcastExcept`가 같은 pkt을 `makeFn`에 그대로 넘기므로 한 장으로는 수신자별 값을 낼 수 없고, 남에게 본인 인벤토리 버전을 흘리지 않는 쪽이 은닉 원칙과 방향이 같다. 클라이언트가 받는 장 수는 이전과 같다. 미설정 표기는 `0xFFFFFFFF` — `_inventoryVersion`이 0에서 시작해 **0이 실재하는 버전**이라 proto3 기본값을 센티널로 쓸 수 없다(`attacker_object_id`와 같은 규약). `slot`은 남에게도 채운다 — 무기는 외형에 드러나 은닉 대상이 아니고, 조건부로 비우면 `FillWeaponChanged`가 수신자를 알아야 한다. **`.proto` 주석은 이 참에 무기 관련 전체를 코드와 맞췄다** — 틀린 판정 규칙을 지우고 3분기 판정표(`slot` 비교 → `inventory_version` 비교)를 넣었고, 공통 사항에 (5) '손에 든 무기' 추적이 네 패킷에 걸쳐 있다는 인덱스를 추가했으며, **이 문서에만 있고 `.proto`에는 없던 두 규칙**(교체 확정 전 발사 트리거 차단, D2C reliable도 순서 보장이 없어 낡은 통보를 `rSeqNum`으로 버릴 것)을 옮겨 적었다. `C2DRequestWeaponFire.weapon_dbid`의 설명도 "장착 중인" → "손에 든"으로 고쳤다(주/보조 둘 다 장착 상태라 원래 표현이 애매했다). **장착 경로는 손대지 않았다** — 요청자 본인은 `D2CResponseEquipItem`만 받으므로 장착으로 손에 든 슬롯이 넘어가는 경우를 클라이언트가 서버 규칙대로 복제해야 하며, 규칙만 `.proto`에 적었다. `D2CResponseEquipItem`에 `current_weapon_slot`을 여는 것은 순수 추가 변경이라 필요해지는 시점에 열린다 (`External_Protocol.proto`, `ClientPacketHandler.cpp`)
- [x] (2026-08-21 #1) 사망 후 5초 세션 유예 — 발단은 "사망 시 유예가 없다"는 확인이었으나, 치명타 처리가 hp=0 `D2CNotifyHealthChange`를 피해자에게 직접 보내며 그 rSeq를 `SetLeaveNotifyRSeq()`로 남기고 있어 **ack 도착 또는 3초 상한까지의 유예가 이미 있었다**(`MarkLeaving`이 `notifyRSeq == 0`일 때 기존 값을 덮어쓰지 않아 살아남는다). 따라서 실질은 "ack 기반 조기 종료 + 3초 상한"을 **"사망은 무조건 5초 확보"** 로 바꾸는 것이었다. 목적은 클라이언트의 급격한 씬 전환 방지 하나이며, **추가 조작이나 연결 종료로 게임 결과를 바꾸는 시도는 일체 차단**한다는 제약이 설계를 지배했다. 골격은 `SessionState`에 **`SPECTATING`을 추가해 "룸에서 분리됨"과 "브로드캐스트 수신 대상에서 제외됨"을 분리**한 것 — `LEFT` 하나가 두 의미를 겸하고 있어 그대로는 유예 중 패킷을 보낼 수 없었다. **차단의 전부는 `IsActiveState()`에 넣지 않은 것** — C2D 핸들러 16개 중 14개가 이미 그 게이트로 시작하고 나머지 둘도 `INIT`/`CONNECTED` 전용이라, 시신 컨테이너 회수·사격·귀환·장비 교체가 한 줄도 더하지 않고 막힌다. 예외는 `Handle_C2D_HeartBeat` 하나로 열었다 — 막으면 클라이언트가 접속 끊김으로 판단해 유예의 목적인 씬 유지가 깨지고, 상태를 바꾸지 않는 순수 에코라 안전하다. 수신 자격은 `CanReceiveBroadcast()`로 분리해 `BroadcastExcept`의 필터를 교체했고(이동·발사·스폰·디스폰·킬 통보가 한 번에 열린다), 상향 ACK는 `HandleClientPacket` 헤더 단계에서 상태 검사 없이 처리되므로 추가 작업이 없었으며 재전송 루프는 `FINALIZED`만 건너뛰므로 유예 중 재전송도 그대로 돈다. **유예 판정은 ack 무관 고정 5초** — `IsFinalizeReady()`에 DEAD 분기를 두고 `DEATH_GRACE_MS(5000)` 경과만 본다. ack 조기 종료를 남기면 통보를 일찍 받았거나 연결을 끊은 클라이언트가 유예를 줄일 수 있고, 부수적으로 `notifyRSeq`가 0으로 남던 두 경우(SendBuffer 확보 실패, 무기 외 사망 원인 추가 시)의 유예 소실도 함께 막힌다. 룸 업데이트가 `roomId % 4` 라운드로빈이라 실제 해제는 5.0~5.1초. **`MarkLeaving()`의 재진입 가드는 재작성이 강제됐다** — `_sessionState == LEFT` 기준이 `SPECTATING` 도입으로 뚫리므로 `_leaveReason` 기준으로 바꿨고, 이것이 유예 중 연결을 끊어 이탈 사유를 `DISCONNECTED`로 바꾸는 경로를 막는 지점이다(`DetectDisconnectedSessions()`는 `IsLeaving()`으로 이미 건너뛰므로 끊어도 서버는 5초를 채우고 DEAD로 끝낸다). 유예를 끝내는 것은 `FinalizeLeave()`가 `LEFT`로 전환하는 한 줄이다. `ClearPendingReliableExcept()`는 **`DetachPlayer()` 앞쪽으로 옮겼다** — 뒤에 두면 같은 함수에서 방금 보낸 시신 스폰·킬 통보가 재전송 큐에서 즉시 지워져 유실 시 복구되지 않는다. 상태 전이 시점상 **킬 통보가 이제 피해자에게도 도달**하므로(사망 화면의 킬러 표시 근거) "LEFT라 피해자를 거른다"던 주석을 반대로 갱신했다. **DB 작업은 유예를 기다리지 않는다** — 이탈 통보를 `FinalizeLeave()`에서 `DetachPlayer()` 끝으로 옮기고 `NotifyLeftOnce()`+`_leftNotified`로 1회를 보장했다(`FinalizeLeave()`의 호출은 분리를 건너뛴 세션용 백스톱으로 남는다). `active_match` 락 해제와 MySQL 인벤토리 반영이 이 통보 한 장에 달려 있어, 유예가 끝나야 나가면 로비로 먼저 돌아간 클라이언트가 재매칭에서 409(`ERR_ALREADY_IN_MATCH`)를 맞고 인벤토리 조회도 사망 전 상태를 본다. **모든 이탈 사유에 적용**했다 — `DetachPlayer()`가 곧 "결과가 확정되는 지점"이라는 불변식을 하나로 유지하는 편이 낫고, 페이로드는 세 사유 모두 이전과 동일하다(두 지점 사이에는 상향이 전부 차단돼 인벤토리가 변할 수 없다). fail-closed 순서(MySQL 성공 후 락 해제)는 그대로다. **결과 확정 자체는 사망 틱에 그대로 뒀다** — 시신 컨테이너 스폰·인벤토리 소멸·필드 디스폰이 전부 같은 틱에 끝나므로 유예 중 언제 어떻게 끊어도 결과는 이미 서버에 박혀 있다. 관전 정보 노출(사망자가 5초간 살아있는 전원의 위치를 계속 받는다)은 **상한 5초로 감수**한다 — 위치 브로드캐스트만 빼면 화면이 얼어붙어 목적과 충돌하고, HP·실드·방어구는 애초에 브로드캐스트에 없다. `D2CNotifyPlayerKilled`에 유예 시간 필드를 여는 것은 **넣지 않기로 확정**했다(클라이언트가 5초 이내에 씬을 정리하고 로비로 돌아가는 쪽으로 구현한다). 함께 테스트용 최소 매칭 인원을 1인 → 2인으로 올렸다(`Matchmaker.cpp`의 `TEMP` 구간, 대기 5초 이상 조건) (`PlayerSession.h/cpp`, `GameRoom.h/cpp`, `ClientPacketHandler.cpp`, `Matchmaker.cpp`, `HTTPServer/database/redis_keys.md`)
- [x] (2026-08-21 #2) 사망 유예 구간의 클라이언트 인수인계 문서화 — 직전 구현(#1)은 서버 동작만 바꿨고 클라이언트가 알 방법이 없었다. `.proto`의 `// [작업사항]` 규약을 그대로 써서 적었으며, 유예가 **여러 패킷에 걸친 동작**이라 개별 message 위가 아니라 파일 상단 **공통 사항 (6)** 으로 넣었다 — 기점(`D2CNotifyPlayerKilled` 수신), 유예 중 계속 도착하는 것(룸 브로드캐스트 전부)과 버려지는 것(하트비트 외 전부), 5초 후 하향 트래픽 종료, 유예 시간을 패킷으로 알려주지 않으므로 5000ms를 클라이언트 상수로 둘 것, 그리고 **게임 결과는 사망 순간에 확정되므로 연결을 끊어도 달라지지 않는다**는 점. 매칭 락 해제가 비동기라 유예를 다 쓰지 않고 즉시 재매칭을 거는 UI라면 409를 실패가 아니라 재시도로 처리하라는 단서도 함께 적었다. **실질은 정정 4건**이다 — #1의 구현이 기존 주석 넷을 조용히 거짓으로 만들었다. ① `D2CNotifyPlayerKilled`의 "피해자 본인은 제외된다"가 정반대가 됐다(`[작업사항]` 블록과 그 아래 영구 주석 둘 다 고쳤고, 피해자에게는 사망 확정 신호를 겸한다고 명시) ② `D2CNotifyHealthChange`가 안내하던 "사망 시 킬러 표기"의 기점을 `D2CNotifyPlayerKilled`로 옮겼다(이 패킷은 피격 방향 표시·교전 상대 추적용으로 남는다) ③ `D2CDespawnPlayerObject`의 "당사자가 사유를 아는 경로" 목록에서 `D2CNotifyHealthChange` → `D2CNotifyPlayerKilled`로 교체하고, `DESPAWN_DEAD`에 **사망자 본인은 자기 제거 통보를 받지 않으므로 유예 동안 캐릭터를 직접 화면에 남겨야 한다**를 추가 ④ `D2CNotifyRecallResult`의 "ACK를 보내야 인벤토리 반출이 제때 확정된다"는 이탈 통보를 `DetachPlayer()`로 옮긴 순간 거짓이 됐다 — 반출은 ACK와 무관하게 귀환 성공 시점에 확정되고 ACK는 세션이 언제 끊기느냐만 좌우한다. **그대로 뒀으면 클라이언트가 ACK 실패를 아이템 손실로 오해할 대목**이었다. `.proto`는 사본이 없어 동기화 대상이 없다 (`External_Protocol.proto`)
- [x] (2026-08-23 #0) 비플레이어 오브젝트 처치 통보 `D2CNotifyObjectKilled` 신설 — 처치로 파괴되는 비플레이어 오브젝트에 대해 "누가 잡았는지"를 룸 전체에 알리는 `D2CNotifyObjectKilled { victim_object_id, killer_object_id }`(pktId 41, reliable)를 추가했다. **처치로 인한 제거는 이 패킷이 `D2CNotifyDespawnObject`(id 39)를 대체한다** — `DestroyDeadObject()`가 despawn 대신 이 패킷을 브로드캐스트하며, 제거 신호를 겸하므로 파괴 1건당 reliable 장 수는 늘지 않는다. 킬러는 `delete` 전에 `GetLastAttackerId()`로 뽑아두고, `CombatObject`가 아니면 `CombatObject::NO_ATTACKER`(0xFFFFFFFF — killer 규약 공유). **피해자가 플레이어인 죽음은 기존 `D2CNotifyPlayerKilled`(id 40) 유지** — 필드 구성이 동일하지만 사망 확정·5초 유예 등 플레이어 전용 의미가 그쪽에 묶여 있어 통합하지 않았다. id 39는 발신처가 0이 된 채 **처치 외 제거 사유가 생길 때를 위한 예약**으로 남긴다(enum·팩토리 유지). 컨테이너는 게임 종료까지 유지되고 전투 사망은 `CombatObject` 뿐이라는 확정에 따라 "열려 있던 컨테이너 UI 닫기" 지침은 두 패킷의 `[작업사항]`에서 제거했다. 무기 정보를 싣지 않는 이유는 id 40과 같다. **`.proto`의 `enum PktId`에 41을 넣는 것을 한 번 빠뜨렸다가 지적받고 채웠다** — message·`enum.h`·팩토리·발신부는 다 맞는데 enum 항목만 비어 있었고, 서버가 `.proto`의 PktId를 참조하지 않아 빌드도 동작도 멀쩡했다. 아래 「진행 고려사항」의 짝 규율이 2026-08-19 #2에 이어 두 번째로 깨진 것이라 그 항목에 재발을 기록했다 (`External_Protocol.proto`, `enum.h`, `ClientPacketHandler.h`, `GameRoom.cpp`)
- [x] (2026-08-24 #0) 매치 종료 후 로비 복귀 시 재로그인을 건너뛰는 `POST /api/session/resume` — 발단은 클라이언트가 GameResultScene → LobbyScene으로 돌아올 때 세션을 이어 쓸 수 있는가였고, 확인해 보니 **서버 세션은 이 시나리오에서 이미 살아 있었다** — `requireAuth`가 `x-session-id`를 검증하고 `refreshSession` Lua가 `sess:*`/`user_sess:*`의 TTL(3600초)을 슬라이딩 갱신하며, 매치 종료 IPC(`D2MNotifyPlayerLeft` → `NotifyPlayerLeftRequest::Execute`)는 `active_match:<db_id>` 락만 지우고 세션 키는 건드리지 않는다. 따라서 필요한 것은 세션 재발급이 아니라 **매치 결과가 반영된 로비 데이터를 다시 주는 엔드포인트 하나**였다. 응답은 `{ uid, money, inventory }`로 login과 같은 스키마이고 **`shopItems`만 뺐다** — 세션 중 불변이라 로그인 때 받은 클라이언트 캐시가 그대로 유효하고, 복귀 때마다 샵 전체를 재전송할 이유가 없다. 조회는 login 핸들러의 두 쿼리(`users.money`, `user_inventory`)를 그대로 재사용해 `Promise.all`로 병렬 실행한다. **게스트는 조회 자체를 건너뛴다** — `user_inventory.uid`에 `users(uid)` FK가 걸려 있어 db_id가 음수인 게스트의 행은 애초에 존재할 수 없고(아래 「진행 고려사항」), `money: 0, inventory: []`는 자리표시자가 아니라 실제 상태와 일치한다. 분기 조건을 `user_type === "0"`이 아니라 **`!== "1"`** 로 둔 것은 세션 해시가 손상됐을 때 정식 유저 경로로 잘못 태워 없는 uid를 조회하지 않기 위해서다. **비정상 세션은 500이 아니라 401** — `db_id` 파싱 실패(NaN)와 users 행 없음(계정 삭제) 둘 다 401로 답한다. 어느 쪽이든 클라이언트 대응은 "일반 로그인 플로우로 폴백" 하나뿐이라 500은 무의미한 재시도만 유발한다. 매치 종료 시 세션 TTL 재충전은 함께 검토했으나 **기각**했다(아래 「진행 고려사항」). 실호출 검증은 아직 하지 않았다(아래 진행 우선사항 4) (`HTTPServer/routes/auth.js`, `HTTPServer/http-api-spec.yaml`)
- [x] (2026-08-25 #0) 발사 시 탄약 차감 재활성화 — `Handle_C2D_RequestWeaponFire`에서 주석으로 죽어 있던 차감 블록을 되살렸다. **그 주석에는 `TEMP:` 마커가 없었다** — "테스트를 위해 임시 비활성화"라고만 적혀 있어 릴리스 전 전수 확인 수단인 `grep -rn "TEMP" src HTTPServer`에 걸리지 않았고 아래 「진행 고려사항」의 개수 집계에서도 빠져 있었다. 비활성화된 차감 지점이 여기 하나뿐임은 `quantity -=`/`--quantity` 전수 검색으로 확인했다(나머지 셋은 컨테이너 이동 둘과 탄창 충전으로 정상 동작 중). **위치는 `IncrementFireSequence()` 뒤 그대로 뒀다** — 탄약 부족으로 거부해도 시퀀스는 소모되어야 한다. 클라이언트는 발사를 시도한 시점에 자기 시퀀스를 올리므로 양쪽이 함께 올라가야 어긋나지 않고, 차감을 시퀀스 증가 **앞**으로 옮기면 서버만 그 값에 멈춰 이후 모든 발사가 시퀀스 불일치로 **영구 거부**된다. **마지막 발을 쏜 뒤 `Clear()`를 부르는 두 줄을 새로 넣었다** — `Slot::IsEmpty()`가 `quantity <= 0 && blueprintId == 0`이라(`Items.h:26`) 차감만 하면 "잔량 0인데 비어 있지 않은" 슬롯이 처음으로 생기고 그 상태가 두 곳으로 샌다: ① `CorpseContainer`의 `place()`가 `IsEmpty()`로만 걸러 `PlaceItem()`을 부르는데 그쪽은 `quantity <= 0`을 거부하고 `nextSlotIndex`는 이미 올라가 **시신 컨테이너 중간에 빈 칸이 낀다**(30칸에 30번 호출이라 넘치지 않아 아이템 손실은 없다) ② `UnloadMagazineToInventory()`가 조기 반환(`PlayerInventory.cpp:71`)에 걸리지 않아 **0발짜리 탄약 스택이 인벤토리 칸을 점유하고** 클라이언트에도 그대로 직렬화돼 나간다. 비워도 잃는 정보는 없다 — 재장전이 붙으면 탄약 종류는 `LoadMagazineFromInventory()`가 `spec->ammoType`에서 다시 뽑는다. **`_inventoryVersion`은 올리지 않기로 확정** — 인벤토리를 바꾸니 올려야 할 것처럼 보이지만, 발사 한 발마다 버전이 오르면 교전 중 클라이언트의 `my_inventory_version`이 상시 낡은 값이 되어 **장착·무기 교체 요청이 전부 `DENY_VERSION_MISMATCH`로 거부된다**. 차감은 `PlayerInventory` 바깥에서 Mutable 게터로 슬롯을 직접 건드리므로 "변경 함수 11곳이 예외 없이 버전을 올린다"는 가드의 전제에 걸리지 않으며, 클라이언트 잔탄과의 어긋남은 명시적 동기화 전까지 정상이라는 전제와도 방향이 같다(동기화 지점은 탄창 두 칸을 이미 싣고 있는 `SerializeFullInventory()`). **잔탄 0의 처리는 조용한 거부** — `weapon_dbid`·`fire_sequence` 불일치와 같은 `return false`라 deny 패킷이 없고 `BroadcastExcept`까지 가지 못해 남에게 발사 이펙트도 나가지 않는다. `.proto`·`enum.h`는 무변경. **재장전은 범위에서 뺐다** — 탄창이 마른 무기는 그 매치 내내 사용 불가가 되며 예비 탄약을 인벤토리에 들고 있어도 꺼내 쓸 수단이 없다(아래 진행 우선사항 2). 클라이언트 측 차감 복구가 별도로 진행 중이라 한쪽만 켜진 구간의 잔탄 어긋남은 정상이며 보정 로직을 넣지 않는다 (`ClientPacketHandler.cpp`)
- [x] (2026-08-25 #1) 플레이어 최대 HP를 1000.00 → 200.00으로 조정 — `PlayerObject::DEFAULT_MAX_HP`를 `100000` → `20000`으로 낮추고 주석의 표기 수치도 함께 갱신했다. **`TEMP:` 마커는 유지했다** — 200.00도 실 스펙이 아니라 여전히 피격 테스트 편의값이므로 위 마커 규율의 전수 확인 대상에서 빠지면 안 된다(집계 8곳은 그대로다). **파생 로직은 한 줄도 고치지 않았다** — `CombatObject` 생성자가 `_maxHp(maxHp), _currentHp(maxHp)`로 초기화해 스폰 시 풀피가 자동으로 따라오고, `PlayerObject`의 두 생성자 모두 이 상수를 넘겨 **HP 리터럴을 직접 쓴 곳이 0곳**이다(전수 검색으로 확인). **실질은 서버 단독 변경이 아니라 양쪽 상수를 맞추는 일이었다** — 최대 HP는 어떤 패킷에도 실리지 않고(`D2CNotifyHealthChange`는 현재 HP 절대값만 싣는다) 클라이언트는 자기 상수 `IngameScene.MAX_HEALTH_POINT`를 최대치의 유일한 출처로 쓰므로, 한쪽만 바꾸면 컴파일도 통신도 정상인 채 **체력바 비율만 조용히 틀어진다**. 클라이언트도 `20000`으로 함께 바뀐 것을 확인했고, 이 계약은 아래 「진행 고려사항」에 남겼다. **밸런스 결과** — AK-47 1발(48.00) 기준 맨몸 **5발**. 경량 조끼는 실드 만충(100.00)이면 **6발**이다(1~2발은 실드가 버티며 감소율 50%가 걸려 24.00씩만 들어가고, 3발째부터 관통해 감소율 없이 48.00이 들어간다). **매치 중에 주워 입으면 5발 그대로** — 착용 시 실드가 0에서 시작하고 재생이 1.00/초라 만충까지 100초가 걸리며, 6발이 나오는 것은 로드아웃에 넣고 입장해 `ChargeShield(GetMaxShield())`를 탄 경우뿐이다. 실드 최대치는 상수가 아니라 착용 방어구의 `ArmorSpec.maxShieldPoint`에서 나오므로 이번 범위에서 제외했다 (`UnityGameObjects/PlayerObject.h`)

---

## 진행 중 / 다음 할 것들

### 진행 우선사항
1. **사망 연출** — 사망 시 이탈 처리(오브젝트 제거·퇴장 통보·인벤토리 소실), 시신 컨테이너, 비플레이어 전투 오브젝트의 사망 경로, 가해자 전달·킬 피드, **사망 후 5초 세션 유예**까지 구현 완료. 현재 클라이언트가 받는 것은 `D2CDespawnPlayerObject(DESPAWN_DEAD)`, 시신의 `D2CNotifySpawnObject`, `D2CNotifyPlayerKilled`(**피해자 본인 포함**), 그리고 피해자 본인의 `D2CNotifyHealthChange(attacker_object_id)` 넷이며, 사망 후 5초 동안은 룸 브로드캐스트가 계속 도착한다. 남은 것은 **사망 애니메이션·연출**뿐이며, 오브젝트마다 다르므로 `OnDeathResolved()` 안에서 각자 처리하는 형태가 된다. 킬 피드에 표시할 이름은 아직 없다 (아래 「진행 고려사항」의 표시명 항목)
2. **재장전 경로 신설** — 2026-08-25 #0으로 탄약 차감이 살아나면서 **탄창이 마른 무기는 그 매치 내내 사용 불가**가 됐다. 런타임 충전 호출부가 0곳이고(`LoadMagazineFromInventory()`는 `PlayerInventory` 생성자에서만 불린다) 재장전 패킷도 없어, 예비 탄약을 인벤토리에 들고 있어도 꺼내 쓸 수단이 없다. 착수하면 `C2DRequestReload`(pktId **42**, `PKT_ID_MAX`를 43으로) 신설 + `LoadMagazineFromInventory()`를 재장전 진입점으로 여는 형태가 된다 — 그 함수가 `remaining = maxAmmo - quantity`로 **부분 충전을 이미 지원**하고 `_inventoryVersion` 증가와 `UpdateFirstEmptySlotIndex()`까지 처리하므로 재사용이 가능하다. 순서 역전 대비 `my_inventory_version` 가드는 `C2DRequestSwitchWeapon`과 같은 관용구로 둘 것. **잔탄 0의 조용한 거부에 통보 수단이 필요해지는 시점도 여기다** — 지금은 마른 무기가 무반응이라 클라이언트가 원인을 알 수 없고 서버 로그로만 확인된다 (`ClientPacketHandler.cpp`, `PlayerInventory.h/cpp`, `External_Protocol.proto`, `enum.h`)
3. **HeartBeat / RequestBlueprint / RequestSpawnMe 클라이언트 연동 테스트 필요** — 프로토콜·직렬화 수정 완료, 빌드 통과. 클라이언트 측 구현 필요
    - `[DROP] 서명 불일치` 출력 → 클라이언트 서명 계산 로직 점검
    - `[DROP] unreliable 시퀀스 중복` 출력 → 클라이언트 uSeqNum 증가 로직 점검
    - **헤더 `timestampEcho` 에코 미구현 시 6초 뒤 강제 이탈** — 수신 패킷의 헤더 `timestamp`를 보관했다가 자신이 보내는 모든 패킷의 `timestampEcho`에 **가공 없이** 되돌려줘야 한다(서버 시계 도메인 값). 채우지 않으면 RTT 측정과 끊김 감지가 함께 무력화되고, 채우다 멈추면 6초 뒤 `DISCONNECTED`로 이탈 처리되어 인벤토리가 소실된다. 현재 `echo == 0` 세션은 판정에서 제외되지만 과도기 조치라 의존하지 말 것. **재전송 버퍼가 실어 오는 낡은 에코는 서버가 역행으로 판정해 버린다** — 무해하지만 갱신에도 기여하지 않으므로, 생존을 증명하는 것은 새로 만든 패킷의 에코뿐이다 (아래 「진행 고려사항」의 끊김 감지·재전송 헤더 항목)
    - StaticObjects 역직렬화 시 `TransformInfo` 구조 변경 반영 여부 클라이언트 측 확인 필요
    - `TestGameRoom::InitTestGameRoom()`에서 TestItemBox 등록 → Blueprint 응답(StaticObjects 청크)에 포함되는지 확인
    - **오브젝트 생성 브로드캐스트 수신 처리 미구현** — ① `D2CNotifySpawnObject`(id 36) 핸들러 신설. 페이로드가 `D2CResponseSpawnByObjectId`와 동일하므로 기존 역직렬화를 재사용할 수 있다 ② `D2CSpawnPlayerObject`(id 13)를 **요청 없이 서버가 먼저 보내는 경우**의 처리 — 지금까지는 `C2DRequestSpawnByObjectId`의 응답으로만 도착했다. ①은 시신 컨테이너가 붙으면서 선택이 아니게 됐다 — 없으면 **시신이 살아있는 플레이어 화면에 보이지 않고** 재접속해 SpawnMe를 새로 받은 경우에만 나타난다
    - **`ObjectType 3`(시신) 렌더링 미구현** — `object_type`이 `int32`라 프로토콜 변경 없이 값만 늘었다(0=None, 1=Player, 2=TestItemBox, 3=Corpse). 클라이언트가 3을 시신으로 매핑해야 하며, 시신은 열 수 있는 컨테이너이기도 하므로 상호작용 대상으로도 잡아야 한다
    - **`D2CNotifyWeaponChanged`(id 37) 수신 처리 미구현** — 같은 룸 플레이어의 손에 든 무기 갱신이자 **자기 무기 교체의 유일한 결과 통보**다. 성공·거부 모두 본인에게 오므로 **`object_id`로 성공/실패를 가르면 안 된다**(구 스펙의 규칙이었고 지금은 틀리다). 판정은 `slot`이 보낸 `target_slot`과 같은지로 하고, 다르면 `inventory_version` 비교로 사유가 갈린다 — 불일치면 버전이 낡은 것이므로 `C2DRequestRecentInventoryInfo(0xFFFFFFFF)`로 재동기화한 뒤 재요청, 일치면 대상 슬롯이 빈 것이므로 UI만 되돌린다. 남의 변경 통보에는 `inventory_version`이 `0xFFFFFFFF`로 온다(`0`은 실재하는 버전이다). 연달아 교체하면 통보가 역순으로 도착할 수 있으므로 마지막으로 반영한 헤더 `rSeqNum`보다 낮은 것은 버릴 것. **방어구는 오지 않는다** — 외형에 드러나지 않으므로 의도적으로 뺐다(아래 「진행 고려사항」의 은닉 항목)
    - **자기 실드 재생 예측 미구현** — 서버는 재생을 100ms 틱으로 돌리지만 **통보 패킷을 보내지 않는다**. 클라이언트가 마지막으로 받은 `D2CNotifyHealthChange`의 `shield_point`를 기준점으로 삼고 자기 방어구의 `ArmorSpec.regenerationPerSecond`로 경과분을 더해 그리며, 다음 `D2CNotifyHealthChange`가 오면 그 값으로 덮는다. 최대치 clamp를 잊지 말 것 — 서버는 `_maxShield`에서 멈춘다. 피격 후 재생 지연은 **없다**(연속 재생)
    - **`D2CNotifyObjectKilled`(id 41) / `D2CNotifyDespawnObject`(id 39) 수신 처리 미구현** — 비플레이어 오브젝트가 **처치로 파괴되면 id 41**이 룸 전체에 오며 제거 신호를 겸한다(`victim_object_id`의 오브젝트를 제거할 것, `killer_object_id == 자기 objectId`면 본인 킬, `0xFFFFFFFF`는 가해자 없음). **id 39는 처치 외 사유의 제거용 예약**으로 현재 서버가 보내는 경로가 없다 — 수신 시 동작(해당 오브젝트 제거)은 id 41과 같게 구현해 둘 것. 플레이어는 기존 `D2CDespawnPlayerObject`(id 35)를 계속 쓰므로 지우는 대상이 패킷으로 갈린다. 파괴 가능한 오브젝트가 추가되기 전까지는 두 패킷 모두 서버가 보내지 않는다
    - **`D2CNotifyPlayerKilled`(id 40) 수신 미구현** — 킬 피드이자 **피해자 본인의 사망 확정 신호**다. 사망 유예 도입으로 피해자도 이 패킷을 받게 됐으므로(`killer_object_id`로 킬러 표시) 씬 정리 타이머의 기점으로 삼을 것 — 아래 사망 유예 항목 참조. 무기 아이콘이 필요하면 `D2CSpawnPlayerObject.weapon_id` + `D2CNotifyWeaponChanged`로 직접 추적할 것 — 다만 **살아있는 오브젝트에서 즉석 조회하면 안 된다**. 상호 사살이나 킬 직후 귀환·끊김이면 킬 피드 도착 시점에 킬러 오브젝트가 이미 디스폰돼 있고, D2C reliable도 순서 보장이 없어 디스폰이 먼저 처리될 수 있다. `objectId → (마지막 무기, 캐릭터 타입)`를 디스폰 뒤에도 남겨둘 것(objectId는 룸 안에서 재사용되지 않아 캐시가 커지지 않는다)
    - **`D2CNotifyHealthChange.attacker_object_id`(4번 필드) 활용 미구현** — 피격 방향 표시(damage indicator)와 "누구에게 죽었나". 사망 시에도 이 패킷이 피해자에게 도달하도록 서버가 보장한다. 위와 같은 이유로 이 id도 이미 사라진 오브젝트를 가리킬 수 있다
    - **`C2DRequestSwitchWeapon`(id 38) 송신 미구현** — `target_slot`(0=주무기, 1=보조무기)과 자신이 아는 `my_inventory_version`을 실어 보낸다. **교체 확정(`D2CNotifyWeaponChanged` 수신) 전까지 발사 트리거를 막을 것** — reliable/unreliable 채널 사이에는 순서 보장이 없어 사격이 먼저 처리되면 `weapon_dbid` 불일치로 조용히 버려진다 (클라이언트 측에서 구현하기로 확정). 결과 판정은 위 `D2CNotifyWeaponChanged` 항목 참조
    - **장착으로 손에 든 슬롯이 넘어가는 경우 미반영** — `C2DRequestEquipItem` 요청자 본인은 `D2CResponseEquipItem`만 받고 `D2CNotifyWeaponChanged`를 받지 않는다. 들고 있던 무기를 내리면 손에 든 슬롯이 반대쪽으로 넘어가므로(양쪽 다 비면 맨손) 클라이언트가 이 서버 규칙을 복제해 반영해야 한다. 규칙이 어긋나면 다음 사격이 `weapon_dbid` 불일치로 조용히 버려진다. 복제가 부담이면 `D2CResponseEquipItem`에 `current_weapon_slot`을 여는 순수 추가 변경으로 서버가 알려줄 수 있다
    - **사망 후 5초 유예 구간의 클라이언트 처리 미구현** — 사망이 확정돼도 서버는 세션을 5초 더 유지하며 룸 브로드캐스트(타 플레이어 이동·발사·스폰·디스폰)를 **계속 보낸다**. 그동안 클라이언트가 보내는 요청은 하트비트를 제외하고 전부 무시되고(응답 없음), 5초가 지나면 하향 트래픽이 끊긴다. 기점은 `D2CNotifyPlayerKilled` 수신이며, **5초 이내에 씬을 정리하고 로비로 돌아가는 것이 클라이언트 몫**이다(유예 시간을 패킷으로 알려주지 않기로 확정했으므로 5000ms를 상수로 들 것). 이 구간의 목적은 급격한 씬 전환 방지 하나이며 조작 복구가 아니다 — 인벤토리 소실과 시신 컨테이너는 사망 순간에 이미 확정되어 연결을 끊어도 되돌아가지 않는다. 상세는 `External_Protocol.proto` 상단 **공통 사항 (6)**
    - **탄약 차감 클라이언트 측 복구 진행 중** — 서버는 2026-08-25 #0부터 발사 1건당 탄창에서 1발을 차감한다. **한쪽만 켜진 구간에서 양측 잔탄이 어긋나는 것은 정상이므로 보정 로직을 넣지 말 것** — 서버가 최종 판정 권한을 가지며, 서버 잔탄이 0이면 발사가 **조용히 거부**되어 데미지도 남에게 가는 발사 이펙트도 없다(deny 패킷 없음, 서버 로그 `[Handle_C2D_RequestWeaponFire] 탄약 부족`만 남는다). 재동기화 지점은 `C2DRequestRecentInventoryInfo` → `D2CFullInventorySync`이며 탄창 두 칸이 포함돼 있다. **재장전 수단은 아직 없다**(위 진행 우선사항 2) — 탄창을 비우면 그 무기는 매치가 끝날 때까지 못 쏜다
    - 위 항목들의 상세는 `External_Protocol.proto`의 `// [작업사항]` 주석에 패킷별로 적혀 있다. **클라이언트 반영이 끝나면 그 접두어가 붙은 블록만 일괄 삭제할 것** — 임시 주석이며, 남겨두면 코드 주석 규약(「진행 고려사항」)과 충돌한다
4. **`POST /api/session/resume` 실호출 검증 및 클라이언트 연동** — 구현은 끝났고 Redis·MySQL이 붙은 환경에서의 확인만 남았다. ① `/api/login` 후 그 sessionId로 호출 시 200 + `uid`/`money`/`inventory`가 DB 값과 일치하는지 ② 호출 직후 `TTL sess:<UUID>`가 3600 근처로 재충전됐는지(미들웨어 슬라이딩 갱신 확인) ③ 삭제·만료된 sessionId로 호출 시 401 ④ `/api/guest` 세션으로 호출 시 200 + `money: 0, inventory: []`. 클라이언트의 `HTTPManager.cs` `PostResumeSessionCall`은 이 응답 스키마를 파싱하도록 이미 구현돼 있으므로 **응답 필드를 바꾸려면 루트 세션에서 양쪽을 함께 고쳐야 한다** — `Extraction/Assets/Scripts/Network/http-api-spec.yaml`이 `HTTPServer/http-api-spec.yaml`의 사본이라 명세도 짝으로 움직인다
5. **윈체스터 룸의 고유 게임 루프 로직** — 상태 브로드캐스트는 `GameRoom::Update()`가 모든 룸에 대해 처리하므로 `WinchesterGameRoom`은 현재 `Update()`를 override하지 않는다. 맵 고유의 주기 처리(이벤트 등)가 정해지면 `Update() override`를 새로 쓰면서 **파생 로직을 먼저 하고 마지막에 `GameRoom::Update()`를 부를 것**. AI는 서버에서 다루지 않기로 했다 (`GameRoom.h/cpp`)

### 진행 고려사항
- **`TODO:` / `TEMP:` / `OPTION:` 주석 구분** — `TODO:`는 아직 구현되지 않은 것, `TEMP:`는 테스트를 위해 의도적으로 값을 제한하거나 코드를 막아둔 것, `OPTION:`은 없어도 동작에 문제가 없고 여유가 생기면 구현할 개선 항목이다. 릴리스를 막는 것은 `TEMP:`뿐이므로 릴리스 전 되돌려야 할 항목은 `grep -rn "TEMP" src HTTPServer`로 전수 확인할 것 (현재 8곳). `OPTION:` 항목은 할 일 목록에 올리지 않고 주석으로만 남긴다. **이 전수 확인은 마커를 빠뜨리지 않았다는 전제 위에 있다** — 2026-08-25 #0에서 되살린 탄약 차감이 `TEMP:` 없이 "테스트를 위해 임시 비활성화"라고만 적힌 채 주석 처리돼 있어 집계에서 빠져 있었다. 코드를 주석으로 막을 때는 문구가 아니라 마커를 먼저 붙일 것
- **코드 주석은 이 세 종류와 상수 유래뿐** — 흐름 서술("~를 전송한다"), 단계 나열, 구획 배너, 설계 배경은 코드에 쓰지 않는다. 코드만 봐서는 알 수 없는 제약·불변식·결정 배경은 **이 문서의 「진행 고려사항」에 적는다** (2026-08-13 #7·#8에서 코드베이스 전체 466줄을 이 기준으로 정리). 코드에 남기는 예외는 숫자만 남는 상수의 유래(슬롯 범위, 페이로드 한계, 프로토콜 반환 코드 등)와 바이트 레이아웃. `src/sample.h`만 예외로 남겨뒀다 — 학습 노트 성격이라 정리 대상으로 본다면 파일째 삭제가 맞다
- **`active_match` 락은 네 곳이 한 묶음** — `match.js`의 `/start` EX 값, `matchCancel` Lua의 `return 2`/`return 3` 분기, `DBProxyRequest.h`의 `ACTIVE_MATCH_TTL_SEC`, `redis_keys.md` 2번 절이 서로를 전제한다. TTL(3600초)은 **백스톱일 뿐**이고 정상 해제는 룸 분리(`GameRoom::DetachPlayer()`) 한 곳이다. 세 가지 불변식을 깨지 말 것 — ① TTL은 최대 게임 길이보다 길어야 한다(짧으면 게임 도중 락이 풀린다) ② `match.js`의 EX와 `ACTIVE_MATCH_TTL_SEC`는 같은 값이어야 한다 ③ `INGAME:` 접두어는 `UpdateEntryTokenRequest`가 쓰고 `matchCancel`이 읽는다. 티켓 TTL(300초)이 락 TTL보다 짧아 생기는 "티켓 없고 락만 남은" 구간을 이 접두어로만 구분할 수 있다
- **이탈 통보는 `DetachPlayer()`에서 한 번만 나간다** — `D2MNotifyPlayerLeft`가 Main의 인벤토리 DB 반영과 `active_match` 락 해제를 모두 촉발하므로, 통보 시점이 곧 "게임 결과가 확정되는 시점"이다. 그래서 세션 종료(`FinalizeLeave()`)가 아니라 **룸에서 분리되는 시점**에 보낸다 — 사망 유예 5초를 기다리면 그동안 락이 잡혀 있어 로비로 먼저 돌아간 클라이언트가 재매칭에서 409를 맞는다. 중복 방지는 `PlayerSession::NotifyLeftOnce()`의 `_leftNotified` 하나이고 `FinalizeLeave()`의 호출은 분리를 건너뛴 세션용 백스톱이다. **통보 시점을 옮기려는 변경은 페이로드 동결 조건을 먼저 볼 것** — 지금 안전한 이유는 분리 시점부터 모든 상향 요청이 차단돼 인벤토리가 변할 수 없어서다
- **`SessionState::SPECTATING`은 "하향만 받고 상향은 전부 버린다"** — 사망 유예 5초 동안의 상태이며, 조작 권한(`IsActiveState()`)과 브로드캐스트 수신 자격(`CanReceiveBroadcast()`)을 갈라놓은 것이 이 상태의 존재 이유다. **차단은 `IsActiveState()`에 이 상태가 없다는 사실 하나에 전적으로 의존한다** — 새 C2D 핸들러를 추가할 때 그 게이트를 빠뜨리면 죽은 플레이어가 자기 시신을 털거나 사격할 수 있게 되고, 컴파일도 통과하고 정상 플레이에서도 드러나지 않는다. 의도적 예외는 `Handle_C2D_HeartBeat` 하나뿐이며(막으면 클라이언트가 접속 끊김으로 판단해 유예의 목적이 깨진다) 상태를 바꾸지 않는 요청에만 이 예외를 허용할 것. 유예를 끝내는 것은 `FinalizeLeave()`가 `LEFT`로 되돌리는 한 줄이다
- **MySQL과 Redis 사이에는 공유 트랜잭션이 없다** — 이탈 반영은 **MySQL 먼저, 락 해제 나중**(fail-closed) 순서에 의존한다. 락을 먼저 풀면 DB 반영 실패 시 유저가 반영 안 된 인벤토리로 새 매치를 시작한다. 반대로 실패 시 락을 계속 잡으면 영구 잠금이 되므로, 재시도 2회 후에는 락을 풀고 페이로드를 error 로그로 남긴다. 이 순서를 바꾸는 변경은 `NotifyPlayerLeftRequest::Execute()` 안에 갇혀 있다
- **매치 종료 시 세션 TTL 재충전은 하지 않기로 확정** — TTL이 갱신되지 않는 구간은 매치 전체가 아니라 **마지막 HTTP 호출(`/api/game/match/start`)부터 복귀 후 `/api/session/resume`까지**다. 매치 중에는 클라이언트가 Dedicate와 UDP로만 통신해 `requireAuth`의 슬라이딩 갱신이 걸리지 않지만, 정상 매치가 길어야 15분이라 TTL 3600초와는 자릿수가 다르다. 초과하더라도 실패 모드가 **401 → 일반 로그인 폴백**이라 인벤토리가 유실되지 않는다(결과는 사망·귀환 시점에 이미 MySQL에 박혀 있다). 붙인다면 자리는 `NotifyPlayerLeftRequest::Execute()`지만 그 경로는 `db_id`만 알고 있어 `user_sess:<userId>` 키에 닿으려면 **db_id → user_id 역참조 수단을 새로 만들어야 한다**(매치 시작 시 sessionId를 Redis에 함께 기록하는 등). 재검토 조건은 결과 화면 체류가 길어지는 UI 변경이나 세션 TTL을 크게 줄이는 결정이다
- **게스트는 MySQL에 행이 없고 그것을 FK가 강제한다** — `/api/guest`는 `guest_uid_counter`를 `DECR`해 **음수 db_id**를 발급하고 `users` 행은 만들지 않는다. `user_inventory.uid`의 `users(uid)` FK가 음수 uid의 INSERT를 거부하므로(`schema.sql:28`) 게스트의 재화·인벤토리는 어느 경로로도 영속되지 않는다 — 이탈 반영이 `_uid < 0`을 먼저 건너뛰는 것도(`DBProxyRequest.cpp:188`) 같은 이유다. 따라서 게스트에게 인벤토리를 돌려주는 API는 **빈 배열이 오답이 아니라 정답**이고, 세션 데이터만 보고 조회를 거는 새 엔드포인트는 `user_type`으로 먼저 갈라야 한다. 게스트에게 영속 데이터를 주려는 요구가 생기면 그 지점은 API가 아니라 FK이며, 정식 계정 전환을 설계해야 한다
- **`sql::Connection`은 자동 재연결이 없다** — `sw::redis::Redis`와 달리 커넥션 풀도 재연결도 없어, `wait_timeout`(기본 8시간)이나 HeatWave 페일오버로 끊기면 이후 모든 사용이 예외를 던진다. **MySQL을 쓰는 코드는 반드시 `pMysql->Get()`으로 핸들을 받을 것** — 그 안에서 `isValid()`→`reconnect()`가 처리된다. `sql::Connection*`를 어딘가에 캐시하면 이 보호가 무력화된다
- **`damageReductionRate`는 실드 잔량이 있을 때만 유효하다** — 실드가 0으로 소진되면 `_currentShield -= damage`가 즉시 음수가 되어 penetrated 경로를 타고 감소율이 적용되지 않는다. 이것이 **의도된 설계로 확정**됐다(2026-08-18 #2). 즉 armor는 "상시 방어력"이 아니라 "실드가 버티는 동안의 감쇄"이므로, 밸런싱에서 `damageReductionRate`를 상시 적용으로 읽지 말 것 (`CombatObject.h`)
- **남의 HP·실드 수치와 방어구 정보는 클라이언트에 보내지 않는다** — 매 틱 브로드캐스트되는 `PlayerState`에는 이동·조준만 있고, `D2CNotifyHealthChange`(hp·shield 절대값)는 `pHitSession->Send()`로 **피격 당사자에게만** 간다. 적 체력바는 원천적으로 불가능하며 이는 의도된 결과다. 방어구도 같은 이유로 뺐다(2026-08-19 #0) — **외형에 드러나지 않고**, `blueprint_id` 하나가 `ArmorSpec`(최대 실드·피해 감소율·재생량) 전체와 동치라 id만 흘려도 곧 수치 노출이 된다. 서버가 안 보내면 클라이언트를 뜯어도 못 얻으므로 치트 표면적도 함께 줄어든다. **새 D2C 패킷에 남의 hp·실드·방어구를 싣지 말 것.** 무기(`weapon_id`)는 외형에 보이므로 예외이고, 히트마커처럼 "맞았다/못 맞았다" bool만 돌려주는 것은 이 원칙과 충돌하지 않는다
- **방어구 착용 시 실드가 0인 것은 의도된 설계다** — `SetShield()`가 `_currentShield = 0`으로 초기화하는 것을 버그로 보지 말 것. 착용 시 만충이나 이전 실드 비율 유지로 바꾸면 인벤토리에 방어구를 쟁여두고 실드가 0이 될 때마다 스왑해 **실드 파괴 리스크를 지지 않는 플레이**가 가능해진다. 만충으로 시작하는 유일한 예외는 스폰 시점의 `ChargeShield(GetMaxShield())`이고, 그 뒤로 채우는 수단은 `RegenShield()` 하나뿐이다. **피격 후 재생 지연은 없다** — `ArmorSpec`에 지연 필드가 없어 현재 데이터 모델이 표현할 수 있는 것이 연속 재생뿐이라서다. 필요해지면 `ArmorSpec` 필드 하나와 `_lastDamagedMs` 추가로 열린다(`TakeDamage()`가 매 피격에 불리므로 기록할 자리는 이미 있다)
- **서버와 클라이언트가 같은 수치를 각자 하드코딩한 곳이 셋이다** — ① `PlayerObject::DEFAULT_MAX_HP` ↔ `IngameScene.MAX_HEALTH_POINT` ② `ItemDataManager::_weaponSpecs` ↔ `ItemDBHelper._weaponSpecs` ③ 같은 두 파일의 `_armorSpecs`. **어긋나도 아무것도 깨지지 않는다** — 최대 HP는 어떤 패킷에도 실리지 않고 스펙 테이블은 id만 오가므로, 컴파일도 통신도 전부 정상인 채 체력바 비율이나 반동·잔탄 표시만 조용히 틀어진다(아래 `enum.h` ↔ `.proto` PktId와 같은 종류의 침묵 실패다). 전투 수치가 전부 100배 스케일이라는 규칙도 양쪽이 함께 지킨다. 수치를 건드릴 때는 예외 없이 양쪽을 같이 고칠 것
- **가해자 id 필드는 예외 없이 명시적으로 채울 것** — `D2CNotifyHealthChange.attacker_object_id`와 `D2CNotifyPlayerKilled.killer_object_id`의 "없음"은 `0xFFFFFFFF`(`CombatObject::NO_ATTACKER`)다. **proto3 기본값 0은 실재하는 object_id**이므로(`GameRoom::_nxtObjectId`가 0부터 발급) 설정을 빠뜨리면 0번 오브젝트에 오귀속된다. 지금은 가해자 없는 피해 경로가 없어 드러나지 않지만, 회복(`REASON_ITEM_HEAL`)·낙하·지역 피해가 붙는 시점에 걸린다
- **킬 피드에 실을 표시명이 없다** — `Protocol/` 전체에 이름 필드가 하나도 없어 클라이언트가 표시할 수 있는 것은 objectId뿐이다. 서버에는 `Player::GetUserId()`가 있지만 그건 로그인 계정 id라 다른 플레이어에게 노출할 것이 못 된다. 표시명을 붙이려면 Main→Dedicate IPC로 닉네임을 실어오고 `D2CSpawnPlayerObject`에 필드를 여는 별도 작업이 필요하다. 그때까지 킬 피드의 실질 가치는 "내가 누구에게 죽었나"(피해자)와 "내가 잡았다"(킬러) 둘이며, 이 둘은 이름 없이도 성립한다
- **통보 대상·시점·보장 범위를 바꾸면 `.proto`의 `[작업사항]` 주석을 함께 훑을 것** — 두 번 연속 어긋났다. 2026-08-21 #0에서는 무기 교체 판정 규칙이 코드와 **정반대**로 적혀 있었고(그대로 구현하면 모든 성공이 실패로 오판된다), #1의 사망 유예는 기존 주석 넷을 한 번에 거짓으로 만들었다(#2에서 정정). 원인은 같다 — **이 주석은 서버 빌드에 아무 영향이 없어 틀려도 아무것도 깨지지 않고**, 깨지는 자리가 클라이언트 세션이라 발견이 늦다. 특히 위험한 부류는 필드가 그대로인데 **수신 대상이나 의미만 바뀌는 변경**이다(브로드캐스트 필터 교체, 통보 시점 이동, ACK가 보장하던 것의 축소). 구현 diff에 `.proto`가 없으면 주석을 안 봤다는 뜻으로 읽을 것
- **패킷을 추가하면 `enum.h`와 `.proto`의 `enum PktId`가 짝이다** — 서버는 `enum.h`의 `PKT_ID_*`만 쓰고 `.proto`의 `PktId`를 참조하지 않으므로, `.proto` 쪽을 빠뜨려도 **서버는 정상 빌드되고 정상 동작한다**. 대신 생성 코드를 쓰는 클라이언트가 그 패킷의 id 상수를 얻지 못해 분기 자체를 만들 수 없다(2026-08-19 #2에서 다섯 개가 이 상태였다). `PKT_ID_MAX`도 함께 올릴 것 — 핸들러 배열 크기이자 수신 시 범위 검사 기준이라 빠뜨리면 새 C2D 패킷이 조용히 버려진다. 통합하지 않은 이유는 서버가 `.proto` PktId를 참조하면 배열 크기 관용구가 protobuf 헤더에 묶이기 때문이며, 위험 구간이 "패킷 추가" 한 시점뿐이라 규율로 지킨다. **다만 그 규율이 두 번 연속 깨졌다** — 2026-08-19 #2(다섯 개 누락)에 이어 2026-08-23 #0에서도 message·`enum.h`·팩토리·발신부를 다 맞추고 `enum PktId` 한 줄만 빠뜨렸다. 나머지가 전부 맞으면 "다 했다"는 착각이 들고 빌드도 통과하므로, **패킷 추가 시 `enum PktId` 확인을 마지막 단계로 따로 둘 것**
- **IPC 프로토콜 파일을 추가하면 `CMakeLists.txt` 두 곳을 같이 고칠 것** — `PROJECT_SOURCES`의 `.pb.cc`/`.pb.h` 목록과 `IPC_PB_NAMES`. 한쪽만 고치면 컴파일 대상에서 빠지거나 `Protocol/Compiled/IPC/` → `src/IPCProtocol/` 복사가 누락된다. External 프로토콜은 protoc가 `src/` 아래로 직접 생성하므로 `PROJECT_SOURCES`만 보면 된다
- **`_allocatedPlayers`의 증감은 두 곳뿐이고 짝이 맞는다** — 증가는 `M2DSession::AllocatePlayers()`의 `+= ticketVec.size()`, 감소는 `ReleasePlayers()`가 받는 `player_count`(= 룸의 `_playerSessions.size()`)뿐이다. 둘이 같다는 보장은 코드에 드러나지 않으므로 근거를 남긴다 — ① `MatchMaker::VerifyAndSetMatchStatus()`의 Lua가 그룹 전원의 티켓 해시 존재와 `WAITING` 상태를 원자적으로 확인한 뒤 `INPROGRESS`로 바꾸고, 1을 반환했을 때만 `DistributePlayerGroup()`이 호출된다 ② Main은 단일 스레드이고 그 검증부터 `MakeM2DMakeRoomForThisGroup()`의 `hgetall`까지 연속 호출이라 중간에 io_uring CQ도 다른 핸들러도 끼어들지 못한다(가용 세션이 없을 때의 `fork`+`execl`만 들어오며 Redis를 건드리지 않는다) ③ 유일한 외부 삭제자인 Node `matchCancel` Lua는 `WAITING`일 때만 `DEL`하므로 `INPROGRESS` 티켓을 지우지 못한다. 따라서 `MakeM2DMakeRoomForThisGroup()`의 `if (val.empty()) continue`는 정상 동작에서 도달하지 않고 `player_infos` 크기는 `ticketVec.size()`와 같다. **이 세 조건 중 하나라도 바꾸면 카운트가 어긋나 프로세스가 다시 누적 증가한다.** 남는 이론적 구멍은 티켓 TTL(300초) 만료가 ①과 ② 사이 마이크로초에 걸리는 경우와 Redis eviction뿐이라 대응하지 않기로 했다
- **전용 프로세스는 룸이 비어도 종료하지 않는다** — 서버 가동 중 관측된 최대 수용 인원만큼의 프로세스를 유지하고, 이후 인원이 줄어도 회수하지 않는다는 결정(fork·exec과 초기화 비용 회피). 이에 따라 `GameRoom::CheckAllLeft()`에 있던 "남은 룸이 없을 때의 프로세스 정리" TODO를 제거했다. 딸려오는 것 둘 — ① 이 결정의 전제(프로세스 수가 최대 동시 인원에 수렴한다)는 `_allocatedPlayers` 감소 경로가 붙어서야 성립한다 (2026-08-14 #1에서 구현 완료) ② 유휴 프로세스도 `DedicateMain`의 1ms 슬립 루프 때문에 초당 1000회 깨어난다. 수십 개가 되면 폴링만으로 상시 CPU를 먹으므로 룸 0개일 때의 백오프(1ms→50ms)가 필요해지지만, **예상 프로세스 수가 2개 규모라 지금은 보류**한다
- **지연 콜백은 예외 없이 sessionId 재조회 규약을 따를 것** — `TimerExecuter`에 취소 API가 없어 룸/세션이 정리된 뒤에도 잔여 콜백이 실행될 수 있다. 콜백이 `PlayerSession*`를 캡처하지 않고 sessionId로 매번 재조회하며 uid·generation을 검증하면(현재 `RecallTick()`이 이 방식), 정리 시 `_players[id] = nullptr`만으로 잔여 콜백이 스스로 포기하므로 별도의 정지 확인(quiescence) 로직이 필요 없다. **한 곳이라도 raw 포인터를 캡처하면 이 설계가 무너진다.** 룸 소멸로 sessionId 반납이 실제로 시작됐으므로 이 규약은 이제 선택이 아니다 — `_freePlayerIds`가 FIFO라 반납된 id는 앞선 것들이 모두 소진된 뒤에야 재사용되어 간격 자체는 넉넉하지만, 재사용이 아예 없던 이전과 달리 규약을 어긴 콜백은 살아 있는 남의 세션을 잡는다
- **reliable in-flight 33개 초과 시 영구 ACK 불가** — ACK 커버리지는 `ackRSeqNum` 1개 + 비트필드 32개 = 총 33개다(`PlayerSession.cpp:85`, `120-130`). `_pendingReliable`에 34개 이상이 동시에 쌓이면 가장 오래된 것은 클라이언트가 이미 받았어도 알릴 수단이 없어 **세션이 끝날 때까지 재전송된다**(수신 측은 `UpdateRRecvState()`에서 `diff > 32`로 버리므로 정확성은 유지, 대역폭만 낭비). 2026-08-18 #3에서 MAX_RETRY를 없애면서 유계였던 낭비가 무계가 됐으므로, `RegisterReliable()`이 in-flight 34장 진입 시 남기는 로그가 이 상태의 유일한 신호다. **reliable 브로드캐스트가 셋으로 늘었다** — `D2CDespawnPlayerObject`(이탈 1건당 1장), `D2CNotifySpawnObject`·`D2CSpawnPlayerObject`(런타임 스폰 1건당 1장). 킬 피드가 붙으면서 **사망 1건은 3장**(시신 스폰 + 킬 피드 + 플레이어 디스폰)이 됐고, 룸 전원이 죽어도 12장이라 아직 여유가 있지만 이 경로가 늘어나면 도달 가능해진다. 대량 통보는 개별 전송 대신 청킹으로 묶을 것. 근본 해결은 비트필드 64비트 확장 또는 송신 창 제한이며 별도 작업
- **끊김 감지는 `PlayerSession::_lastEchoTs` 하나에 달려 있다** — `MarkLeaving(DISCONNECTED)`의 호출부는 `GameRoom::DetectDisconnectedSessions()` 한 곳뿐이고, 그 판정은 클라이언트가 UDP 헤더에 되돌려주는 `timestampEcho`가 6초 이상 갱신되지 않는 것을 본다. 딸린 전제 셋 — ① **클라이언트가 0.1초 주기로 상태를 보낸다**(6초 = 연속 60회 유실이라는 근거가 여기서 나온다. 주기를 늘리면 안전 마진이 그만큼 깎인다) ② `UpdateRtt()`가 `_lastEchoTs`를 갱신하는 유일한 지점이고, 서명·시퀀스 검사를 통과한 패킷에서만 불리며 **역행하는 에코는 버린다**(재전송분이 낡은 값을 실어 오므로 — 아래 재전송 헤더 항목) ③ `echo == 0`이면 판정을 건너뛰므로, 클라이언트가 `timestampEcho`를 채우지 않으면 감지가 조용히 무력화된다(`OPTION:`으로 남김). 수신 시각 기반으로 바꾸려는 유혹이 있다면 `_clientAddr`이 갱신되지 않아 NAT 리바인딩 시 하향만 죽는 경우를 놓친다는 점을 볼 것
- **재전송 패킷의 헤더는 최초 송신 시점 값이다** — `CheckRetransmits()`가 보관 버퍼를 그대로 복사해 보내고(헤더를 고치려면 서명이 전체 패킷 해시라 재서명이 따라온다), 클라이언트도 미확인 reliable 페이로드를 풀링해 같은 방식으로 재전송하므로 **양방향**이다. 따라서 재전송분의 `timestamp`·`timestampEcho`·ACK 필드는 신뢰할 수 없다. 원본이 유실되면 재전송이 그 시퀀스의 첫 도착이라 중복 검사도 걸러주지 못하므로, 방어는 수신 측 **단조 갱신**이 전부다 — `UpdateRtt()`의 `_lastEchoTs`와 `SetLastRecvTimestamp()`의 `_lastRecvTimestamp` 두 곳이며, **되돌리면 끊김 오판 킥이 즉시 되살아난다**(2026-08-19 #3). 재스탬프+재서명은 보류했고 **재검토 조건은 클라이언트가 "중복 패킷이어도 헤더 필드는 반영한다"를 확정하는 시점**이다 — 그때는 중복 reliable을 헤더 처리 전에 버리는 조기 `return`(`ClientPacketHandler.h:186-190`)도 함께 볼 것. 지금은 그 `return`이 낡은 헤더를 막는 쪽으로 작동하지만, 재스탬프가 들어오면 반대로 유효한 갱신을 버리는 자리가 된다
- **응답 없는 세션의 재전송 큐는 이탈 처리로만 정리된다** — 개별 제거 경로는 `ProcessIncomingAck()` 뿐이고, `GameRoom::DetachPlayer()`의 `ClearPendingReliableExcept()`가 유일한 일괄 정리 지점이다. MAX_RETRY가 없어져 살아있는 세션에는 무한 재전송하므로, 큐의 상한을 정하는 것은 이제 **끊김 감지 6초**다. 감지 임계값을 늘리면 그만큼 죽은 세션의 큐가 오래 남아 `GetRetransmitCandidates()`가 매번 전체를 순회한다
- **브로드캐스트 페이로드 재직렬화는 의도적 보류** — N명에게 보낼 때 페이로드가 N번 직렬화된다. 세션마다 헤더·서명이 달라 `SendBuffer` 재사용이 불가능하지만 **페이로드 바이트 자체는 동일**하므로 1회 직렬화 + memcpy로 줄일 수 있다. 다만 4인/100ms 기준 룸당 절약분이 ~3μs, 250룸이어도 코어의 0.75% 수준이고 같은 경로의 `sendto` 비용이 이를 압도해 실익이 없다. `Make*FromPayload` 짝 함수가 패킷마다 늘어나 `pktId`/`reliable`이 어긋날 여지가 생기는 쪽이 손해. **재검토 조건: 룸 인원 10명 초과 / 틱 25~50ms로 상향 / 동시 룸 수 세 자릿수.** 변경 영향은 `ClientPacketHandler`와 브로드캐스트 두 함수 안에 갇혀 있다
- **reliable C2D 채널은 순서를 보장하지 않는다** — `PlayerSession::UpdateRRecvState()`(`PlayerSession.cpp:57-79`)는 최고 시퀀스 + 32비트 비트필드로 **중복만 거른다**. 늦게 도착한 낮은 시퀀스도 처음 보는 것이면 그대로 처리되고, reliable(rSeqNum)과 unreliable(uSeqNum)은 시퀀스 공간이 분리돼 두 채널 사이에는 순서 개념 자체가 없다. 따라서 **상태를 바꾸는 C2D 요청은 토글·증분이 아니라 절대 지정으로 설계할 것** — 재전송과 순서 역전에도 결과가 같아야 한다. 다른 조작과의 순서 역전은 `my_inventory_version` 같은 버전 가드로 막는다(`C2DRequestEquipItem`, `C2DRequestSwitchWeapon`이 쓰는 관용구). `PlayerInventory`의 변경 함수 11곳이 예외 없이 `_inventoryVersion`을 올리는 것이 이 가드의 전제다. **D2C 방향도 같다** — 서버가 같은 상태를 연달아 통보하면 클라이언트에 역순으로 도착해 낡은 값이 최종 상태로 남을 수 있다. 그래서 **상태 전체를 절대값으로 싣고**(증분·토글 금지) 클라이언트가 헤더 `rSeqNum`으로 낡은 통보를 버리게 한다. 이 규약이 필요한 통보는 현재 `D2CNotifyWeaponChanged` 하나이며 `.proto` 주석에 적어뒀다
- **사격 거부는 조용하다** — `Handle_C2D_RequestWeaponFire`의 `weapon_dbid` 불일치·`fire_sequence` 불일치·**탄약 부족** 셋 모두 deny 패킷 없이 `return false`다. 거부는 `BroadcastExcept`보다 앞이라 데미지뿐 아니라 남에게 가는 발사 이펙트도 함께 사라진다. 클라이언트에는 "총이 안 나감"으로만 보이므로 증상을 만나면 서버 로그의 `[Handle_C2D_RequestWeaponFire]` 줄을 먼저 볼 것
- **탄약 차감의 규약 둘** — ① **`_inventoryVersion`을 올리지 않는다.** 인벤토리를 바꾸므로 올려야 할 것처럼 보이지만, 발사 한 발마다 버전이 오르면 교전 중 클라이언트의 `my_inventory_version`이 상시 낡은 값이 되어 장착·무기 교체가 전부 `DENY_VERSION_MISMATCH`로 거부된다. 차감이 `PlayerInventory` 바깥에서 Mutable 게터로 슬롯을 직접 건드리는 것이 이 예외를 성립시키며, 클라이언트 잔탄과 서버 잔탄은 명시적 동기화(`SerializeFullInventory()`, 탄창 두 칸 포함) 전까지 어긋나는 것이 정상이다 — **어긋남을 버그로 보고 보정 로직을 넣지 말 것.** 서버가 최종 판정 권한을 갖는다 ② **잔량 0인 슬롯을 남기지 않는다.** `Slot::IsEmpty()`가 `quantity <= 0 && blueprintId == 0`이라 차감만 하면 "0발인데 비어 있지 않은" 슬롯이 생기고, `CorpseContainer::place()`가 그것을 채우려다 실패해 시신 컨테이너에 빈 칸을 남기거나 `UnloadMagazineToInventory()`가 0발 탄약 스택으로 인벤토리 칸을 점유한다. 차감 직후의 `Clear()`가 둘을 원천 차단하므로 **수량을 줄이는 코드를 새로 쓸 때마다 같은 처리를 붙일 것**
- **시신 컨테이너의 용량에는 여유가 0이다** — `PlayerInventory::INVENTORY_SLOT_COUNT`(25) + 장착·탄창 5칸 = 30이 `Container::DEFAULT_CONTAINER_VOLUME`(30)과 정확히 같다. 인벤토리 칸이나 장착 슬롯을 늘리면서 `DEFAULT_CONTAINER_VOLUME`을 함께 늘리지 않으면 `Container::PlaceItem()`이 `false`만 반환하고 초과분을 **조용히 버린다**(반환값을 보는 호출부가 없다). `CorpseContainer.h`의 `static_assert`가 이 경우를 컴파일 타임에 잡으므로 빌드가 깨지면 두 상수를 같이 볼 것
- **`GameRoom::Update()`를 override하면 마지막에 베이스를 부를 것** — 베이스가 `BroadcastPlayerStates()`를 담당하므로 빠뜨리면 그 룸에서 플레이어가 서로 움직이지 않는다(윈체스터에서 실제로 일어났던 증상). 먼저 부르면 그 틱의 파생 로직이 만든 변화가 다음 틱에야 나가 상태가 한 틱 밀린다. 이 누락을 구조적으로 막는 NVI(비가상 `Update()` + 순수 가상 훅)는 **의도적으로 채택하지 않았다** — `Update`가 둘로 보이는 쪽의 인지 비용이 더 크다는 판단이므로, 이 규약은 규율로 지킨다
- **`CombatObject`는 `_dynamicObjects`에만 둔다** — 회수 경로인 `GameRoom::DestroyDeadObject()`가 `_dynamicObjects`만 보므로, 정적으로 등록된 전투 오브젝트는 죽어도 회수되지 않고 화면에서만 사라진다. 입구는 `CanBeStaticObject()`가 막지만(`SpawnStaticObject()` 세 갈래 모두에서 호출) 우회하면 성립한다. AI를 서버에서 다루지 않기로 했으므로 이 자리에 올 수 있는 것은 파괴 가능한 오브젝트뿐이다
- **파괴 가능하면서 컨테이너인 오브젝트는 만들지 않는다** — 만들면 약탈 중이던 세션의 `_interactingContainerId`가 사라진 오브젝트를 가리킨다. 아래 「시신에는 TTL이 없다」에서 예고한 문제가 이 조합에서 실현되므로, 필요해지면 제거 시 해당 id를 보는 세션을 찾아 `-1`로 되돌리는 처리가 함께 와야 한다
- **비플레이어 사망 경로는 아직 한 번도 실행되지 않는다** — `CombatObject`의 파생이 `PlayerObject`뿐이라 `FindNonplayerObject()`가 전투 오브젝트를 찾는 일이 없다. 검증용 더미 클래스를 만들지 않기로 했으므로 첫 파괴 가능 오브젝트를 만드는 시점에 피격→사망→회수→통보 전체를 그 자리에서 디버그할 것. 그때 지켜야 할 불변식 둘 — ① 회수 지시는 `TakeDamage()` 안이 아니라 **호출부가** 한다(안에서 `delete`하면 호출부가 직후에 읽는 `GetCurrentHp()`가 use-after-free) ② `OnDeathResolved()`에 `_dynamicObjects`의 반복자를 넘기지 않는다(훅이 흔적을 스폰하면 rehash로 무효화)
- **시신에는 TTL이 없다** — 룸 소멸까지 유지되고 `~GameRoom()`이 회수한다. 나중에 만료를 붙이려면 두 가지가 따라온다 — ① 약탈 중이던 세션의 `_interactingContainerId`가 사라진 오브젝트를 가리키게 되므로 소멸 시 해당 세션을 찾아 `-1`로 되돌려야 한다 ② `TimerExecuter`에 취소 API가 없어 잔여 콜백이 objectId 재조회 규약(세션의 sessionId 재조회와 같은 형태)을 따라야 한다. 지금은 둘 다 성립하지 않아 문제 자체가 없다
- **귀환의 설계 의도 셋은 미비점이 아니다** — ① **자발적 취소 패킷이 없다.** 존을 벗어나는 것이 유일한 취소 수단이고 요청 재전송은 "진행 중"으로 무시된다. 필요해지면 별도 패킷으로 추가한다 ② **존 검사는 `RecallTick`(1초 간격)에서만 한다.** 매 상태 갱신마다 전 플레이어의 위치를 검사하면 실제로 귀환 중인 소수를 위해 룸 전체가 비용을 무는 구조가 되고, 자발적 취소로 존을 벗어나는 플레이어가 경계선에서 알짱거릴 이유가 없으므로 틱 사이의 이탈·복귀는 허용한다 ③ **`RECALL_RESULT_PLAYER_DEAD` 경로는 현재 도달 불가**지만 남겨둔다 — 사망 시 `DetachPlayer()`의 `EndRecall()`이 generation을 올려 다음 틱이 조용히 종료되기 때문이며, 추후 멀티스레드 환경에서는 실제로 발생할 여지가 있다
- **귀환 유효성 검사는 안티치트가 아님** — 검사에 쓰이는 `PlayerObject::position`은 `ApplyState()`에서 클라이언트가 보낸 좌표를 그대로 기록한 값이다(`PlayerObject.cpp:10-13`). 좌표 조작 클라이언트는 항상 통과한다. 실질적 차단에는 서버 측 이동 검증(속도/텔레포트 체크)이 별도로 필요

---
