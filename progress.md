# 진행 상황 정리 (2026-08-29 업데이트)


## 완료된 것들

### 네트워크 / 패킷
- [x] (2026-08-26 #6) 재장전 응답을 델타에서 전체 스냅샷으로 교체 — 응답에 어느 무기의 탄창인지가 없어 요청과 응답 사이에 무기가 바뀌면 반대쪽 슬롯에 써넣었다. `D2CFullInventorySync`를 통째로 중첩하는 형태로 바꿨고, 없어진 필드 3·5·6·7은 클라이언트에 구스펙 생성 코드가 나가 있어 `reserved`로 막았다 (`External_Protocol.proto`, `ClientPacketHandler.cpp`, `PlayerInventory.h/cpp`)
- [x] (2026-08-27 #0) `D2CFullInventorySync` 크기 검사를 두지 않기로 확정 — 최악이 834B로 더 빡빡한 제약인 수신 버퍼(1024B)의 85%라 검사가 방어할 구간이 없다. 근거와 재계산 시 놓치기 쉬운 둘은 「진행 고려사항」으로 옮겼고, 2026-08-26 #6의 정상 경로 실측도 함께 끝났다 (`progress.md`)
- [x] (2026-08-27 #1) 세션 경계 규칙 수립과 `.proto` 주석 정리 반영 — 서버 세션이 클라이언트 디렉터리를 **수정뿐 아니라 열람도 하지 않는다**는 규칙을 루트 `CLAUDE.md`에 세웠다(컨텍스트 오염 차단). 클라이언트 세션의 `.proto` 정리(651→391줄)로 끊긴 서버 문서의 참조 둘을 복구했다 (`CLAUDE.md`, `progress.md`)
- [x] (2026-08-27 #2) 컨테이너를 열지 않으면 인벤토리 정리가 통째로 막히던 것 수정 — 컨테이너 ID 검사가 맨 위에서 돌아 양쪽 id가 `0xFFFFFFFF`인 **순수 인벤토리 내 이동까지** 거부했다. 옆 `Handle_C2D_RequestEquipItem`처럼 한쪽이라도 센티널이 아닐 때만 검사하도록 옮겼고 본문은 그대로다 (`ClientPacketHandler.cpp`)
- [x] (2026-08-27 #3) 오브젝트 표시명 도입과 킬 로그 반영 — `virtual GetObjectName()`으로 타입당 고정 이름과 인스턴스별 이름을 갈라(비-플레이어 저장 비용 0B) 킬 통보 둘에 이름 필드를 순수 추가했다. 함께 `CorpseContainer` → `PlayerLootContainer`, `ObjectType::Corpse` → `PlayerLoot`로 정리했고 와이어 값 3은 그대로다 (`UnityGameObject.h`, `PlayerObject.h`, `PlayerLootContainer.h`, `GameRoom.h/cpp`, `ClientPacketHandler.cpp`, `External_Protocol.proto`)
- [x] (2026-08-29 #0) 재장전 연출 통보 경로 신설 — `C2DNotifyReloadSequence`(44)·`D2CNotifyReloadSequence`(45) 추가, `PKT_ID_MAX` 46. 남에게 "장전 중"만 보여주는 순수 연출 채널이라 서버가 단계 번호를 보관하지 않으며, 15=완료만 서버가 발행한다 — 거부된 재장전이 남의 화면에서 완료로 보이는 것을 막는다 (`External_Protocol.proto`, `enum.h`, `ClientPacketHandler.h/cpp`)
- [x] (2026-08-29 #1) 아이템 데이터 헤더를 `database/`에서 서버 트리로 복사하도록 빌드에 연결 — 이것으로 아이템 수치의 출처가 서버 헤더에서 **DB**로 뒤집혔다. 붙이는 과정에서 세 총기의 `spread_recovery_rate`가 이미 갈라져 있던 것(`database/` 800, `src/` 400)을 발견했고, 다음 빌드에서 800으로 바뀐다 (`CMakeLists.txt`)
- [x] (2026-08-29 #2) 평문 비밀번호 제거와 클라이언트 반영 완료분 `[작업사항]` 정리 — 공개 저장소에 올라가 있던 `generate_script.py`의 MySQL root 비밀번호를 지우고 DB 계정 비밀번호를 교체했으며, `.gitignore` + `git rm --cached`로 추적까지 끊었다(추적 중인 파일에는 ignore가 적용되지 않아 둘은 짝이다). `.proto` 상단 `[작업사항]` (3)(4)(5)를 삭제해 `D2CUpdatePlayerStates` 한 건만 남겼다 (`.gitignore`, `database/generate_script.py`, `External_Protocol.proto`)
- [x] (2026-08-29 #3) `progress.md` 완료 목록 압축 — 스킬의 완료 항목 규칙이 "전체 10개 상한 + 항목당 3문장 이하"로 바뀌어 기존 항목을 "무엇을 왜"만 남기도록 다시 썼다(완료 목록 29,870 → 4,468B). 잘라낸 것은 「진행 고려사항」이 이미 들고 있는 설계 근거·불변식뿐이며, 대응 항목이 없던 `(2026-08-26 #4)`의 down 마이그레이션 관례는 지우기 전에 그쪽으로 옮겼다. **남은 비대화 지점은 파일의 69%(44KB)를 차지하는 「진행 고려사항」 44개 항목이고 여기에는 아직 상한이 없다** (`progress.md`)
- [x] (2026-08-29 #4) 「진행 우선사항」 최신화 — 클라이언트 코드 실사 결과 미구현으로 적혀 있던 연동 목록 12건이 전부 구현돼 있어 목록에서 제거했고, `generate_script.py` 자격증명 항목은 해결 확인으로 종결했다. 우선사항에 남은 것은 래그돌·사망 애니메이션, 피격 방향 표시, 실환경 검증 체크리스트뿐이다. 지우기 전에 영속 규칙 둘(`[작업사항]` 블록 삭제 전 이관 단계, 생성기의 저장소 외부화)은 「진행 고려사항」으로 옮겼다 (`progress.md`)

---

## 진행 중 / 다음 할 것들

### 진행 우선사항
1. **사망 연출 — 남은 것은 캐릭터 자체의 연출뿐** — 서버 측(이탈 처리·시신 컨테이너·가해자 전달·킬 피드·사망 후 5초 세션 유예)은 완료됐고, 클라이언트도 `D2CNotifyPlayerKilled` 수신 → 킬 로그 표시 → 탑뷰 사망 카메라(`DeathCameraController`, 2초 보간) → 매치 이탈까지 붙어 있다. 클라이언트의 씬 정리 타이머는 `MATCH_EXIT_DELAY = 4f`로 서버 유예 5초보다 짧게 잡아 하향 트래픽이 끊기기 전에 나간다. 남은 것은 피격·사망 시 캐릭터의 래그돌·애니메이션 트리거(`PlayerController.ProcessHit`이 TODO 스텁)이며 전부 클라이언트 몫이다 — 서버는 `OnDeathResolved()` 훅을 열어둔 것으로 완료이고, 비플레이어 오브젝트의 연출은 첫 파괴 가능 오브젝트가 생길 때 그 훅 안에서 각자 처리한다
2. **클라이언트 연동 — 구현 확인 완료, 실환경 검증과 부분구현 둘만 남았다** — 2026-08-29 클라이언트 코드 실사 결과, 이 항목이 들고 있던 미구현 목록(스폰 통보 36·선제 `D2CSpawnPlayerObject` 13, 무기 교체 37/38, 재장전 42/43, 재장전 연출 44/45, 킬 피드 40, 오브젝트 킬·디스폰 41/39, 헤더 `timestampEcho` 에코, 탄약 차감, 장착 시 손 슬롯 이동 규칙, 자기 실드 재생 예측, 사망 유예 처리)은 **전부 구현돼 있어 목록에서 제거했다**. 남은 것 둘 —
    - **피격 방향 표시(damage indicator) 미구현** — `D2CNotifyHealthChange.attacker_object_id`의 수신·가해자 추적(5초 기한)까지는 구현됐으나 표시 UI·이펙트가 없다. 피격 리액션을 담당할 `PlayerController.ProcessHit`이 TODO 스텁이다 (위 사망 연출 항목과 같은 자리다)
    - **실환경 스모크 테스트** — 서버 로그에 `[DROP] 서명 불일치`·`[DROP] unreliable 시퀀스 중복`이 찍히는지, StaticObjects 역직렬화가 `TransformInfo` 구조 변경을 반영하는지, `TestGameRoom::InitTestGameRoom()`의 TestItemBox가 Blueprint 응답(StaticObjects 청크)에 포함되는지 확인
3. **`POST /api/session/resume` 실호출 검증** — 서버 구현과 클라이언트 연동(`LobbyScene`이 결과 씬 경유 복귀 시 `PostResumeSessionCall` 호출)은 끝났고, Redis·MySQL이 붙은 환경에서의 확인만 남았다. ① `/api/login` 후 그 sessionId로 호출 시 200 + `uid`/`money`/`inventory`가 DB 값과 일치하는지 ② 호출 직후 `TTL sess:<UUID>`가 3600 근처로 재충전됐는지(미들웨어 슬라이딩 갱신 확인) ③ 삭제·만료된 sessionId로 호출 시 401 ④ `/api/guest` 세션으로 호출 시 200 + `money: 0, inventory: []`. **응답 필드를 바꾸려면 루트 세션에서 양쪽을 함께 고쳐야 한다** — `Extraction/Assets/Scripts/Network/http-api-spec.yaml`이 `HTTPServer/http-api-spec.yaml`의 사본이라 명세도 짝으로 움직인다
4. **윈체스터 룸의 고유 게임 루프 로직** — 상태 브로드캐스트는 `GameRoom::Update()`가 모든 룸에 대해 처리하므로 `WinchesterGameRoom`은 현재 `Update()`를 override하지 않는다. 맵 고유의 주기 처리(이벤트 등)가 정해지면 `Update() override`를 새로 쓰면서 **파생 로직을 먼저 하고 마지막에 `GameRoom::Update()`를 부를 것**. AI는 서버에서 다루지 않기로 했다 (`GameRoom.h/cpp`)

### 진행 고려사항
- **`TODO:` / `TEMP:` / `OPTION:` 주석 구분** — `TODO:`는 아직 구현되지 않은 것, `TEMP:`는 테스트를 위해 의도적으로 값을 제한하거나 코드를 막아둔 것, `OPTION:`은 없어도 동작에 문제가 없고 여유가 생기면 구현할 개선 항목이다. 릴리스를 막는 것은 `TEMP:`뿐이므로 릴리스 전 되돌려야 할 항목은 `grep -rn "TEMP" src HTTPServer`로 전수 확인할 것 (현재 8곳). `OPTION:` 항목은 할 일 목록에 올리지 않고 주석으로만 남긴다. **이 전수 확인은 마커를 빠뜨리지 않았다는 전제 위에 있다** — 2026-08-25 #0에서 되살린 탄약 차감이 `TEMP:` 없이 "테스트를 위해 임시 비활성화"라고만 적힌 채 주석 처리돼 있어 집계에서 빠져 있었다. 코드를 주석으로 막을 때는 문구가 아니라 마커를 먼저 붙일 것
- **코드 주석은 이 세 종류와 상수 유래뿐** — 흐름 서술("~를 전송한다"), 단계 나열, 구획 배너, 설계 배경은 코드에 쓰지 않는다. 코드만 봐서는 알 수 없는 제약·불변식·결정 배경은 **이 문서의 「진행 고려사항」에 적는다** (2026-08-13 #7·#8에서 코드베이스 전체 466줄을 이 기준으로 정리). 코드에 남기는 예외는 숫자만 남는 상수의 유래(슬롯 범위, 페이로드 한계, 프로토콜 반환 코드 등)와 바이트 레이아웃. `src/sample.h`만 예외로 남겨뒀다 — 학습 노트 성격이라 정리 대상으로 본다면 파일째 삭제가 맞다
- **`active_match` 락은 네 곳이 한 묶음** — `match.js`의 `/start` EX 값, `matchCancel` Lua의 `return 2`/`return 3` 분기, `DBProxyRequest.h`의 `ACTIVE_MATCH_TTL_SEC`, `redis_keys.md` 2번 절이 서로를 전제한다. TTL(3600초)은 **백스톱일 뿐**이고 정상 해제는 룸 분리(`GameRoom::DetachPlayer()`) 한 곳이다. 세 가지 불변식을 깨지 말 것 — ① TTL은 최대 게임 길이보다 길어야 한다(짧으면 게임 도중 락이 풀린다) ② `match.js`의 EX와 `ACTIVE_MATCH_TTL_SEC`는 같은 값이어야 한다 ③ `INGAME:` 접두어는 `UpdateEntryTokenRequest`가 쓰고 `matchCancel`이 읽는다. 티켓 TTL(300초)이 락 TTL보다 짧아 생기는 "티켓 없고 락만 남은" 구간을 이 접두어로만 구분할 수 있다
- **이탈 통보는 `DetachPlayer()`에서 한 번만 나간다** — `D2MNotifyPlayerLeft`가 Main의 인벤토리 DB 반영과 `active_match` 락 해제를 모두 촉발하므로, 통보 시점이 곧 "게임 결과가 확정되는 시점"이다. 그래서 세션 종료(`FinalizeLeave()`)가 아니라 **룸에서 분리되는 시점**에 보낸다 — 사망 유예 5초를 기다리면 그동안 락이 잡혀 있어 로비로 먼저 돌아간 클라이언트가 재매칭에서 409를 맞는다. 중복 방지는 `PlayerSession::NotifyLeftOnce()`의 `_leftNotified` 하나이고 `FinalizeLeave()`의 호출은 분리를 건너뛴 세션용 백스톱이다. **통보 시점을 옮기려는 변경은 페이로드 동결 조건을 먼저 볼 것** — 지금 안전한 이유는 분리 시점부터 모든 상향 요청이 차단돼 인벤토리가 변할 수 없어서다
- **`SessionState::SPECTATING`은 "하향만 받고 상향은 전부 버린다"** — 사망 유예 5초 동안의 상태이며, 조작 권한(`IsActiveState()`)과 브로드캐스트 수신 자격(`CanReceiveBroadcast()`)을 갈라놓은 것이 이 상태의 존재 이유다. **차단은 `IsActiveState()`에 이 상태가 없다는 사실 하나에 전적으로 의존한다** — 새 C2D 핸들러를 추가할 때 그 게이트를 빠뜨리면 죽은 플레이어가 자기 시신을 털거나 사격할 수 있게 되고, 컴파일도 통과하고 정상 플레이에서도 드러나지 않는다. 의도적 예외는 `Handle_C2D_HeartBeat` 하나뿐이며(막으면 클라이언트가 접속 끊김으로 판단해 유예의 목적이 깨진다) 상태를 바꾸지 않는 요청에만 이 예외를 허용할 것. 유예를 끝내는 것은 `FinalizeLeave()`가 `LEFT`로 되돌리는 한 줄이다
- **MySQL과 Redis 사이에는 공유 트랜잭션이 없다** — 이탈 반영은 **MySQL 먼저, 락 해제 나중**(fail-closed) 순서에 의존한다. 락을 먼저 풀면 DB 반영 실패 시 유저가 반영 안 된 인벤토리로 새 매치를 시작한다. 반대로 실패 시 락을 계속 잡으면 영구 잠금이 되므로, 재시도 2회 후에는 락을 풀고 페이로드를 error 로그로 남긴다. 이 순서를 바꾸는 변경은 `NotifyPlayerLeftRequest::Execute()` 안에 갇혀 있다
- **매치 종료 시 세션 TTL 재충전은 하지 않기로 확정** — TTL이 갱신되지 않는 구간은 매치 전체가 아니라 **마지막 HTTP 호출(`/api/game/match/start`)부터 복귀 후 `/api/session/resume`까지**다. 매치 중에는 클라이언트가 Dedicate와 UDP로만 통신해 `requireAuth`의 슬라이딩 갱신이 걸리지 않지만, 정상 매치가 길어야 15분이라 TTL 3600초와는 자릿수가 다르다. 초과하더라도 실패 모드가 **401 → 일반 로그인 폴백**이라 인벤토리가 유실되지 않는다(결과는 사망·귀환 시점에 이미 MySQL에 박혀 있다). 붙인다면 자리는 `NotifyPlayerLeftRequest::Execute()`지만 그 경로는 `db_id`만 알고 있어 `user_sess:<userId>` 키에 닿으려면 **db_id → user_id 역참조 수단을 새로 만들어야 한다**(매치 시작 시 sessionId를 Redis에 함께 기록하는 등). 재검토 조건은 결과 화면 체류가 길어지는 UI 변경이나 세션 TTL을 크게 줄이는 결정이다
- **게스트는 MySQL에 행이 없고 그것을 FK가 강제한다** — `/api/guest`는 `guest_uid_counter`를 `DECR`해 **음수 db_id**를 발급하고 `users` 행은 만들지 않는다. `user_inventory.uid`의 `users(uid)` FK가 음수 uid의 INSERT를 거부하므로(`schema.sql:28`) 게스트의 재화·인벤토리는 어느 경로로도 영속되지 않는다 — 이탈 반영이 `_uid < 0`을 먼저 건너뛰는 것도(`DBProxyRequest.cpp:188`) 같은 이유다. 따라서 게스트에게 인벤토리를 돌려주는 API는 **빈 배열이 오답이 아니라 정답**이고, 세션 데이터만 보고 조회를 거는 새 엔드포인트는 `user_type`으로 먼저 갈라야 한다. 게스트에게 영속 데이터를 주려는 요구가 생기면 그 지점은 API가 아니라 FK이며, 정식 계정 전환을 설계해야 한다
- **스키마의 출처는 `database/` 이고 `HTTPServer/database/schema.sql` 과의 중복은 감수한다** — `database/`는 루트에 따로 서서 나머지 디렉터리와 **별개로 관리하기로 확정**됐다. 두 곳에 같은 테이블 정의가 있는 것을 알고 있으므로 **통합·삭제를 제안하지 말 것.** 다만 값이 갈리면 조용히 틀어진다 — 2026-08-26 #3의 `aggression_level`(5 vs 7)이 그 사례이고, 실 DB에서 뽑은 `database/current_schema.sql`이 언제나 사실이다. **시드 데이터를 마이그레이션에 넣지 않기로 확정** — 저장소가 공개돼 있어 **기존 DB가 어떤 정보를 품고 있는지까지 알려줄 이유가 없다**는 판단이다. 스키마와 데이터는 노출 성격이 다르고 마이그레이션은 스키마 도구다. **시드 마이그레이션을 추가하자는 제안을 하지 말 것.** 딸려오는 제약 — 빈 DB에 마이그레이션만 돌리면 환경이 완성되지 않는다. `items` 7행이 없으면 `RedisHandler.cpp:20`의 아이템 캐시와 `shopCache.js:7`의 샵이 비고 `match.js:198`의 로드아웃 `item_type` 조회가 깨진다. `weapon_specs`가 비면 스프레드 조정 마이그레이션이 조용한 no-op이 된다. **새 환경은 실 DB 덤프로 채운다** — 저장소 밖의 절차다. 지금 드러나지 않는 것은 로컬과 공개가 같은 DB를 쓰고 있어서다
- **`sql::Connection`은 자동 재연결이 없다** — `sw::redis::Redis`와 달리 커넥션 풀도 재연결도 없어, `wait_timeout`(기본 8시간)이나 HeatWave 페일오버로 끊기면 이후 모든 사용이 예외를 던진다. **MySQL을 쓰는 코드는 반드시 `pMysql->Get()`으로 핸들을 받을 것** — 그 안에서 `isValid()`→`reconnect()`가 처리된다. `sql::Connection*`를 어딘가에 캐시하면 이 보호가 무력화된다
- **`damageReductionRate`는 실드 잔량이 있을 때만 유효하다** — 실드가 0으로 소진되면 `_currentShield -= damage`가 즉시 음수가 되어 penetrated 경로를 타고 감소율이 적용되지 않는다. 이것이 **의도된 설계로 확정**됐다(2026-08-18 #2). 즉 armor는 "상시 방어력"이 아니라 "실드가 버티는 동안의 감쇄"이므로, 밸런싱에서 `damageReductionRate`를 상시 적용으로 읽지 말 것 (`CombatObject.h`)
- **남의 HP·실드 수치와 방어구 정보는 클라이언트에 보내지 않는다** — 매 틱 브로드캐스트되는 `PlayerState`에는 이동·조준만 있고, `D2CNotifyHealthChange`(hp·shield 절대값)는 `pHitSession->Send()`로 **피격 당사자에게만** 간다. 적 체력바는 원천적으로 불가능하며 이는 의도된 결과다. 방어구도 같은 이유로 뺐다(2026-08-19 #0) — **외형에 드러나지 않고**, `blueprint_id` 하나가 `ArmorSpec`(최대 실드·피해 감소율·재생량) 전체와 동치라 id만 흘려도 곧 수치 노출이 된다. 서버가 안 보내면 클라이언트를 뜯어도 못 얻으므로 치트 표면적도 함께 줄어든다. **새 D2C 패킷에 남의 hp·실드·방어구를 싣지 말 것.** 무기(`weapon_id`)는 외형에 보이므로 예외이고, 히트마커처럼 "맞았다/못 맞았다" bool만 돌려주는 것은 이 원칙과 충돌하지 않는다
- **방어구 착용 시 실드가 0인 것은 의도된 설계다** — `SetShield()`가 `_currentShield = 0`으로 초기화하는 것을 버그로 보지 말 것. 착용 시 만충이나 이전 실드 비율 유지로 바꾸면 인벤토리에 방어구를 쟁여두고 실드가 0이 될 때마다 스왑해 **실드 파괴 리스크를 지지 않는 플레이**가 가능해진다. 만충으로 시작하는 유일한 예외는 스폰 시점의 `ChargeShield(GetMaxShield())`이고, 그 뒤로 채우는 수단은 `RegenShield()` 하나뿐이다. **피격 후 재생 지연은 없다** — `ArmorSpec`에 지연 필드가 없어 현재 데이터 모델이 표현할 수 있는 것이 연속 재생뿐이라서다. 필요해지면 `ArmorSpec` 필드 하나와 `_lastDamagedMs` 추가로 열린다(`TakeDamage()`가 매 피격에 불리므로 기록할 자리는 이미 있다)
- **아이템 수치의 출처는 DB이고 두 헤더는 생성 산출물이다** — 2026-08-29 #1에서 뒤집혔다. `database/generate_script.py`가 `items`·`weapon_specs`·`armor_specs`를 읽어 `database/ItemDataManager.h`(C++)와 `database/ItemDBHelper.cs`(C#)를 생성하고, 서버 헤더는 CMake의 `SyncRuntimeFiles`가 빌드마다 `src/DedicateProcess/ItemDataManager.h`로 복사한다. **따라서 수치 변경의 순서는 마이그레이션 → `generate_script.py` → 빌드다.** **기획 데이터 마이그레이션의 down에는 역방향 SQL을 두지 않는다**(2026-08-26 #4에서 확정) — 되돌릴 수치를 down에 적으면 그 값이 사본 하나로 더 늘어나 위의 갈라짐을 한 곳 더 만든다. 롤백은 새 마이그레이션으로 처리하고 down에는 그 취지를 주석으로만 남길 것. 어느 헤더도 직접 고치지 말 것 — 서버 쪽은 다음 빌드에 조용히 되돌아가고, `database/` 쪽은 다음 생성에 덮인다. **자동화가 닿지 않는 구간이 둘 남아 있다** — ① `generate_script.py` 실행은 수동이라 마이그레이션만 돌리면 헤더가 낡은 채로 남는다 ② `ItemDBHelper.cs`를 클라이언트로 옮기는 경로가 이 저장소에 없다. **이 구간에서 실제로 갈라진 적이 있다** — 마이그레이션 `20260828212325`이 `spread_recovery_rate`를 800으로 올렸는데 서버 트리 사본은 400에 머물러 있었고, 2026-08-29 #1의 복사 규칙이 붙어서야 드러났다. 별개로 최대 HP도 `PlayerObject::DEFAULT_MAX_HP` ↔ `IngameScene.MAX_HEALTH_POINT` 짝이다. **어긋나도 아무것도 깨지지 않는다** — 최대 HP는 어떤 패킷에도 실리지 않고 스펙 테이블은 id만 오가므로, 컴파일도 통신도 전부 정상인 채 체력바 비율이나 반동·잔탄 표시만 조용히 틀어진다(아래 `enum.h` ↔ `.proto` PktId와 같은 종류의 침묵 실패다). 전투 수치가 전부 100배 스케일이라는 규칙은 DB 값이 들고 있고 생성기가 그대로 옮긴다. **최대 HP 짝만은 생성 경로 밖이라 여전히 손으로 맞춰야 한다**. **`generate_script.py` 자체가 2026-08-29 #2에서 `.gitignore` 대상이 되어 저장소 밖 파일이다** — 새 환경은 실 DB 덤프처럼 이 스크립트도 저장소 밖에서 받아와야 하며, 스크립트 안의 접속 정보는 자리 표시 문자열이라 로컬 환경에 맞게 채워야 실행된다
- **가해자 id 필드는 예외 없이 명시적으로 채울 것** — `D2CNotifyHealthChange.attacker_object_id`와 `D2CNotifyPlayerKilled.killer_object_id`의 "없음"은 `0xFFFFFFFF`(`CombatObject::NO_ATTACKER`)다. **proto3 기본값 0은 실재하는 object_id**이므로(`GameRoom::_nxtObjectId`가 0부터 발급) 설정을 빠뜨리면 0번 오브젝트에 오귀속된다. 지금은 가해자 없는 피해 경로가 없어 드러나지 않지만, 회복(`REASON_ITEM_HEAL`)·낙하·지역 피해가 붙는 시점에 걸린다. **이름 쪽은 반대로 빈 문자열 하나로 통일했다**(2026-08-27 #3) — `killer_object_name`은 가해자 부재와 서버 조회 실패를 구분하지 않는다. 서버 내부 사정을 클라이언트에 알릴 이유가 없고 `killer_object_id`가 이미 판별자라서다. **조회 실패는 정상 플레이에서 실제로 발생한다** — 사망 유예 5초 사이에 가해자가 방을 뜨면 `_playerObjects`에서 이미 빠져 있다
- **표시명은 `UnityGameObject::GetObjectName()` 하나에서 나오고, 플레이어의 이름은 로그인 userId다** — 2026-08-27 #3에서 확정. **userId 노출은 은닉 방침의 예외가 아니라 의도된 설계다** — 총격음과 킬 로그가 맞물려 플레이어에게 선택지를 만들어 주는 것이 목적이므로, 별도 닉네임을 도입하자는 제안을 하지 말 것. 구조는 **타입당 고정 이름과 인스턴스별 이름의 이원화**다 — 베이스가 `ObjectTypeToName(objectType)`을 반환하고(비-플레이어 오브젝트는 이름 저장 비용이 **0B**다), 인스턴스마다 이름이 다른 것만 override한다(현재 `PlayerObject` 하나). **새 오브젝트 타입을 추가하면 `ObjectType` 열거값과 `ObjectTypeToName()`의 case가 짝이다** — 빠뜨리면 `default:`가 `OBJECT_NAME_NONE`("None")을 돌려주므로 컴파일도 통신도 정상인 채 이름만 조용히 틀어진다(위 `enum.h` ↔ `.proto` PktId와 같은 종류의 침묵 실패다). **헤더의 이름 상수는 예외 없이 `inline`으로 둘 것** — 네임스페이스 스코프의 `const`는 기본이 internal linkage라 TU마다 사본이 생기고 주소 비교가 어긋난다. **`objectName`을 데이터 멤버(값이든 포인터든)로 되돌리려는 변경은 두 가지를 먼저 볼 것** — 값 멤버는 비-플레이어 오브젝트마다 32B를 물리고, 포인터는 `PlayerObject`가 자기 멤버를 가리키게 되어 베이스가 먼저 초기화되는 순서 문제와 복사·이동 시 dangling을 함께 들여온다
- **통보 대상·시점·보장 범위를 바꾸면 `.proto`의 `[작업사항]` 주석을 함께 훑을 것** — 두 번 연속 어긋났다. 2026-08-21 #0에서는 무기 교체 판정 규칙이 코드와 **정반대**로 적혀 있었고(그대로 구현하면 모든 성공이 실패로 오판된다), #1의 사망 유예는 기존 주석 넷을 한 번에 거짓으로 만들었다(#2에서 정정). 원인은 같다 — **이 주석은 서버 빌드에 아무 영향이 없어 틀려도 아무것도 깨지지 않고**, 깨지는 자리가 클라이언트 세션이라 발견이 늦다. 특히 위험한 부류는 필드가 그대로인데 **수신 대상이나 의미만 바뀌는 변경**이다(브로드캐스트 필터 교체, 통보 시점 이동, ACK가 보장하던 것의 축소). 구현 diff에 `.proto`가 없으면 주석을 안 봤다는 뜻으로 읽을 것. **`[작업사항]` 블록은 현재 `D2CUpdatePlayerStates` 한 건만 남았고, 블록 삭제에는 한 단계가 있다** — 그 안의 "계속 지켜야 할 규칙"(에코 규약, `max` 반영, 낡은 응답 폐기 같은 영속 계약)을 해당 저장소의 CLAUDE.md로 **먼저 옮긴 뒤** 지울 것. 일회성 구현 지시와 영속 계약이 한 블록에 섞여 있어 통째로 지우면 후자가 딸려 나간다
- **패킷을 추가하면 `enum.h`와 `.proto`의 `enum PktId`가 짝이다** — 서버는 `enum.h`의 `PKT_ID_*`만 쓰고 `.proto`의 `PktId`를 참조하지 않으므로, `.proto` 쪽을 빠뜨려도 **서버는 정상 빌드되고 정상 동작한다**. 대신 생성 코드를 쓰는 클라이언트가 그 패킷의 id 상수를 얻지 못해 분기 자체를 만들 수 없다(2026-08-19 #2에서 다섯 개가 이 상태였다). `PKT_ID_MAX`도 함께 올릴 것 — 핸들러 배열 크기이자 수신 시 범위 검사 기준이라 빠뜨리면 새 C2D 패킷이 조용히 버려진다. 통합하지 않은 이유는 서버가 `.proto` PktId를 참조하면 배열 크기 관용구가 protobuf 헤더에 묶이기 때문이며, 위험 구간이 "패킷 추가" 한 시점뿐이라 규율로 지킨다. **다만 그 규율이 두 번 연속 깨졌다** — 2026-08-19 #2(다섯 개 누락)에 이어 2026-08-23 #0에서도 message·`enum.h`·팩토리·발신부를 다 맞추고 `enum PktId` 한 줄만 빠뜨렸다. 나머지가 전부 맞으면 "다 했다"는 착각이 들고 빌드도 통과하므로, **패킷 추가 시 `enum PktId` 확인을 마지막 단계로 따로 둘 것**
- **IPC 프로토콜 파일을 추가하면 `CMakeLists.txt` 두 곳을 같이 고칠 것** — `PROJECT_SOURCES`의 `.pb.cc`/`.pb.h` 목록과 `IPC_PB_NAMES`. 한쪽만 고치면 컴파일 대상에서 빠지거나 `Protocol/Compiled/IPC/` → `src/IPCProtocol/` 복사가 누락된다. External 프로토콜은 protoc가 `src/` 아래로 직접 생성하므로 `PROJECT_SOURCES`만 보면 된다
- **`_allocatedPlayers`의 증감은 두 곳뿐이고 짝이 맞는다** — 증가는 `M2DSession::AllocatePlayers()`의 `+= ticketVec.size()`, 감소는 `ReleasePlayers()`가 받는 `player_count`(= 룸의 `_playerSessions.size()`)뿐이다. 둘이 같다는 보장은 코드에 드러나지 않으므로 근거를 남긴다 — ① `MatchMaker::VerifyAndSetMatchStatus()`의 Lua가 그룹 전원의 티켓 해시 존재와 `WAITING` 상태를 원자적으로 확인한 뒤 `INPROGRESS`로 바꾸고, 1을 반환했을 때만 `DistributePlayerGroup()`이 호출된다 ② Main은 단일 스레드이고 그 검증부터 `MakeM2DMakeRoomForThisGroup()`의 `hgetall`까지 연속 호출이라 중간에 io_uring CQ도 다른 핸들러도 끼어들지 못한다(가용 세션이 없을 때의 `fork`+`execl`만 들어오며 Redis를 건드리지 않는다) ③ 유일한 외부 삭제자인 Node `matchCancel` Lua는 `WAITING`일 때만 `DEL`하므로 `INPROGRESS` 티켓을 지우지 못한다. 따라서 `MakeM2DMakeRoomForThisGroup()`의 `if (val.empty()) continue`는 정상 동작에서 도달하지 않고 `player_infos` 크기는 `ticketVec.size()`와 같다. **이 세 조건 중 하나라도 바꾸면 카운트가 어긋나 프로세스가 다시 누적 증가한다.** 남는 이론적 구멍은 티켓 TTL(300초) 만료가 ①과 ② 사이 마이크로초에 걸리는 경우와 Redis eviction뿐이라 대응하지 않기로 했다
- **전용 프로세스는 룸이 비어도 종료하지 않는다** — 서버 가동 중 관측된 최대 수용 인원만큼의 프로세스를 유지하고, 이후 인원이 줄어도 회수하지 않는다는 결정(fork·exec과 초기화 비용 회피). 이에 따라 `GameRoom::CheckAllLeft()`에 있던 "남은 룸이 없을 때의 프로세스 정리" TODO를 제거했다. 딸려오는 것 둘 — ① 이 결정의 전제(프로세스 수가 최대 동시 인원에 수렴한다)는 `_allocatedPlayers` 감소 경로가 붙어서야 성립한다 (2026-08-14 #1에서 구현 완료) ② 유휴 프로세스도 `DedicateMain`의 1ms 슬립 루프 때문에 초당 1000회 깨어난다. 수십 개가 되면 폴링만으로 상시 CPU를 먹으므로 룸 0개일 때의 백오프(1ms→50ms)가 필요해지지만, **예상 프로세스 수가 2개 규모라 지금은 보류**한다
- **지연 콜백은 예외 없이 sessionId 재조회 규약을 따를 것** — `TimerExecuter`에 취소 API가 없어 룸/세션이 정리된 뒤에도 잔여 콜백이 실행될 수 있다. 콜백이 `PlayerSession*`를 캡처하지 않고 sessionId로 매번 재조회하며 uid·generation을 검증하면(현재 `RecallTick()`이 이 방식), 정리 시 `_players[id] = nullptr`만으로 잔여 콜백이 스스로 포기하므로 별도의 정지 확인(quiescence) 로직이 필요 없다. **한 곳이라도 raw 포인터를 캡처하면 이 설계가 무너진다.** 룸 소멸로 sessionId 반납이 실제로 시작됐으므로 이 규약은 이제 선택이 아니다 — `_freePlayerIds`가 FIFO라 반납된 id는 앞선 것들이 모두 소진된 뒤에야 재사용되어 간격 자체는 넉넉하지만, 재사용이 아예 없던 이전과 달리 규약을 어긴 콜백은 살아 있는 남의 세션을 잡는다
- **reliable in-flight 33개 초과 시 영구 ACK 불가** — ACK 커버리지는 `ackRSeqNum` 1개 + 비트필드 32개 = 총 33개다(`PlayerSession.cpp:85`, `120-130`). `_pendingReliable`에 34개 이상이 동시에 쌓이면 가장 오래된 것은 클라이언트가 이미 받았어도 알릴 수단이 없어 **세션이 끝날 때까지 재전송된다**(수신 측은 `UpdateRRecvState()`에서 `diff > 32`로 버리므로 정확성은 유지, 대역폭만 낭비). 2026-08-18 #3에서 MAX_RETRY를 없애면서 유계였던 낭비가 무계가 됐으므로, `RegisterReliable()`이 in-flight 34장 진입 시 남기는 로그가 이 상태의 유일한 신호다. **reliable 브로드캐스트가 셋으로 늘었다** — `D2CDespawnPlayerObject`(이탈 1건당 1장), `D2CNotifySpawnObject`·`D2CSpawnPlayerObject`(런타임 스폰 1건당 1장). 킬 피드가 붙으면서 **사망 1건은 3장**(시신 스폰 + 킬 피드 + 플레이어 디스폰)이 됐고, 룸 전원이 죽어도 12장이라 아직 여유가 있지만 이 경로가 늘어나면 도달 가능해진다. 대량 통보는 개별 전송 대신 청킹으로 묶을 것. 근본 해결은 비트필드 64비트 확장 또는 송신 창 제한이며 별도 작업
- **끊김 감지는 `PlayerSession::_lastEchoTs` 하나에 달려 있다** — `MarkLeaving(DISCONNECTED)`의 호출부는 `GameRoom::DetectDisconnectedSessions()` 한 곳뿐이고, 그 판정은 클라이언트가 UDP 헤더에 되돌려주는 `timestampEcho`가 6초 이상 갱신되지 않는 것을 본다. 딸린 전제 셋 — ① **클라이언트가 0.1초 주기로 상태를 보낸다**(6초 = 연속 60회 유실이라는 근거가 여기서 나온다. 주기를 늘리면 안전 마진이 그만큼 깎인다) ② `UpdateRtt()`가 `_lastEchoTs`를 갱신하는 유일한 지점이고, 서명·시퀀스 검사를 통과한 패킷에서만 불리며 **역행하는 에코는 버린다**(재전송분이 낡은 값을 실어 오므로 — 아래 재전송 헤더 항목) ③ `echo == 0`이면 판정을 건너뛰므로, 클라이언트가 `timestampEcho`를 채우지 않으면 감지가 조용히 무력화된다(`OPTION:`으로 남김). 수신 시각 기반으로 바꾸려는 유혹이 있다면 `_clientAddr`이 갱신되지 않아 NAT 리바인딩 시 하향만 죽는 경우를 놓친다는 점을 볼 것
- **재전송 패킷의 헤더는 최초 송신 시점 값이다** — `CheckRetransmits()`가 보관 버퍼를 그대로 복사해 보내고(헤더를 고치려면 서명이 전체 패킷 해시라 재서명이 따라온다), 클라이언트도 미확인 reliable 페이로드를 풀링해 같은 방식으로 재전송하므로 **양방향**이다. 따라서 재전송분의 `timestamp`·`timestampEcho`·ACK 필드는 신뢰할 수 없다. 원본이 유실되면 재전송이 그 시퀀스의 첫 도착이라 중복 검사도 걸러주지 못하므로, 방어는 수신 측 **단조 갱신**이 전부다 — `UpdateRtt()`의 `_lastEchoTs`와 `SetLastRecvTimestamp()`의 `_lastRecvTimestamp` 두 곳이며, **되돌리면 끊김 오판 킥이 즉시 되살아난다**(2026-08-19 #3). 재스탬프+재서명은 보류했고 **재검토 조건은 클라이언트가 "중복 패킷이어도 헤더 필드는 반영한다"를 확정하는 시점**이다 — 그때는 중복 reliable을 헤더 처리 전에 버리는 조기 `return`(`ClientPacketHandler.h:186-190`)도 함께 볼 것. 지금은 그 `return`이 낡은 헤더를 막는 쪽으로 작동하지만, 재스탬프가 들어오면 반대로 유효한 갱신을 버리는 자리가 된다
- **응답 없는 세션의 재전송 큐는 이탈 처리로만 정리된다** — 개별 제거 경로는 `ProcessIncomingAck()` 뿐이고, `GameRoom::DetachPlayer()`의 `ClearPendingReliableExcept()`가 유일한 일괄 정리 지점이다. MAX_RETRY가 없어져 살아있는 세션에는 무한 재전송하므로, 큐의 상한을 정하는 것은 이제 **끊김 감지 6초**다. 감지 임계값을 늘리면 그만큼 죽은 세션의 큐가 오래 남아 `GetRetransmitCandidates()`가 매번 전체를 순회한다
- **브로드캐스트 페이로드 재직렬화는 의도적 보류** — N명에게 보낼 때 페이로드가 N번 직렬화된다. 세션마다 헤더·서명이 달라 `SendBuffer` 재사용이 불가능하지만 **페이로드 바이트 자체는 동일**하므로 1회 직렬화 + memcpy로 줄일 수 있다. 다만 4인/100ms 기준 룸당 절약분이 ~3μs, 250룸이어도 코어의 0.75% 수준이고 같은 경로의 `sendto` 비용이 이를 압도해 실익이 없다. `Make*FromPayload` 짝 함수가 패킷마다 늘어나 `pktId`/`reliable`이 어긋날 여지가 생기는 쪽이 손해. **재검토 조건: 룸 인원 10명 초과 / 틱 25~50ms로 상향 / 동시 룸 수 세 자릿수.** 변경 영향은 `ClientPacketHandler`와 브로드캐스트 두 함수 안에 갇혀 있다
- **`uSeqNum`은 모든 unreliable 패킷이 공유한다** — `UpdateURecvState()`(`PlayerSession.cpp:101`)가 최고 시퀀스보다 크지 않은 것을 전부 드롭하므로, 발사처럼 드물게 나가는 unreliable 패킷은 **매 틱 나가는 이동 패킷보다 조금만 늦게 도착해도 핸들러에 도달하지 못한다.** reliable 쪽이 32비트 비트필드로 재정렬을 흡수하는 것과 달리 unreliable 에는 창이 없다. 따라서 **unreliable 로 오는 값에 "직전 값 + 1" 같은 연속성을 요구하지 말 것** — 2026-08-26 #0이 정확히 이것으로 터졌다(발사 한 장이 사라지면 이후 전부 거부). 단조 증가만 요구하고 간격은 받아들이는 형태로 쓸 것
- **reliable C2D 채널은 순서를 보장하지 않는다** — `PlayerSession::UpdateRRecvState()`(`PlayerSession.cpp:57-79`)는 최고 시퀀스 + 32비트 비트필드로 **중복만 거른다**. 늦게 도착한 낮은 시퀀스도 처음 보는 것이면 그대로 처리되고, reliable(rSeqNum)과 unreliable(uSeqNum)은 시퀀스 공간이 분리돼 두 채널 사이에는 순서 개념 자체가 없다. 따라서 **상태를 바꾸는 C2D 요청은 토글·증분이 아니라 절대 지정으로 설계할 것** — 재전송과 순서 역전에도 결과가 같아야 한다. 다른 조작과의 순서 역전은 `my_inventory_version` 같은 버전 가드로 막는다(`C2DRequestEquipItem`, `C2DRequestSwitchWeapon`이 쓰는 관용구). `PlayerInventory`의 변경 함수 11곳이 예외 없이 `_inventoryVersion`을 올리는 것이 이 가드의 전제다. **D2C 방향도 같다** — 서버가 같은 상태를 연달아 통보하면 클라이언트에 역순으로 도착해 낡은 값이 최종 상태로 남을 수 있다. 그래서 **상태 전체를 절대값으로 싣고**(증분·토글 금지) 클라이언트가 헤더 `rSeqNum`으로 낡은 통보를 버리게 한다. 이 규약이 필요한 통보는 현재 `D2CNotifyWeaponChanged` 하나이며, **`.proto` 주석에 있던 지시는 클라이언트 반영 완료로 삭제되어 지금은 클라이언트 저장소의 CLAUDE.md가 갖고 있다** — 새 통보에 이 규약이 필요해지면 서버가 `.proto`에 다시 적어 전달해야 한다
- **중계 통보를 설계할 때의 규칙 둘** — 2026-08-29 #0에서 확정. ① **C2D 패킷에 발신자 자신의 `object_id`를 싣지 말 것.** 서버는 세션에서 이미 알고 있고 브로드캐스트에는 그 값만 싣는다. 필드를 두면 남의 id를 실어 **그 플레이어가 무언가 하고 있는 것처럼 보이게 만드는** 통로가 생기며, 대조 검사를 붙여 막더라도 그 검사가 실제로 막는 것은 정상 클라이언트의 자기 id 오기입뿐이다. 필드를 없애면 스푸핑이 구조적으로 표현 불가능해진다. **`C2DRequestWeaponFire.weapon_dbid`나 `C2DRequestRecall.recall_spot_index` 같은 기존 에코와 혼동하지 말 것** — 그쪽은 서버가 모르는 값이거나(어느 요청에 대한 응답인지) 서버 상태와 대조해 **불일치 자체가 의미를 갖는** 값이다. 발신자 id는 둘 다 아니다 ② **재시작하는 단계 번호를 unreliable에 올릴 때는 수신 측이 각 단계를 독립적으로 다뤄야 한다.** 매번 0부터 다시 시작하는 번호는 재정렬이 일어나면 이전 회차의 0과 이번 회차의 0을 구분할 수 없고, `uSeqNum`에는 reliable의 32비트 비트필드 같은 흡수 창이 없어 조금만 늦게 도착해도 통째로 버려진다(위 `uSeqNum` 항목). **그럼에도 `C2D/D2CNotifyReloadSequence`를 unreliable로 확정한 근거는 그 번호가 상태가 아니라 연출 큐라서다** — 유실의 대가가 "적이 장전 중인 줄 몰랐다"이고 서버에 보정할 상태가 없어 reliable이 보장할 것이 없다. **따라서 판단 기준은 "번호가 재시작하는가"가 아니라 "수신 측이 직전 단계 도착을 전제하는가"다** — 전제하면(누적 상태를 쌓거나 단계 누락으로 멈추는 구성) reliable이 필요하고, 각 단계가 독립적이면 unreliable로 충분하다. 상태를 옮기는 번호는 여전히 단조 증가(`fire_sequence`)여야 한다 ③ **한 사건의 권위 있는 결과와 연출 신호는 채널을 나눠도 된다.** 재장전 완료가 그 형태다 — 당사자는 `D2CResponseReload`(reliable, 인벤토리 스냅샷)로 결과를 받고, 남들은 `D2CNotifyReloadSequence`(unreliable, 단계 15)로 연출만 받는다. 후자가 유실돼도 남의 화면에서 연출이 타임아웃될 뿐 어느 쪽 인벤토리도 어긋나지 않는다. **다만 발행 지점이 갈리면 안 된다** — 완료 단계를 클라이언트도 보낼 수 있게 두면 거부된 재장전이 남의 화면에서 완료로 보이므로, 서버가 발행하는 값은 클라이언트에서 오면 버릴 것
- **사격 거부는 조용하다** — `Handle_C2D_RequestWeaponFire`의 `weapon_dbid` 불일치·`fire_sequence` 되돌아감·**탄약 부족** 셋 모두 deny 패킷 없이 `return false`다. 거부는 `BroadcastExcept`보다 앞이라 데미지뿐 아니라 남에게 가는 발사 이펙트도 함께 사라진다. 클라이언트에는 "총이 안 나감"으로만 보이므로 증상을 만나면 서버 로그의 `[Handle_C2D_RequestWeaponFire]` 줄을 먼저 볼 것. **잔탄 0에 통보를 붙이지 않기로 확정했다**(2026-08-26 #5) — 근거는 빈도가 아니라 구조다. 클라이언트는 트리거를 당길 때 차감하고 서버는 발사 패킷을 **수락했을 때만** 차감하므로, 유실·재정렬이 나면 클라이언트만 줄고 서버는 그대로다(중복은 `uSeqNum`이 거른다). 즉 **서버 잔탄은 항상 클라이언트 잔탄보다 크거나 같아** 클라이언트가 먼저 마르고 재장전도 먼저 건다. "클라이언트는 남았다고 믿는데 서버가 0이라 갇히는" 상태는 정상 클라이언트에서 발생할 수 없고, 그 상태에 빠지려면 클라이언트가 고장났거나 조작된 경우인데 그때 통보는 목적에 맞지 않는다
- **탄창 폐기와 낙관적 탄종 기대는 둘 다 설계 확정이다** — 버그로 보고 고치려 들지 말 것(2026-08-26 #5에서 판정). ① **`UnloadMagazineToInventory()`가 같은 탄종 스택도 빈 칸도 없을 때 탄창을 폐기하고 `true`를 반환하는 것**(`PlayerInventory.cpp:82-85`)은 의도다. 미리 장전된 탄약은 이미 소비된 것에 준해 다루므로 자리가 없으면 그 자리에서 폐기하는 것이 범위 안이고, 인벤토리가 찼다는 이유로 무기 교체를 막는 쪽이 더 나쁘다. 딸려오는 것 — 이 함수에는 **`return false` 경로가 하나도 없어** 호출부 넷(`104, 134, 240, 267`)의 가드와 `DENY_MAGAZINE_UNLOAD_FAILED`(0x0100)가 죽은 코드다. "폐기하지 않고 거부한다"로 방향이 바뀔 여지를 남겨 그대로 뒀다 ② **`LoadMagazineFromInventory()`에 탄창 자신의 탄종 검사가 없는 것**도 의도다. 인벤토리 쪽 검사는 43행에 있고, 없는 것은 탄창이 이미 다른 탄종을 물고 있는 경우인데 — 무기 슬롯의 점유자를 바꾸는 네 경로가 **전부 스왑 앞에서** unload를 부르고 unload는 어느 갈래로 가도 탄창을 비우므로, 탄창이 담을 수 있는 것은 "그 슬롯 무기의 탄종" 아니면 "빈 것"뿐이다. 재장전은 점유자를 바꾸지 않아 이 불변식을 건드리지 않는다. **네 경로 중 하나라도 unload 없이 스왑하면 이 기대가 무너진다** ③ 딸린 사실 하나 — **재장전 결과는 클라이언트가 미러할 수 없다.** `LoadMagazineFromInventory()`가 인벤토리를 0번부터 훑어 같은 탄종을 찾고 필요한 만큼 여러 칸에서 나눠 빼며 0이 된 칸은 비우므로, 어느 칸이 몇 발씩 빠지는지가 서버의 스캔 순서에 달려 있다. 장착·이동이 인덱스와 버전만 주고 클라이언트가 자기 예측을 확정하는 방식(`D2CResponseEquipItem`)을 쓸 수 있는 것은 그쪽이 재현 가능한 단일 스왑이기 때문이고, 재장전이 응답에 인벤토리 전체를 싣는 것(2026-08-26 #6)은 이 성질에서 나온 결과다
- **와이어에서 "비어 있음"은 `0xFFFFFFFF`이고 `0`이 아니다** — proto3의 uint32 기본값이 0이라 0을 센티널로 쓰면 "서버가 명시적으로 비웠다"와 "필드를 안 실었다"가 구분되지 않는다(위 「가해자 id 필드」와 같은 형태의 사고다). 현재 일가는 셋 — `PLAYER_OBJECT_ID_SENTINEL`·`INVENTORY_VERSION_NOT_SET`(`ClientPacketHandler.cpp` 상단)과 `CombatObject::NO_ATTACKER`. **`SLOT_CLEARED_BLUEPRINT_ID`는 2026-08-26 #6에서 제거됐다** — 재장전 델타에서 "이 칸이 비워졌다"를 표현하려고 둔 것이었고 유일한 사용처였는데, 응답이 스냅샷으로 바뀌면서 빈 칸을 누락으로 표현하게 돼 필요가 없어졌다. **서버 내부의 `Slot::IsEmpty()`가 `blueprintId == 0`을 쓰는 것과 혼동하지 말 것** — 그 값은 와이어에 나간 적이 없고(`FillSlotProto` 호출부가 전부 `!IsEmpty()` 뒤다) 나가서도 안 된다
- **`D2CFullInventorySync`는 청킹도 크기 검사도 하지 않기로 확정** — 최악이 **834B 페이로드 / 869B 데이터그램**이라 실 MTU(1500 − IP 20 − UDP 8 = 1472B)의 59%다. **다만 이 경로에서 실제로 구속하는 것은 MTU가 아니라 수신 버퍼다** — `recvfrom`은 버퍼보다 큰 데이터그램을 단편화가 아니라 **절단**으로 처리하므로, 서버의 `UDPTask.h:24`(`_recvBuffer[1024]`)와 같은 크기를 클라이언트가 쓰고 있으면 조용히 잘린다. 그 더 빡빡한 기준으로 재도 85%라 여유가 있으므로, 이 메시지가 나가는 세 지점(`ClientPacketHandler.cpp:150, 879`와 재장전 응답이 중첩하는 `726`)을 `GameRoom.cpp:54, 103`의 `SAFE_PAYLOAD_LIMIT` 청킹이나 런타임 검사 없이 그대로 둔다. **최악값을 다시 뽑을 때 놓치기 쉬운 것 둘** — ① `instance_uid`는 `user_inventory.inventory_id`(BIGINT AUTO_INCREMENT)라 varint가 3~5B에 머문다(2026-08-26 #5의 1056B는 원소당 35B로 잡은 과대 추정이었고, #6이 재장전 응답에 이 메시지를 통째로 중첩할 수 있었던 근거가 이 재계산이다) ② **탄창 두 칸이 가장 비싼 슬롯이다** — `slot_index`가 -1인데 음수 `int32` varint는 10B라 인벤토리 칸(1B)보다 슬롯당 9B를 더 먹는다. 인벤토리 칸을 늘리거나 장착 슬롯을 추가하는 변경이 오면 이 계산을 다시 할 것
- **탄약 차감의 규약 둘** — ① **`_inventoryVersion`을 올리지 않는다.** 인벤토리를 바꾸므로 올려야 할 것처럼 보이지만, 발사 한 발마다 버전이 오르면 교전 중 클라이언트의 `my_inventory_version`이 상시 낡은 값이 되어 장착·무기 교체가 전부 `DENY_VERSION_MISMATCH`로 거부된다. 차감이 `PlayerInventory` 바깥에서 Mutable 게터로 슬롯을 직접 건드리는 것이 이 예외를 성립시키며, 클라이언트 잔탄과 서버 잔탄은 명시적 동기화(`D2CFullInventorySync`의 탄창 두 칸 — `D2CResponseReload`도 이것을 중첩해 싣는다) 전까지 어긋나는 것이 정상이다 — **어긋남을 버그로 보고 보정 로직을 넣지 말 것.** 서버가 최종 판정 권한을 갖는다 ② **잔량 0인 슬롯을 남기지 않는다.** `Slot::IsEmpty()`가 `quantity <= 0 && blueprintId == 0`이라 차감만 하면 "0발인데 비어 있지 않은" 슬롯이 생기고, `PlayerLootContainer::place()`가 그것을 채우려다 실패해 전리품 컨테이너에 빈 칸을 남기거나 `UnloadMagazineToInventory()`가 0발 탄약 스택으로 인벤토리 칸을 점유한다. 차감 직후의 `Clear()`가 둘을 원천 차단하므로 **수량을 줄이는 코드를 새로 쓸 때마다 같은 처리를 붙일 것**
- **플레이어 전리품 컨테이너의 용량에는 여유가 0이다** — `PlayerInventory::INVENTORY_SLOT_COUNT`(25) + 장착·탄창 5칸 = 30이 `Container::DEFAULT_CONTAINER_VOLUME`(30)과 정확히 같다. 인벤토리 칸이나 장착 슬롯을 늘리면서 `DEFAULT_CONTAINER_VOLUME`을 함께 늘리지 않으면 `Container::PlaceItem()`이 `false`만 반환하고 초과분을 **조용히 버린다**(반환값을 보는 호출부가 없다). `PlayerLootContainer.h`의 `static_assert`가 이 경우를 컴파일 타임에 잡으므로 빌드가 깨지면 두 상수를 같이 볼 것
- **`GameRoom::Update()`를 override하면 마지막에 베이스를 부를 것** — 베이스가 `BroadcastPlayerStates()`를 담당하므로 빠뜨리면 그 룸에서 플레이어가 서로 움직이지 않는다(윈체스터에서 실제로 일어났던 증상). 먼저 부르면 그 틱의 파생 로직이 만든 변화가 다음 틱에야 나가 상태가 한 틱 밀린다. 이 누락을 구조적으로 막는 NVI(비가상 `Update()` + 순수 가상 훅)는 **의도적으로 채택하지 않았다** — `Update`가 둘로 보이는 쪽의 인지 비용이 더 크다는 판단이므로, 이 규약은 규율로 지킨다
- **`CombatObject`는 `_dynamicObjects`에만 둔다** — 회수 경로인 `GameRoom::DestroyDeadObject()`가 `_dynamicObjects`만 보므로, 정적으로 등록된 전투 오브젝트는 죽어도 회수되지 않고 화면에서만 사라진다. 입구는 `CanBeStaticObject()`가 막지만(`SpawnStaticObject()` 세 갈래 모두에서 호출) 우회하면 성립한다. AI를 서버에서 다루지 않기로 했으므로 이 자리에 올 수 있는 것은 파괴 가능한 오브젝트뿐이다
- **컨테이너 경로에는 미결 둘이 남아 있다** — 2026-08-27 #2에서 파악됐고 고치지 않았다. ① **`C2DRequestOpenContainer`의 실패가 전부 조용하다** — 이미 다른 컨테이너가 열려 있음·룸 없음·오브젝트 못 찾음·`Container` 타입 아님 네 갈래가 모두 deny 패킷도 로그도 없이 `return false`다. 클라이언트는 열기가 거부됐는지 패킷이 유실됐는지 구분할 수 없고, 열렸다고 가정하고 진행하면 뒤이은 조작이 엉뚱한 로그로 나타난다 ② **`Handle_C2D_RequestInteractContainerObject`가 정상 조작의 거부에도 `DENY_SERVER_INTERNAL`(0x0200)을 쓴다** — 순서 역전으로 `C2DCloseContainer`가 먼저 처리된 경우가 대표적인데, 클라이언트에는 "서버 내부 오류"로 보여 재시도가 무의미하다는 판단조차 못 한다. 둘 다 증상이 아니라 **진단을 어렵게 만드는 구조**라, 컨테이너 관련 버그 제보를 받으면 이 둘을 먼저 의심할 것
- **파괴 가능하면서 컨테이너인 오브젝트는 만들지 않는다** — 만들면 약탈 중이던 세션의 `_interactingContainerId`가 사라진 오브젝트를 가리킨다. 아래 「플레이어 전리품 컨테이너에는 TTL이 없다」에서 예고한 문제가 이 조합에서 실현되므로, 필요해지면 제거 시 해당 id를 보는 세션을 찾아 `-1`로 되돌리는 처리가 함께 와야 한다
- **비플레이어 사망 경로는 아직 한 번도 실행되지 않는다** — `CombatObject`의 파생이 `PlayerObject`뿐이라 `FindNonplayerObject()`가 전투 오브젝트를 찾는 일이 없다. 검증용 더미 클래스를 만들지 않기로 했으므로 첫 파괴 가능 오브젝트를 만드는 시점에 피격→사망→회수→통보 전체를 그 자리에서 디버그할 것. 그때 지켜야 할 불변식 둘 — ① 회수 지시는 `TakeDamage()` 안이 아니라 **호출부가** 한다(안에서 `delete`하면 호출부가 직후에 읽는 `GetCurrentHp()`가 use-after-free) ② `OnDeathResolved()`에 `_dynamicObjects`의 반복자를 넘기지 않는다(훅이 흔적을 스폰하면 rehash로 무효화)
- **플레이어 전리품 컨테이너에는 TTL이 없다** — 룸 소멸까지 유지되고 `~GameRoom()`이 회수한다. 나중에 만료를 붙이려면 두 가지가 따라온다 — ① 약탈 중이던 세션의 `_interactingContainerId`가 사라진 오브젝트를 가리키게 되므로 소멸 시 해당 세션을 찾아 `-1`로 되돌려야 한다 ② `TimerExecuter`에 취소 API가 없어 잔여 콜백이 objectId 재조회 규약(세션의 sessionId 재조회와 같은 형태)을 따라야 한다. 지금은 둘 다 성립하지 않아 문제 자체가 없다
- **귀환의 설계 의도 셋은 미비점이 아니다** — ① **자발적 취소 패킷이 없다.** 존을 벗어나는 것이 유일한 취소 수단이고 요청 재전송은 "진행 중"으로 무시된다. 필요해지면 별도 패킷으로 추가한다 ② **존 검사는 `RecallTick`(1초 간격)에서만 한다.** 매 상태 갱신마다 전 플레이어의 위치를 검사하면 실제로 귀환 중인 소수를 위해 룸 전체가 비용을 무는 구조가 되고, 자발적 취소로 존을 벗어나는 플레이어가 경계선에서 알짱거릴 이유가 없으므로 틱 사이의 이탈·복귀는 허용한다 ③ **`RECALL_RESULT_PLAYER_DEAD` 경로는 현재 도달 불가**지만 남겨둔다 — 사망 시 `DetachPlayer()`의 `EndRecall()`이 generation을 올려 다음 틱이 조용히 종료되기 때문이며, 추후 멀티스레드 환경에서는 실제로 발생할 여지가 있다
- **귀환 유효성 검사는 안티치트가 아님** — 검사에 쓰이는 `PlayerObject::position`은 `ApplyState()`에서 클라이언트가 보낸 좌표를 그대로 기록한 값이다(`PlayerObject.cpp:10-13`). 좌표 조작 클라이언트는 항상 통과한다. 실질적 차단에는 서버 측 이동 검증(속도/텔레포트 체크)이 별도로 필요

---
