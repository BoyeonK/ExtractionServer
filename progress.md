# 진행 상황 정리 (2026-08-14 업데이트)


## 완료된 것들

### 네트워크 / 패킷
- [x] (2026-08-13 #3) `GameRoom`의 오브젝트 생성 브로드캐스트 구현 — `Spawn*Object()` 6곳(Test·Winchester × 정적/동적/플레이어)의 `TODO : 생성 정보를 broadcast` 해소. 비플레이어 오브젝트용 `D2CNotifySpawnObject`(단건, reliable) 신설, 플레이어 오브젝트는 기존 `D2CSpawnPlayerObject`를 재사용해 새 패킷 없음. 통보는 `GameRoom::NotifySpawnObject()` / `NotifySpawnPlayerObject()` protected 헬퍼 두 개로 모아 파생 룸 2종이 같은 코드를 갖지 않게 했다. **스폰은 한 게임에 몇 번 일어나지 않으므로 배칭·청킹 없이 오브젝트 1개당 패킷 1장을 스폰 함수 안에서 즉시 전송** — 지연 전송 큐를 두지 않으니 그 큐의 생명주기(룸 소멸 시 잔여 항목, 이탈 세션 참조)를 다룰 필요가 없다. 스폰 함수는 `try_emplace().second`를 확인해 실제로 등록된 경우에만 통보한다. `SpawnPlayerObject()`에 `ownerSessionId` 파라미터를 추가해 당사자를 `BroadcastExcept`로 제외 — 당사자는 `D2CResponseSpawnMeSpawnSpot`으로 이미 자기 오브젝트를 알고 있어 통보를 받으면 자기 분신이 하나 더 생긴다(현재 순서 `SpawnMe → LoadingComplete`에서는 아직 INPLAY가 아니라 필터에 자연히 걸러지지만, 전환 순서에 안전성을 의존하지 않는다). 정적 2곳은 런타임 스폰 경로가 없어 실제 트래픽은 0이나 훅은 동일하게 넣었다 (`External_Protocol.proto`, `enum.h`, `ClientPacketHandler.h/cpp`, `GameRoom.h/cpp`)
- [x] (2026-08-13 #4) 이탈 확정의 DB 반영 IPC 구현 — 매치 결과가 처음으로 영구 저장된다. `D2MNotifyPlayerLeft { uid, leave_reason, inventory_slots, equipment_slots }` 신설(pktId 103), 슬롯은 입장 때의 `PlayerInfo`와 같은 분할·인덱스를 써서 입퇴장을 대칭으로 맞췄다. 경로는 `PlayerSession::FinalizeLeave()` → `DediServerService::NotifyPlayerLeftToMain()` → Main `Handle_D2M_NotifyPlayerLeft()`(슬롯 인덱스를 DB 기준 80~104 / 105~107로 변환) → `NotifyPlayerLeftRequest`. 반영 규칙은 ①`uid < 0`(게스트)이면 DB 생략하고 락 해제만 — 게스트는 `guest_uid_counter`로 음수 uid를 받고 `users` 테이블에 행이 없어 `user_inventory.uid` FK가 INSERT를 거부하므로, "반출물 파기" 정책과 스키마가 같은 결론이다 ②`RECALLED`는 인벤토리+장착 교체 ③`DEAD`/`DISCONNECTED`는 해당 범위 삭제만(빈손). 창고(0~79)는 어느 경로에서도 건드리지 않는다. **Main에 상시 MySQL 연결을 두기로 결정** — DB 작업이 드물어 단일 스레드 루프를 조금 막아도 지장이 없고, Node에 위임하면 버그 결과가 Main으로 역전파되어야 하는 비효율이 생긴다는 판단. `sql::Connection`은 `sw::redis::Redis`와 달리 자동 재연결이 없어(`wait_timeout` 8시간·HeatWave 페일오버로 죽은 핸들이 됨) `MysqlHandle`을 신설해 `Get()`에서 `isValid()`→`reconnect()`를 처리하고, `main.cpp`의 블록 스코프 연결을 전역 `pMysql`로 승격(`InitializeItemCache()`도 이 핸들 사용). MySQL과 Redis는 공유 트랜잭션이 없으므로 **MySQL 먼저·락 해제 나중**(fail-closed) 순서를 지키고, 재시도 2회 후 최종 실패 시에는 락을 풀고 페이로드를 error 로그로 남긴다(레이드 하나 유실이 영구 잠금보다 낫다). 인라인 실행 대신 기존 `PendingDBRequest` 큐를 타는 이유는 이탈이 몰려서 발생하고(룸 하나당 4명) 순서 보장·재시도를 한 곳에 갇혀 두기 위함. `LeaveReason` 값 일치는 `static_assert` 4개로 검증. 부수적으로 `DetachPlayer()`의 인벤토리 소실 조건이 `DEAD`만이었던 것을 `DEAD || DISCONNECTED`로 넓혔다(끊김=사망 결정 반영, 시신은 `DEAD`에만 남긴다) (`IPC_Dedicate.proto`, `IPC_enum.proto`, `MysqlHandle.h/cpp`, `GlobalVariable.h/cpp`, `main.cpp`, `PacketHandler.h/cpp`, `DBProxyRequest.h/cpp`, `PlayerSession.h/cpp`, `DediServerService.h/cpp`, `GameRoom.cpp`, `CMakeLists.txt`)
- [x] (2026-08-13 #5) `active_match` 락의 임시 처리 3곳 제거 및 게임 중 해제 구멍 차단 — 락의 정상 해제 지점이 이탈 확정 한 곳으로 확정되면서 `match.js` TTL·`matchCancel` Lua·`redis_keys.md`의 `TEMP` 문구를 걷어냈다. 다만 **TTL을 완전히 없애면 계정이 영구 잠긴다** — 티켓 TTL은 300초인데 매칭이 성사되지 않으면 "티켓은 없고 락은 남은" 상태가 되고 복구 수단이 사라진다. 그래서 TTL을 300초→**3600초 백스톱**으로 조정(통보 유실·데디 크래시 대비, 최대 게임 길이보다 길어야 함)하고 `return 2`(만료 정리) 분기는 유지했다. 여기서 두 번째 구멍을 발견 — 게임이 300초를 넘기면 진행 중에도 티켓이 사라져 `/cancel`이 이를 "만료된 대기 티켓"으로 오해해 락을 풀 수 있었다(상태 검사 `status == SUCCESS`는 티켓이 살아 있을 때만 유효). `UpdateEntryTokenRequest::Execute()`가 게임 시작 시점에 락 값을 `INGAME:<ticketId>`로 덮어쓰고(`SET`이 TTL을 지우므로 백스톱 TTL을 재설정), `matchCancel`이 그 접두어를 검사해 거부(`return 3` → HTTP 409 `ERR_MATCH_IN_PROGRESS`)하도록 했다. `redis_keys.md`의 `active_match` 절을 해제 경로 3종 표로 재작성 (`match.js`, `DBProxyRequest.h/cpp`, `redis_keys.md`)
- [x] (2026-08-14 #0) 룸 소멸 경로 구현 — 매치 종료 경로의 마지막 조각. 진입점은 `GameRoom::CheckAllLeft()` 한 곳으로 유지하되 **그 자리에서는 예약만** 하고(`DediServerService::ReserveRoomDestroy()`) 실제 회수는 메인 루프의 새 단계 `DestroyPendingRooms()`에서만 수행한다 — `CheckAllLeft()`는 `UpdateGameRooms()`가 `_gameRooms`를 순회하는 도중 호출되므로 그 자리에서 `erase`+`ReleaseThis()`를 하면 순회 중인 맵과 실행 중인 객체가 동시에 무효화된다(이탈 처리의 "예약 후 루프 밖에서 수행"과 같은 형태). 룸이 자기 소멸을 지목할 수 있도록 `_roomId`를 멤버로 신설하고 파생 룸 생성자를 통해 초기화(`ObjectPool::Acquire()`의 완벽 전달로 호출부는 `Acquire(roomId)` 한 줄). **회수 주기는 1초** — 종료된 룸의 자원 회수를 서두를 이유가 없다. 지연 구간 동안 룸이 `_gameRooms`에 남아 계속 도는 것이 무해함을 세 경로 모두 확인했다(`ProcessLeaves()`는 전원 FINALIZED라 무동작, `TestGameRoom::Update()`는 `_playerObjects`가 비어 첫 줄에서 반환, `CheckRetransmits()`는 FINALIZED 세션을 건너뛴다). 세션 회수 순서는 ①`_tokenToPlayerSession` 엔트리 제거 ②`_players[id] = nullptr` ③`_freePlayerIds` 반납 ④`ObjectPool<PlayerSession>::Release()` — ①은 세션이 살아 있을 때만 토큰 문자열을 읽을 수 있어서, ②는 ③보다 먼저여야 잔여 지연 콜백이 재조회로 자멸하는 규약이 성립해서다. 오브젝트 3종 해제는 별도 정리 함수 대신 `~GameRoom()`으로 옮겼다 — `ObjectPool::Release()`가 `~T()`를 부르므로 `ReleaseThis()` 한 번으로 끝나고 호출 순서를 지킬 필요가 없다. 이 과정에서 `UnityGameObject`에 가상 소멸자가 없어 `UnityGameObject*`로 파생 오브젝트를 `delete`하면 UB였던 것을 발견해 함께 고쳤다(가상 함수는 이미 있었고 소멸 경로 자체가 없어 드러나지 않던 것). `_tokenToPlayerSession` 정리는 **오늘 시점에는 죽은 코드**다 — INIT 세션은 이탈 확정에 도달할 수 없고(세 이탈 경로가 모두 INPLAY나 재전송 큐를 요구), 접속한 세션은 `/connect`가 이미 바인딩을 마쳐 토큰이 지워져 있다. 그럼에도 넣은 이유는 두 전제 중 하나만 깨지면(입장 타임아웃 도입, 바인딩 IPC 유실) 풀 재사용된 남의 세션에 IP가 덮어써지고, `_clientAddr`이 곧 송신 목적지라 정상 플레이 중인 유저의 모든 패킷이 엉뚱한 주소로 나가면서 로그에는 "연결 끊김"만 남기 때문 (`GameRoom.h/cpp`, `DediServerService.h/cpp`, `DedicateMain.cpp`, `UnityGameObjects/UnityGameObject.h`)
- [x] (2026-08-14 #1) 룸 회수의 `_allocatedPlayers` 감소 경로 구현 — `D2MNotifyRoomDestroyed { room_id, player_count }` 신설(pktId 104). `M2DSession::GetAffordablePlayers()`가 프로세스 재사용 여부를 결정하는데 `_allocatedPlayers`에 감소 경로가 없어 "현재 수용 인원"이 아니라 **누적 인원**으로 동작했고, 프로세스당 50명 상한 / 룸 4인이므로 한 프로세스가 12개 룸을 처리하면 영구히 만석이 되어 **동시 접속과 무관하게 매치 12번마다 프로세스가 하나씩 늘어났다**(한계선은 UDP 포트 범위 101개, 그 뒤로는 자식이 `InitUDP()` 실패로 죽어도 Main이 모른 채 매치가 `_tempMatchPkts`에 갇힌다). 경로는 `DediServerService::DestroyRoom()` → `NotifyRoomDestroyedToMain()` → Main `Handle_D2M_NotifyRoomDestroyed()` → `M2DSession::ReleasePlayers()`. **플레이어 단위가 아니라 룸 단위로 결정** — `D2MNotifyPlayerLeft`에 얹어 1씩 빼는 방법도 가능했지만, Main에는 룸 개념이 없어 나중에 룸 개수로 상한을 바꾸려면 어차피 이 패킷이 필요하고, 감소 시점이 실제 자원 반납 시점(이탈 확정이 아니라 회수 완료)과 일치한다. 인원 수는 세션 반납 루프에 들어가기 전에 확보하며(반납 후에는 맵이 의미를 잃는다), 전송 실패 시 로그만 남기고 회수는 계속 진행한다 — 여기서 멈추면 용량뿐 아니라 메모리까지 함께 샌다. 발신자는 `dynamic_cast<M2DSession*>`으로 검증하고, 결과가 음수면 error 로그 후 0으로 clamp한다(음수는 `GetAffordablePlayers()`가 상한을 넘겨 과배정을 부르므로 신호 하나를 잃는 것보다 나쁘다). `room_id`는 Main이 쓰지 않지만 두 프로세스의 로그를 대조할 유일한 열쇠라 실었다. 착수 전 "증가분과 룸 인원이 어긋날 수 있는가"를 먼저 검사해 **어긋날 수 없음을 확인**하고 보정 로직을 넣지 않기로 했다(근거는 아래 「진행 고려사항」) (`IPC_Dedicate.proto`, `IPC_enum.proto`, `PacketHandler.h/cpp`, `M2DSessions.h/cpp`, `DediServerService.h/cpp`)

### 코드 규약 / 문서
- [x] (2026-08-13 #2) `OPTION:` 주석 카테고리 도입 — 기존 `TODO:`(아직 구현되지 않은 것) / `TEMP:`(테스트를 위해 의도적으로 막아둔 것)에 더해, **없어도 동작에 문제가 없고 여유가 생기면 구현할 개선 항목**을 `OPTION:`으로 구분한다. `TEMP:`와 달리 릴리스를 막지 않는 것이 구분점. 첫 적용은 `GameRoom::CheckAllLeft()`의 입장 타임아웃 — 한 번도 접속하지 않은 세션(`INIT`)은 이탈 확정에 도달하지 못해 전원 이탈 조건이 성립하지 않으나, 매칭까지 마친 유저가 끝내 접속하지 않는 경우가 드물어 우선순위가 낮다 (`GameRoom.cpp`)
- [x] (2026-08-13 #6) `RedisProxyService` → `DBProxyService` 리네임 — MySQL 작업까지 겸하게 되어 이름이 실제 역할과 어긋났다(코드에 "큐 이름은 Redis 전용처럼 보이지만 실제 역할은 블로킹 외부 저장소 작업 큐"라는 **주석으로 이름을 변호하고 있던 것**이 신호였다). 자식 프로세스 대신 처리한다는 `Proxy`의 의미는 저장소 종류와 무관하게 유효하므로 유지하고 `Redis` 부분만 교체 — `RedisProxyService`→`DBProxyService`, `PendingRedisRequest`→`PendingDBRequest`, `RegisterRedisRequest()`→`RegisterDBRequest()`, `pRedisProxyService`→`pDBProxyService`, `RedisProxyRequest.h/cpp`→`DBProxyRequest.h/cpp`(`git mv`로 이력 유지), `hasRedisWork`→`hasDBWork`. 이름만으로 끝나지 않는 두 곳을 함께 정리 — ① `Execute(sw::redis::Redis*)`에서 파라미터 제거. Redis만 주입받고 MySQL은 전역으로 접근하는 비대칭이 남으면 이름이 또 어긋난다. 둘 다 전역이므로 파생 클래스가 필요한 것만 쓰고, 저장소가 늘어도 시그니처가 흔들리지 않는다. `DBProxyService`의 `_pRedis` 멤버·생성자 인자도 제거(`pRedis`가 이미 전역이라 주입에 실익이 없었다) ② 서비스 클래스 선언·구현을 `RedisHandler.h/cpp`에서 `DBProxyRequest.h/cpp`로 이동. `RedisHandler`는 실제로 Redis 캐시를 만드는 `InitializeItemCache()`만 남아 이름이 맞다. 이 이동으로 `main.cpp`가 `RedisHandler.h`를 통해 간접적으로 받고 있던 클래스 정의가 끊겨 `DBProxyRequest.h`를 직접 include하도록 고쳤다(`PacketHandler.cpp`는 원래부터 직접 include). 루트·`src/` CLAUDE.md의 `Redis Proxy (자식 프로세스 대신 Redis 처리)` 표기도 갱신 (`DBProxyRequest.h/cpp`, `RedisHandler.h/cpp`, `GlobalVariable.h/cpp`, `main.cpp`, `PacketHandler.cpp`, `CMakeLists.txt`, `CLAUDE.md`, `src/CLAUDE.md`)
- [x] (2026-08-13 #7) 흐름 서술 주석 일괄 제거 — 주석 밀도가 높은 상위 8개 파일에서 **주석 330줄 삭제, 코드 변경 0**. 기준은 "코드가 그대로 말하는 것은 쓰지 않는다" — 흐름 서술, 단계 나열, `── 구획 ──` 배너, 함수·타입명의 반복, 설계 배경 서술을 지웠다. 남긴 것은 ①`TODO:`/`TEMP:`/`OPTION:` ②코드에 숫자만 남는 상수의 유래(`1024-45`의 헤더 35B+여유 10B, 장착 슬롯 105·106, `matchCancel` Lua 반환 코드, xxHash 시드 0) ③UDP 헤더 필드의 바이트 레이아웃 ④주석 처리된 옛 코드. 지우면서 코드에서 사라진 제약 4가지(`INGAME:` 접두어의 파일 간 결합, 이탈 ②③의 호출 지점 제약, 지연 콜백의 sessionId 재조회 규약, `_playerObjects` 순회 중 변형 회피)는 **이 문서의 「진행 고려사항」과 `redis_keys.md`에 같은 내용이 이미 있어 정보 자체는 보존된다** — 앞으로도 이런 제약은 코드 주석이 아니라 문서 쪽에 적는다. 부수적으로 주석이 사라져 홀로 남은 `SetLeaveNotifyRSeq()` 선언을 다른 setter 옆으로 옮겼고, 파일 재작성 과정에서 LF로 바뀐 `GameRoom.h`의 줄바꿈을 CRLF로 되돌려 diff 부풀음(191줄→42줄)을 정리했다 (`ClientPacketHandler.h/cpp`, `PlayerSession.h`, `GameRoom.h/cpp`, `DBProxyRequest.cpp`, `match.js`, `ipcManager.js`)
- [x] (2026-08-13 #8) 주석 정리를 나머지 37개 파일로 확장 — #7과 같은 기준으로 코드베이스 전체를 훑어 마무리했다. 두 차례 합산 **주석 620줄→201줄, 뒤에 붙은 주석 93줄→46줄(총 466줄 제거), 코드 변경 0**. 절반만 정리된 상태로 두면 새 코드를 쓸 때 어느 쪽을 따를지 매번 흔들리므로 규약이 서지 않는다는 판단. 다음 작업(룸 소멸)이 얹힐 `DediServerService.h/cpp`를 우선 처리했다. 남은 247줄의 내역은 `TODO:` 28 / `TEMP:` 10 / `OPTION:` 3 / 주석 처리된 옛 코드 14, 나머지는 상수·인코딩 유래다 — 슬롯 범위, 쿼터니언 압축 상수(`1.41421356f`·`511.5`·`[-0.707106, 0.707106]`), `CombatObject`의 100배 스케일, `damageReductionRate`의 만분율, Lua 반환 코드, 컨테이너의 key/index 의미, `TimerExecuter`의 `_aliveToken` 선언 요구사항. **`src/sample.h`는 의도적으로 제외** — io_uring 사용법 학습 노트라 설명이 곧 내용이며, 정리 대상으로 본다면 파일째 삭제가 맞다. 검증은 diff에서 주석이 아닌 줄이 바뀌었는지 훑는 방식으로 두 배치 모두 수행했고 코드 토큰 변경은 0이었다 (`MapDataManager.h`, `PlayerSession.cpp`, `DBProxyRequest.h`, `DediSessions.cpp`, `DediServerService.h/cpp`, `M2DSessions.cpp`, `Matchmaker.cpp`, `PacketHandler.cpp`, `DediManager.cpp`, `PlayerInventory.h/cpp`, `UnityGameObject.h`, `CombatObject.h`, `PlayerObject.h`, `TimerExecuter.h`, `MysqlHandle.h/cpp`, `main.cpp`, `EnvSetter.cpp`, `IoUringWrapper.h/cpp`, `SocketWrapper.h/cpp`, `NetAddress.cpp`, `RecvBuffer.cpp`, `ObjectPool.h`, `enum.h`, `DedicateMain.cpp`, `HTTPserver.cpp`, `RedisHandler.cpp`, `auth.js`, `items.js`, `index.js`, `middleware/auth.js`, `mysqlClient.js`, `redisClient.js`)
- [x] (2026-08-14 #2) 빌드 시 사본 갱신 누락 수정 — `.proto`를 재생성하고 `make`만 돌리면 **옛 헤더로 빌드되던** 문제(pktId 104 추가 직후 실제로 발생). `Protocol/Compiled/IPC/` → `src/IPCProtocol/` 복사가 `file(COPY)`, 즉 **CMake 구성 시점 명령**이라 빌드 그래프에 없었다. 목적지 파일이 갱신되지 않으니 make는 재빌드할 이유조차 없다고 판단해, `cmake`를 다시 돌리기 전까지 영원히 옛 산출물로 컴파일된다. 파일 6개(`IPC_HTTP`/`IPC_Dedicate`/`IPC_enum` × `.cc`/`.h`)를 각각 `add_custom_command`의 `OUTPUT`으로 선언하고 원본을 `DEPENDS`로 걸어 그래프에 편입했고, `.pb.h`를 include하는 TU가 많아 `add_custom_target(CopyIpcPb)` + `add_dependencies`로 타깃 수준의 순서까지 못박았다. **`copy_if_different`를 쓴 이유** — 스크립트가 `.proto` 하나만 고쳐도 셋을 모두 재생성하므로, 그냥 `copy`면 내용이 같은 나머지 둘의 mtime이 갱신돼 관련 TU가 전부 재컴파일된다(대신 내용이 같으면 복사 명령 자체는 매 빌드 재실행되나 프로세스 6개 수 ms라 훨씬 싸다). `file(GLOB)`은 제거하고 이름을 명시 — `PROJECT_SOURCES`에 같은 셋이 이미 하드코딩돼 있어 실익이 없었고 GLOB은 새 파일을 재구성 전까지 모른다. 같은 성질의 POST_BUILD 복사 2개도 함께 옮겼다 — **POST_BUILD는 타깃이 재링크될 때만 실행되므로** `.env`만 고치면 빌드 디렉터리 사본이 옛것으로 남고, `IPC_HTTP.proto`의 Node 측 사본은 빌드가 실패하면 아예 동기화되지 않았다. `.env` 부재 시 동작은 빌드 실패에서 경고 후 건너뜀으로 완화했고(빌드 입력이 아니라 런타임 설정), 반대로 `Protocol/Compiled/IPC/` 산출물 부재는 `FATAL_ERROR`로 잡아 어떤 파일이 없고 무엇을 실행해야 하는지 알린다. **External 프로토콜은 손대지 않았다** — protoc가 `src/DedicateProcess/ExternalProtocol/`에 직접 생성해 경유지가 없으므로 이 문제가 성립하지 않는다 (`CMakeLists.txt`)

---

## 진행 중 / 다음 할 것들

### 진행 우선사항
1. **시신 컨테이너 및 사망 연출** — 사망 시 이탈 처리(오브젝트 제거·퇴장 통보·인벤토리 소실)는 구현 완료. 남은 것 셋
    - **시신 컨테이너** — `GameRoom::DetachPlayer()`의 TODO 자리(오브젝트 제거 **직전**, `position`과 인벤토리를 아직 쥐고 있는 유일한 지점)에서 `Container` 파생 오브젝트를 스폰하고 `Clear()` 대신 인벤토리를 그쪽으로 '이동'시킨다. `Container::PlaceItem()`이 protected이므로 `PlayerInventory`를 통째로 받아 채우는 전용 파생 클래스가 낫다. **의존하던 두 조각이 모두 완성됐다** — `SpawnDynamicObject()`가 생성 통보까지 보내므로 그것만 호출하면 클라이언트에 보이고, `~GameRoom()`이 `_dynamicObjects`를 해제하므로 시신의 수명 문제도 없다
    - **사망 연출** — 현재는 `D2CDespawnPlayerObject(DESPAWN_DEAD)`로 제거만 통보한다. 킬 피드·사망 애니메이션이 필요하면 별도 패킷
    - **비플레이어 전투 오브젝트의 사망** — `GameRoom::ProcessLeaves()`의 TODO. 세션이 없어 이탈 경로를 탈 수 없다. `FindNonplayerObject()` → `dynamic_cast<CombatObject*>`로 타입 판별 후 처리
2. **플레이어 장비 변화 브로드캐스팅** — EquipItem 성공 시 같은 방의 다른 플레이어에게 장비 변경 사항을 전송하는 로직 구현 필요. 전송 수단(`GameRoom::BroadcastExcept`)은 준비됐으므로 전용 패킷 정의만 추가하면 된다 (`ClientPacketHandler.cpp` TODO)
3. **HeartBeat / RequestBlueprint / RequestSpawnMe 클라이언트 연동 테스트 필요** — 프로토콜·직렬화 수정 완료, 빌드 통과. 클라이언트 측 구현 필요
    - `[DROP] 서명 불일치` 출력 → 클라이언트 서명 계산 로직 점검
    - `[DROP] unreliable 시퀀스 중복` 출력 → 클라이언트 uSeqNum 증가 로직 점검
    - StaticObjects 역직렬화 시 `TransformInfo` 구조 변경 반영 여부 클라이언트 측 확인 필요
    - `TestGameRoom::InitTestGameRoom()`에서 TestItemBox 등록 → Blueprint 응답(StaticObjects 청크)에 포함되는지 확인
    - **오브젝트 생성 브로드캐스트 수신 처리 미구현** — ① `D2CNotifySpawnObject`(id 36) 핸들러 신설. 페이로드가 `D2CResponseSpawnByObjectId`와 동일하므로 기존 역직렬화를 재사용할 수 있다 ② `D2CSpawnPlayerObject`(id 13)를 **요청 없이 서버가 먼저 보내는 경우**의 처리 — 지금까지는 `C2DRequestSpawnByObjectId`의 응답으로만 도착했다
4. `GameRoom::Update()` 추가 게임 로직 구현 — `TestGameRoom::Update()`의 PlayerState 브로드캐스트 완료. `WinchesterGameRoom::Update()` 로직 미구현(빈 함수). AI, 이벤트 등 추가 게임 루프 로직 필요 (`GameRoom.h/cpp`)
5. **죽은 Dedicate 자식 프로세스를 Main이 인지하지 못한다** — `waitpid`/`SIGCHLD` 처리가 없고 `M2DSession::SessionState::Terminated`를 설정하는 지점도 없다. 자식이 `InitUDP()` 실패(포트 고갈)나 크래시로 죽어도 `_dediSessions`에 살아 있는 것으로 남아 매치가 배정되고, 그 매치는 `_tempMatchPkts`에 갇힌 채 유저는 입장 정보를 받지 못하며 `active_match` 락이 백스톱 TTL(3600초)까지 잠긴다. 좀비 프로세스도 함께 쌓인다. 프로세스를 종료하지 않기로 한 결정과 무관하게 필요한 경로다
6. **윈체스터 귀환 영역 좌표 확정** — `MapDataManager.h`의 `_winchesterRecallZones`(북/남/동 3개)는 아직 자리표시자 값. 실제 맵 지오메트리 및 클라이언트 트리거 콜라이더와 대조하여 확정 필요. 서버 반경 ⊇ 클라이언트 트리거(약 +0.5m 여유)를 지킬 것 — 서버 쪽이 좁으면 "귀환 UI는 떴는데 서버가 거부"하는 버그가 됨. 튜토리얼 맵은 확정 완료
7. **연결 끊김 유예(3초) 재검토** — `PlayerSession::LEAVE_GRACE_MS_DISCONNECTED`. DB 반영이 붙었으므로 **오탐의 비용이 실재화됐다** — 끊김을 사망과 동일 처리하기로 했으니 오탐 한 번이 곧 아이템 전소이고, 재접속 복귀는 구현하지 않기로 결정된 상태다. 3초는 이 비용에 비해 짧다
    - 유예를 늘리는 방향은 두 관점이 일치한다. 오탐 흡수 외에 **전투 로그아웃 완화 효과**가 생겼기 때문 — 유예 동안 아바타가 INPLAY로 남아 있어 공격자가 그 안에 마무리하면 사유가 `DEAD`로 확정되고 시신이 남는다. 즉 "불리하면 회선 끊기"에 회수 창을 주는 셈이다
    - 늘릴 때는 재전송 큐 비용을 함께 볼 것 (아래 "응답 없는 세션의 재전송 큐" 항목)
8. **탄창 잔탄이 반출되지 않는다** — `PlayerSession::SerializeInventoryForIPC()`의 TODO. 입장 시 `LoadMagazineFromInventory()`가 인벤토리 탄약을 탄창으로 옮기는데 DB에 탄창 대응 슬롯이 없어, 귀환에 성공해도 총에 든 탄약만큼이 사라진다. 탄창을 인벤토리로 되돌린 뒤 직렬화할지 결정 필요 — 되돌리면 빈 슬롯이 없을 때의 처리가 따라온다

### 진행 고려사항
- **`TODO:` / `TEMP:` / `OPTION:` 주석 구분** — `TODO:`는 아직 구현되지 않은 것, `TEMP:`는 테스트를 위해 의도적으로 값을 제한하거나 코드를 막아둔 것, `OPTION:`은 없어도 동작에 문제가 없고 여유가 생기면 구현할 개선 항목이다. 릴리스를 막는 것은 `TEMP:`뿐이므로 릴리스 전 되돌려야 할 항목은 `grep -rn "TEMP" src HTTPServer`로 전수 확인할 것 (현재 8곳). `OPTION:` 항목은 할 일 목록에 올리지 않고 주석으로만 남긴다
- **코드 주석은 이 세 종류와 상수 유래뿐** — 흐름 서술("~를 전송한다"), 단계 나열, 구획 배너, 설계 배경은 코드에 쓰지 않는다. 코드만 봐서는 알 수 없는 제약·불변식·결정 배경은 **이 문서의 「진행 고려사항」에 적는다** (2026-08-13 #7·#8에서 코드베이스 전체 466줄을 이 기준으로 정리). 코드에 남기는 예외는 숫자만 남는 상수의 유래(슬롯 범위, 페이로드 한계, 프로토콜 반환 코드 등)와 바이트 레이아웃. `src/sample.h`만 예외로 남겨뒀다 — 학습 노트 성격이라 정리 대상으로 본다면 파일째 삭제가 맞다
- **`active_match` 락은 네 곳이 한 묶음** — `match.js`의 `/start` EX 값, `matchCancel` Lua의 `return 2`/`return 3` 분기, `DBProxyRequest.h`의 `ACTIVE_MATCH_TTL_SEC`, `redis_keys.md` 2번 절이 서로를 전제한다. TTL(3600초)은 **백스톱일 뿐**이고 정상 해제는 이탈 확정 한 곳이다. 세 가지 불변식을 깨지 말 것 — ① TTL은 최대 게임 길이보다 길어야 한다(짧으면 게임 도중 락이 풀린다) ② `match.js`의 EX와 `ACTIVE_MATCH_TTL_SEC`는 같은 값이어야 한다 ③ `INGAME:` 접두어는 `UpdateEntryTokenRequest`가 쓰고 `matchCancel`이 읽는다. 티켓 TTL(300초)이 락 TTL보다 짧아 생기는 "티켓 없고 락만 남은" 구간을 이 접두어로만 구분할 수 있다
- **MySQL과 Redis 사이에는 공유 트랜잭션이 없다** — 이탈 확정 반영은 **MySQL 먼저, 락 해제 나중**(fail-closed) 순서에 의존한다. 락을 먼저 풀면 DB 반영 실패 시 유저가 반영 안 된 인벤토리로 새 매치를 시작한다. 반대로 실패 시 락을 계속 잡으면 영구 잠금이 되므로, 재시도 2회 후에는 락을 풀고 페이로드를 error 로그로 남긴다. 이 순서를 바꾸는 변경은 `NotifyPlayerLeftRequest::Execute()` 안에 갇혀 있다
- **`sql::Connection`은 자동 재연결이 없다** — `sw::redis::Redis`와 달리 커넥션 풀도 재연결도 없어, `wait_timeout`(기본 8시간)이나 HeatWave 페일오버로 끊기면 이후 모든 사용이 예외를 던진다. **MySQL을 쓰는 코드는 반드시 `pMysql->Get()`으로 핸들을 받을 것** — 그 안에서 `isValid()`→`reconnect()`가 처리된다. `sql::Connection*`를 어딘가에 캐시하면 이 보호가 무력화된다
- **`CombatObject::TakeDamage()` 감소율 적용 범위 미확정** — armor 착용 중이어도 실드가 0으로 소진되면 `_currentShield -= damage`가 즉시 음수가 되어 penetrated 경로를 타고 `damageReductionRate`가 전혀 적용되지 않는다. "실드 잔량이 있을 때만 감소율 적용"이 의도인지, armor 착용만으로 감소율이 상시 적용되어야 하는지 확인 필요 (`CombatObject.h:46`)
- **IPC 프로토콜 파일을 추가하면 `CMakeLists.txt` 두 곳을 같이 고칠 것** — `PROJECT_SOURCES`의 `.pb.cc`/`.pb.h` 목록과 `IPC_PB_NAMES`. 한쪽만 고치면 컴파일 대상에서 빠지거나 `Protocol/Compiled/IPC/` → `src/IPCProtocol/` 복사가 누락된다. External 프로토콜은 protoc가 `src/` 아래로 직접 생성하므로 `PROJECT_SOURCES`만 보면 된다
- **`_allocatedPlayers`의 증감은 두 곳뿐이고 짝이 맞는다** — 증가는 `M2DSession::AllocatePlayers()`의 `+= ticketVec.size()`, 감소는 `ReleasePlayers()`가 받는 `player_count`(= 룸의 `_playerSessions.size()`)뿐이다. 둘이 같다는 보장은 코드에 드러나지 않으므로 근거를 남긴다 — ① `MatchMaker::VerifyAndSetMatchStatus()`의 Lua가 그룹 전원의 티켓 해시 존재와 `WAITING` 상태를 원자적으로 확인한 뒤 `INPROGRESS`로 바꾸고, 1을 반환했을 때만 `DistributePlayerGroup()`이 호출된다 ② Main은 단일 스레드이고 그 검증부터 `MakeM2DMakeRoomForThisGroup()`의 `hgetall`까지 연속 호출이라 중간에 io_uring CQ도 다른 핸들러도 끼어들지 못한다(가용 세션이 없을 때의 `fork`+`execl`만 들어오며 Redis를 건드리지 않는다) ③ 유일한 외부 삭제자인 Node `matchCancel` Lua는 `WAITING`일 때만 `DEL`하므로 `INPROGRESS` 티켓을 지우지 못한다. 따라서 `MakeM2DMakeRoomForThisGroup()`의 `if (val.empty()) continue`는 정상 동작에서 도달하지 않고 `player_infos` 크기는 `ticketVec.size()`와 같다. **이 세 조건 중 하나라도 바꾸면 카운트가 어긋나 프로세스가 다시 누적 증가한다.** 남는 이론적 구멍은 티켓 TTL(300초) 만료가 ①과 ② 사이 마이크로초에 걸리는 경우와 Redis eviction뿐이라 대응하지 않기로 했다
- **`MakeM2DMakeRoomForThisGroup()`에 예외 처리가 없다** — `hgetall`이 던지면 `_allocatedPlayers`는 이미 증가한 뒤라(`M2DSessions.cpp:77`이 패킷 생성보다 앞) 그만큼 영구히 남는다. 다만 같은 예외가 매치메이킹 루프 전체를 빠져나가는 쪽이 더 큰 사안이므로, 카운트 문제가 아니라 예외 안전성 작업으로 따로 다룬다
- **전용 프로세스는 룸이 비어도 종료하지 않는다** — 서버 가동 중 관측된 최대 수용 인원만큼의 프로세스를 유지하고, 이후 인원이 줄어도 회수하지 않는다는 결정(fork·exec과 초기화 비용 회피). 이에 따라 `GameRoom::CheckAllLeft()`에 있던 "남은 룸이 없을 때의 프로세스 정리" TODO를 제거했다. 딸려오는 것 둘 — ① 이 결정의 전제(프로세스 수가 최대 동시 인원에 수렴한다)는 `_allocatedPlayers` 감소 경로가 붙어서야 성립한다 (2026-08-14 #1에서 구현 완료) ② 유휴 프로세스도 `DedicateMain`의 1ms 슬립 루프 때문에 초당 1000회 깨어난다. 수십 개가 되면 폴링만으로 상시 CPU를 먹으므로 룸 0개일 때의 백오프(1ms→50ms)가 필요해지지만, **예상 프로세스 수가 2개 규모라 지금은 보류**한다
- **지연 콜백은 예외 없이 sessionId 재조회 규약을 따를 것** — `TimerExecuter`에 취소 API가 없어 룸/세션이 정리된 뒤에도 잔여 콜백이 실행될 수 있다. 콜백이 `PlayerSession*`를 캡처하지 않고 sessionId로 매번 재조회하며 uid·generation을 검증하면(현재 `RecallTick()`이 이 방식), 정리 시 `_players[id] = nullptr`만으로 잔여 콜백이 스스로 포기하므로 별도의 정지 확인(quiescence) 로직이 필요 없다. **한 곳이라도 raw 포인터를 캡처하면 이 설계가 무너진다.** 룸 소멸로 sessionId 반납이 실제로 시작됐으므로 이 규약은 이제 선택이 아니다 — `_freePlayerIds`가 FIFO라 반납된 id는 앞선 것들이 모두 소진된 뒤에야 재사용되어 간격 자체는 넉넉하지만, 재사용이 아예 없던 이전과 달리 규약을 어긴 콜백은 살아 있는 남의 세션을 잡는다
- **reliable in-flight 33개 초과 시 영구 ACK 불가** — ACK 커버리지는 `ackRSeqNum` 1개 + 비트필드 32개 = 총 33개다(`PlayerSession.cpp:85`, `120-130`). `_pendingReliable`에 34개 이상이 동시에 쌓이면 가장 오래된 것은 클라이언트가 이미 받았어도 알릴 수단이 없어 MAX_RETRY까지 재전송된다(수신 측은 `UpdateRRecvState()`에서 `diff > 32`로 버리므로 정확성은 유지, 대역폭만 낭비). **reliable 브로드캐스트가 셋으로 늘었다** — `D2CDespawnPlayerObject`(이탈 1건당 1장), `D2CNotifySpawnObject`·`D2CSpawnPlayerObject`(런타임 스폰 1건당 1장). 스폰·이탈 모두 한 게임에 몇 번뿐이고 룸 인원이 4명이라 아직 여유가 크지만, 이 경로가 늘어나면 도달 가능해진다. 대량 통보는 개별 전송 대신 청킹으로 묶을 것. 근본 해결은 비트필드 64비트 확장 또는 송신 창 제한이며 별도 작업
- **응답 없는 세션의 재전송 큐는 이탈 처리로만 정리된다** — MAX_RETRY 초과 패킷은 `DediServerService.cpp`에서 제거되지 않고(제거 경로는 `ProcessIncomingAck()` 뿐), `GameRoom::DetachPlayer()`의 `ClearPendingReliableExcept()`가 유일한 일괄 정리 지점이다. 즉 **끊김 판정 → 3초 유예 → 분리**까지는 큐가 그대로 쌓인 채 `GetRetransmitCandidates()`가 매번 전체를 순회한다. 유예가 짧아 실질적인 손해는 없으나, 유예를 늘릴 때는 이 비용을 함께 볼 것
- **브로드캐스트 페이로드 재직렬화는 의도적 보류** — N명에게 보낼 때 페이로드가 N번 직렬화된다. 세션마다 헤더·서명이 달라 `SendBuffer` 재사용이 불가능하지만 **페이로드 바이트 자체는 동일**하므로 1회 직렬화 + memcpy로 줄일 수 있다. 다만 4인/100ms 기준 룸당 절약분이 ~3μs, 250룸이어도 코어의 0.75% 수준이고 같은 경로의 `sendto` 비용이 이를 압도해 실익이 없다. `Make*FromPayload` 짝 함수가 패킷마다 늘어나 `pktId`/`reliable`이 어긋날 여지가 생기는 쪽이 손해. **재검토 조건: 룸 인원 10명 초과 / 틱 25~50ms로 상향 / 동시 룸 수 세 자릿수.** 변경 영향은 `ClientPacketHandler`와 브로드캐스트 두 함수 안에 갇혀 있다
- **귀환 유효성 검사는 안티치트가 아님** — 검사에 쓰이는 `PlayerObject::position`은 `ApplyState()`에서 클라이언트가 보낸 좌표를 그대로 기록한 값이다(`PlayerObject.cpp:10-13`). 좌표 조작 클라이언트는 항상 통과한다. 실질적 차단에는 서버 측 이동 검증(속도/텔레포트 체크)이 별도로 필요

---
